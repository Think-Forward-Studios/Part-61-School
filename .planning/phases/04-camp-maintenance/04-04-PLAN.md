---
phase: 04-camp-maintenance
plan: 04
type: execute
wave: 4
depends_on: ['04-03']
files_modified:
  - apps/web/app/(app)/admin/maintenance/page.tsx
  - apps/web/app/(app)/admin/aircraft/[id]/MaintenancePanel.tsx
  - apps/web/app/(app)/admin/aircraft/[id]/page.tsx
  - apps/web/app/(app)/admin/aircraft/[id]/maintenance/page.tsx
  - apps/web/app/(app)/admin/squawks/page.tsx
  - apps/web/app/(app)/admin/squawks/[id]/page.tsx
  - apps/web/app/(app)/admin/work-orders/page.tsx
  - apps/web/app/(app)/admin/work-orders/[id]/page.tsx
  - apps/web/app/(app)/admin/ads/page.tsx
  - apps/web/app/(app)/admin/ads/[id]/page.tsx
  - apps/web/app/(app)/admin/parts/page.tsx
  - apps/web/app/(app)/admin/parts/[id]/page.tsx
  - apps/web/app/(app)/admin/maintenance-templates/page.tsx
  - apps/web/app/(app)/dispatch/_components/MelBadge.tsx
autonomous: true
requirements: [MNT-01, MNT-03, MNT-04, MNT-05, MNT-11]
must_haves:
  truths:
    - "Admin can view a cross-fleet maintenance dashboard sorted by soonest due"
    - "Each aircraft detail page shows a MaintenancePanel with next grounding countdown"
    - "Admin can walk a squawk from open → triaged → in_work → fixed → returned_to_service via UI"
    - "Admin can create a work order, add tasks, consume parts, and complete the sign-off ceremony"
    - "IA user sees a '§91.409 overrun' button ONLY when blocking item is a 100-hour inspection"
    - "Dispatch screen shows a yellow MEL badge for aircraft with deferred squawks"
    - "Parts inventory UI lists on-hand, lots, and consumption history"
    - "Maintenance template manager lets admins apply a template to a new aircraft"
    - "Banned-term lint passes on every new UI file"
  artifacts:
    - path: "apps/web/app/(app)/admin/maintenance/page.tsx"
      provides: "Cross-fleet maintenance dashboard"
    - path: "apps/web/app/(app)/admin/aircraft/[id]/MaintenancePanel.tsx"
      provides: "Drop-in panel for aircraft detail page"
    - path: "apps/web/app/(app)/admin/work-orders/[id]/page.tsx"
      provides: "WO detail + tasks + parts + sign-off ceremony UI"
    - path: "apps/web/app/(app)/dispatch/_components/MelBadge.tsx"
      provides: "Yellow deferred-squawk indicator on dispatch screen"
  key_links:
    - from: "MaintenancePanel.tsx"
      to: "admin.maintenance.list + admin.overruns.active + admin.downtimeForecast.get"
      via: "trpc hooks"
    - from: "work-orders/[id]/page.tsx SignOffCeremony component"
      to: "admin.workOrders.signOff"
      via: "Big ceremonial button with legal-binding language"
    - from: "aircraft/[id]/page.tsx"
      to: "MaintenancePanel.tsx"
      via: "dropped in alongside EnginesPanel + RecentFlightsPanel (pattern from Phase 2)"
    - from: "dispatch screen"
      to: "MelBadge"
      via: "Reads deferred squawks for the aircraft being dispatched"
---

<objective>
Ship all admin CAMP UI pages and the dispatch MEL badge. This is pure view/form work against the routers from 04-03 — no new business logic.

Purpose: the UI is the inspector-facing and mechanic-facing experience for every CAMP capability. Keep it dense, scannable, and ceremonial where it matters (sign-offs, overruns).
Output: 13 new UI files + 1 dispatch component + integration into aircraft detail page.
</objective>

<execution_context>
@/Users/christopher/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christopher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-camp-maintenance/04-CONTEXT.md
@.planning/phases/04-camp-maintenance/04-03-SUMMARY.md
@apps/web/app/(app)/admin/aircraft/[id]/page.tsx
@apps/web/app/(app)/admin/squawks/page.tsx
@apps/web/app/(app)/dispatch/page.tsx
@packages/domain/src/schemas/maintenanceKindLabels.ts
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cross-fleet + per-aircraft maintenance UI (dashboard, panel, detail, templates)</name>
  <files>
    apps/web/app/(app)/admin/maintenance/page.tsx,
    apps/web/app/(app)/admin/aircraft/[id]/MaintenancePanel.tsx,
    apps/web/app/(app)/admin/aircraft/[id]/page.tsx,
    apps/web/app/(app)/admin/aircraft/[id]/maintenance/page.tsx,
    apps/web/app/(app)/admin/ads/page.tsx,
    apps/web/app/(app)/admin/ads/[id]/page.tsx,
    apps/web/app/(app)/admin/maintenance-templates/page.tsx
  </files>
  <action>
    /admin/maintenance/page.tsx: server component. Lists every maintenance_item across the fleet where status in ('due_soon','overdue','grounding'), grouped by aircraft, sorted by (status desc, next_due_at asc). Columns: aircraft tail, kind (display label via maintenanceKindLabels), title, status chip (green/yellow/red/dark-red), next due (date OR hours OR both), action button → links to /admin/aircraft/[id]/maintenance.

    /admin/aircraft/[id]/MaintenancePanel.tsx: client component dropped into the existing aircraft detail page alongside EnginesPanel + RecentFlightsPanel (mirror Phase 2 panel pattern). Shows: (a) "Next grounding: ~14 days (100-hour @ 4617.5 hrs)" countdown sourced from admin.downtimeForecast.get; (b) a red banner if aircraft.grounded_at is not null showing grounded_reason + link to blocking item; (c) summary counts: X current, Y due_soon, Z overdue; (d) "View all" → /admin/aircraft/[id]/maintenance; (e) §91.409 overrun CTA: renders ONLY when blocking item kind = 'hundred_hour_inspection' AND current user has mechanic_authority='ia' — button opens an OverrunModal (justification textarea min 20 chars, max_additional_hours 1-10 input, "THIS OVERRIDES THE AIRWORTHINESS GATE" warning in orange, confirm IA cert snapshot display). Submit → admin.overruns.grant; show red countdown banner after grant.

    Update apps/web/app/(app)/admin/aircraft/[id]/page.tsx: import and render MaintenancePanel alongside existing panels. Do NOT disturb EnginesPanel or RecentFlightsPanel.

    /admin/aircraft/[id]/maintenance/page.tsx: per-aircraft maintenance tab. Three sections: Maintenance Items (all, grouped by status), ADs (aircraft_ad_compliance rows), Components (aircraft_component rows with life remaining). Each item has a "Complete" button that opens a CompleteMaintenanceModal (completedAt date, completedAtHours jsonb builder, workOrderId optional, signer confirmation) → admin.maintenance.complete.

    /admin/ads/page.tsx: AD catalog list. Columns: ad_number, title, effective_date, applicability summary. "New AD" button. "Apply to Fleet" bulk button on each row → admin.ads.applyToFleet, shows toast with new compliance count.

    /admin/ads/[id]/page.tsx: AD detail. Shows summary markdown, applicability, per-aircraft compliance grid (aircraft tail | applicable | status | first_due | actions). "Record compliance" action per aircraft → RecordComplianceModal requiring IA signer snapshot.

    /admin/maintenance-templates/page.tsx: template manager. List system + school templates. "Create template" form. Per-template edit page shows template_lines with add/remove.

    STYLING: use existing admin page CSS patterns. Status chips: green=current, yellow=due_soon, red=overdue, dark-red=grounding. No banned terms — use "authorized" / "compliant" in any labels. All display labels come from maintenanceKindLabels.

    Use `mechanicOrAdminProcedure` server-side; do not rely on UI hiding alone. Use optimistic updates via trpc react query where it feels responsive.

    CRITICAL: verify `pnpm --filter @part61/web lint` passes — the banned-term rule will scream if any new file references "approved" or "Part 141" or "certified course".
  </action>
  <verify>cd "/Users/christopher/Desktop/Part 61 School" &amp;&amp; . scripts/dev-env.sh &amp;&amp; pnpm --filter @part61/web typecheck &amp;&amp; pnpm --filter @part61/web lint &amp;&amp; pnpm --filter @part61/web build 2>&amp;1 | tail -15</verify>
  <done>All 7 files created; typecheck, lint, and build all green; no banned-term violations; MaintenancePanel renders inside aircraft detail without breaking existing panels.</done>
</task>

<task type="auto">
  <name>Task 2: Squawk lifecycle + work orders + parts UI + dispatch MEL badge</name>
  <files>
    apps/web/app/(app)/admin/squawks/page.tsx,
    apps/web/app/(app)/admin/squawks/[id]/page.tsx,
    apps/web/app/(app)/admin/work-orders/page.tsx,
    apps/web/app/(app)/admin/work-orders/[id]/page.tsx,
    apps/web/app/(app)/admin/parts/page.tsx,
    apps/web/app/(app)/admin/parts/[id]/page.tsx,
    apps/web/app/(app)/dispatch/_components/MelBadge.tsx
  </files>
  <action>
    /admin/squawks/page.tsx: fleet-wide squawk board. Filter chips by status (open / triaged / deferred / in_work / fixed) — default to "active" (everything except returned_to_service/cancelled). Cursor pagination via admin.squawks.list. Columns: opened_at, aircraft tail, severity (chip), status (chip), title, triaged_by, actions.

    /admin/squawks/[id]/page.tsx: squawk detail with full timeline. Shows open/triage/defer/in_work/fixed/RTS events. Action bar shows the next valid transitions based on current status + current user authority:
      - 'open' + mechanic → "Triage" button (opens TriageModal: severity select + radio Defer vs In Work + if Defer: deferred_until date + deferral_justification textarea)
      - 'triaged' + mechanic → "Defer" / "Start Work Order" (Start Work Order creates a WO pre-linked to this squawk)
      - 'deferred' + mechanic → "Start Work Order"
      - 'in_work' + mechanic → "Mark Fixed" (requires workOrderId link)
      - 'fixed' + a_and_p/ia → "Sign and Return to Service" (CEREMONIAL button: big, red border, "This legally clears the aircraft to fly" text, confirm cert snapshot, requires cert number display). Calls admin.squawks.returnToService.
      - Any state → "Cancel" secondary action.

    /admin/work-orders/page.tsx: WO list with cursor pagination via admin.workOrders.list. Filters: status, aircraftId, assignedTo. Columns: created_at, aircraft, kind (display label), status chip, assigned_to, tasks (completed/total).

    /admin/work-orders/[id]/page.tsx: WO detail with three sections:
      1. Header: title, kind, aircraft, status, assigned mechanic, source (squawk/maintenance_item link).
      2. Tasks section: ordered list with description, required_authority chip, completed state, "Mark complete" button per task (requires matching authority).
      3. Parts section: consumed parts list + "Add consumption" button (PartConsumptionModal: part select, lot select (required for lot-tracked), quantity input; calls admin.workOrders.consumePart).
      4. Sign-off section: disabled until all tasks completed; when enabled, shows big "Sign and Return to Service" ceremonial button in red. Modal: shows highest required_authority, displays signer cert snapshot preview ("You are signing as Jane Mechanic, IA 3001234567"), requires explicit "I certify this work is complete" checkbox, "This is legally binding" language in red. Submits admin.workOrders.signOff.

    /admin/parts/page.tsx: parts table (part_number, description, kind, on_hand_qty, unit, preferred_supplier). "New part" button. "Receive lot" quick action.

    /admin/parts/[id]/page.tsx: part detail. Three sections: summary (editable), lots (part_lot list with received_at/qty_remaining/supplier/invoice_ref; "Receive new lot" button), consumption history (work_order_part_consumption joined to work_order for context).

    apps/web/app/(app)/dispatch/_components/MelBadge.tsx: small yellow badge component. Accepts `aircraftId` prop; queries admin.squawks.list({aircraftId, status:'deferred'}); renders a yellow pill "MEL: N deferred" if count > 0 with tooltip listing titles. Do NOT block dispatch — reminder only (per CONTEXT).

    Wire MelBadge into the existing dispatch screen next to each aircraft row (minimal edit — find the existing aircraft display in dispatch/page.tsx and add `<MelBadge aircraftId={...} />` next to the tail number).

    STYLING: ceremonial sign-off buttons use bold red border + extra padding + explicit confirm step. Orange for overrun/caution. Yellow for deferred/MEL. Red for grounded. Green for current. All labels vetted against banned-term list.
  </action>
  <verify>cd "/Users/christopher/Desktop/Part 61 School" &amp;&amp; . scripts/dev-env.sh &amp;&amp; pnpm --filter @part61/web typecheck &amp;&amp; pnpm --filter @part61/web lint &amp;&amp; pnpm --filter @part61/web build 2>&amp;1 | tail -15</verify>
  <done>All 7 files created; typecheck/lint/build all green; dispatch screen renders MelBadge without regressing Phase 3 dispatch tests; squawk state machine transitions render correctly per user authority.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @part61/web typecheck` green
- `pnpm --filter @part61/web lint` green (banned-term clean)
- `pnpm --filter @part61/web build` succeeds
- Phase 3 dispatch tests still green
</verification>

<success_criteria>
Every CAMP capability from 04-01/02/03 is reachable through admin UI. The dispatch screen surfaces MEL status. The sign-off ceremony feels legally weighty. The §91.409 overrun is gated behind IA authority and ONLY surfaces on 100-hour items.
</success_criteria>

<output>
Create `.planning/phases/04-camp-maintenance/04-04-SUMMARY.md` listing every page created, the state-machine transitions implemented in the squawk detail, the ceremonial sign-off UX decisions, and any lint workarounds.
</output>
