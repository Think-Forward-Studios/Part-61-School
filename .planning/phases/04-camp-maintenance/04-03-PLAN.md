---
phase: 04-camp-maintenance
plan: 03
type: execute
wave: 3
depends_on: ['04-02']
files_modified:
  - packages/api/src/procedures.ts
  - packages/api/src/helpers/signerSnapshot.ts
  - packages/api/src/helpers/maintenanceAuthority.ts
  - packages/api/src/helpers/workOrderBooks.ts
  - packages/api/src/routers/admin/maintenance.ts
  - packages/api/src/routers/admin/ads.ts
  - packages/api/src/routers/admin/components.ts
  - packages/api/src/routers/admin/workOrders.ts
  - packages/api/src/routers/admin/parts.ts
  - packages/api/src/routers/admin/logbook.ts
  - packages/api/src/routers/admin/maintenanceTemplates.ts
  - packages/api/src/routers/admin/overruns.ts
  - packages/api/src/routers/admin/squawks.ts
  - packages/api/src/routers/admin/index.ts
  - packages/domain/src/schemas/intervalRule.ts
  - packages/domain/src/schemas/adApplicability.ts
  - packages/domain/src/schemas/signerSnapshot.ts
  - packages/domain/src/schemas/maintenanceKindLabels.ts
  - packages/domain/src/schemas/maintenance.ts
  - packages/domain/src/index.ts
  - tests/api/phase4-camp-api.test.ts
autonomous: true
requirements: [MNT-02, MNT-03, MNT-04, MNT-05, MNT-07, MNT-08, MNT-09]
must_haves:
  truths:
    - "mechanicOrAdminProcedure exists and rejects callers without active_role in ('mechanic','admin')"
    - "buildSignerSnapshot returns a copied jsonb (user_id, full_name, certificate_type, certificate_number, signed_at) and refuses insufficient authority"
    - "admin.maintenance.create validates interval_rule via zod discriminated union and rejects garbage JSON"
    - "admin.workOrders.signOff writes one logbook entry per touched book, updates source maintenance_item OR ad_compliance OR component, unlocks the aircraft when last grounding item clears"
    - "admin.workOrders.consumePart uses SELECT FOR UPDATE on the lot row and rejects insufficient quantity"
    - "admin.squawks.triage / defer / returnToService enforce mechanic_authority on every transition"
    - "admin.overruns.grant rejects non-100-hour items (DB trigger double-enforced) and captures IA signer snapshot"
    - "admin.ads.applyToFleet calls apply_ads_to_aircraft and returns the count of new compliance rows"
    - "Historical signer snapshot integrity: mutating a mechanic's cert number does NOT retroactively change old signatures"
  artifacts:
    - path: "packages/api/src/procedures.ts"
      provides: "mechanicOrAdminProcedure"
    - path: "packages/api/src/helpers/signerSnapshot.ts"
      provides: "buildSignerSnapshot + requireMechanicAuthority"
    - path: "packages/api/src/routers/admin/workOrders.ts"
      provides: "Full WO CRUD + signOff ceremony + consumePart"
    - path: "packages/domain/src/schemas/intervalRule.ts"
      provides: "zod discriminated union for interval_rule JSONB"
  key_links:
    - from: "admin.workOrders.signOff"
      to: "buildSignerSnapshot + logbook_entry insert + maintenance_item update + aircraft.grounded_at clear"
      via: "single withTenantTx transaction"
    - from: "admin.workOrders.consumePart"
      to: "part_lot SELECT FOR UPDATE"
      via: "Serializes concurrent consumers (RESEARCH Pattern 4)"
    - from: "admin.overruns.grant"
      to: "maintenance_overrun insert + fn_maintenance_overrun_validate_kind trigger"
      via: "Two-layer kind-check: TS + DB (PITFALL 5)"
    - from: "admin.squawks.returnToService"
      to: "trg_squawk_rts_maybe_unground + buildSignerSnapshot"
      via: "mechanic_authority required; trigger clears grounded_at if no other cause"
---

<objective>
Ship the complete tRPC router layer for CAMP: new composed procedure, signer-snapshot helper, maintenance/ads/components/workOrders/parts/logbook/templates/overruns routers, extended squawks router for the 5-state lifecycle, domain zod schemas, and API integration tests.

Purpose: mirror Plan 03-02's success pattern (routers + tests in one plan). Every mutation that touches safety data MUST go through mechanicOrAdminProcedure + buildSignerSnapshot, so consolidating them prevents drift.
Output: 14 new/modified packages/api files, 6 new packages/domain files, one API integration test file proving the ceremony contracts hold.
</objective>

<execution_context>
@/Users/christopher/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christopher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-camp-maintenance/04-CONTEXT.md
@.planning/phases/04-camp-maintenance/04-RESEARCH.md
@.planning/phases/04-camp-maintenance/04-01-SUMMARY.md
@.planning/phases/04-camp-maintenance/04-02-SUMMARY.md
@packages/api/src/procedures.ts
@packages/api/src/routers/admin/squawks.ts
@packages/api/src/routers/admin/index.ts
@packages/domain/src/schemas/scheduleLabels.ts
@CLAUDE.md
</context>

<interfaces>
From packages/api/src/procedures.ts (Phase 3): `protectedProcedure`, `adminProcedure`, `requireRole(...roles)`. New: `mechanicOrAdminProcedure = protectedProcedure.use(requireRole('mechanic','admin'))`.

From packages/db schema (04-01): maintenanceItem, maintenanceItemTemplate, airworthinessDirective, aircraftAdCompliance, aircraftComponent, workOrder, workOrderTask, workOrderPartConsumption, part, partLot, logbookEntry, maintenanceOverrun — all importable from `@part61/db`.

From packages/db SQL (04-02): `maintenance_next_due`, `component_life_remaining`, `recompute_maintenance_status`, `apply_ads_to_aircraft`, `aircraft_next_grounding_forecast`, `is_airworthy_at` — call via `ctx.tx.execute(sql`select * from public.maintenance_next_due(${id})`)`.

Signer snapshot shape (LOCKED):
```typescript
type SignerSnapshot = {
  user_id: string;
  full_name: string;
  certificate_type: 'a_and_p' | 'ia';
  certificate_number: string;
  signed_at: string; // ISO
};
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Domain schemas + procedures.ts + helpers</name>
  <files>
    packages/domain/src/schemas/intervalRule.ts,
    packages/domain/src/schemas/adApplicability.ts,
    packages/domain/src/schemas/signerSnapshot.ts,
    packages/domain/src/schemas/maintenanceKindLabels.ts,
    packages/domain/src/schemas/maintenance.ts,
    packages/domain/src/index.ts,
    packages/api/src/procedures.ts,
    packages/api/src/helpers/signerSnapshot.ts,
    packages/api/src/helpers/maintenanceAuthority.ts,
    packages/api/src/helpers/workOrderBooks.ts
  </files>
  <behavior>
    - intervalRuleSchema parses valid hobbs/tach/airframe/engine/calendar/combined rules and rejects `{clock:'unknown'}`, `{clock:'hobbs', hours:-5}`, `{clock:'calendar', months:'twelve'}`
    - adApplicabilitySchema parses `{aircraft_make:'Cessna', aircraft_model:'172'}` and `{}` (all-optional)
    - signerSnapshotSchema round-trips user_id/full_name/certificate_type/certificate_number/signed_at
    - maintenanceKindLabels.ts exports an object mapping each maintenance_item_kind to a display string that passes the banned-term lint (NO 'approved', NO 'certified course', NO 'Part 141' anywhere in the file). File is outside apps/web/** and packages/exports/** lint glob — add package.json or eslint override if needed.
    - mechanicOrAdminProcedure rejects callers without active_role in ('mechanic','admin')
    - buildSignerSnapshot(tx, userId, 'ia') throws FORBIDDEN if user has mechanic_authority='a_and_p'
    - buildSignerSnapshot throws FAILED_PRECONDITION if faa_airman_cert_number missing
    - buildSignerSnapshot returns a frozen snapshot object — mutating the row afterward does not change the returned object
    - requireMechanicAuthority(ctx, 'ia') throws FORBIDDEN for a_and_p ctx; passes for ia ctx
    - booksTouchedByTaskKinds(['annual_inspection']) returns Set{'airframe','engine','prop'}; ['oil_change'] returns Set{'engine'}; ['squawk_repair'] returns Set{'airframe'} by default
    - taskKindRequiredAuthority('annual_inspection') === 'ia'; ('100_hour_inspection') === 'a_and_p'; ('squawk_repair') === 'a_and_p'
  </behavior>
  <action>
    Create packages/domain/src/schemas/intervalRule.ts with the zod discriminated union exactly as in RESEARCH Pattern 3 (hobbs/tach/airframe/engine hours variants + calendar + combined with mode). Export `intervalRuleSchema` and `type IntervalRule`.

    Create packages/domain/src/schemas/adApplicability.ts with zod object: aircraft_make/model optional, year_range optional tuple of two ints, serial_range optional tuple of two strings, engine/prop optional strings. Export `adApplicabilitySchema` and type.

    Create packages/domain/src/schemas/signerSnapshot.ts with zod for the SignerSnapshot shape.

    Create packages/domain/src/schemas/maintenanceKindLabels.ts — a plain `export const maintenanceKindLabels: Record<MaintenanceItemKind, string>`. Use labels: 'Annual inspection', 'Hundred-hour inspection', 'Airworthiness directive', 'Oil change', '91.413 transponder check', '91.411 pitot-static', 'ELT battery', '91.207 ELT inspection', 'VOR check', 'Component life limit', 'Manufacturer service bulletin', 'Custom item'. VERIFY NONE contain banned terms ('Part 141', 'approved', 'certified course'). If the lint still fires because the file IS inside the glob, add an ESLint file-level override or move under packages/domain/src/schemas/ (which is outside apps/web/** and packages/exports/**) — verify by grepping eslint config.

    Create packages/domain/src/schemas/maintenance.ts with shared TS types: MaintenanceItemKind, MaintenanceItemStatus, MechanicAuthorityKind, LogbookBook, WorkOrderStatus, WorkOrderKind. Re-export from index.ts.

    Add to packages/api/src/procedures.ts: `export const mechanicOrAdminProcedure = protectedProcedure.use(requireRole('mechanic','admin'));` — mirror existing composed procedures.

    Create packages/api/src/helpers/signerSnapshot.ts per RESEARCH Example 2: buildSignerSnapshot(tx, userId, requiredAuthority) + requireMechanicAuthority(ctxAuthority, required). Return a frozen object via Object.freeze to document the "copied, not referenced" contract.

    Create packages/api/src/helpers/maintenanceAuthority.ts: `taskKindRequiredAuthority(kind)` returning 'ia' for annual_inspection, 'a_and_p' for 100_hour_inspection / squawk_repair / component_replacement / oil_change, and 'a_and_p' default for ad_compliance (some ADs will override at tRPC boundary).

    Create packages/api/src/helpers/workOrderBooks.ts per RESEARCH Example 4: booksTouchedByTaskKinds(kinds: MaintenanceItemKind[]) returning Set<LogbookBook>. Annual touches all three. Oil change touches engine only. 100-hour touches airframe + engine. Component-life: read component.kind, map magneto/vacuum_pump/spark_plug/mag_points → engine; prop → prop+airframe; alternator/elt/elt_battery/starter → airframe.

    Add unit tests for domain schemas and helpers inline in tests/api/phase4-camp-api.test.ts (Task 2 will extend).
  </action>
  <verify>cd "/Users/christopher/Desktop/Part 61 School" &amp;&amp; . scripts/dev-env.sh &amp;&amp; pnpm --filter @part61/domain typecheck &amp;&amp; pnpm --filter @part61/api typecheck &amp;&amp; pnpm --filter @part61/web lint 2>&amp;1 | grep -iE "banned-term|error" || echo OK</verify>
  <done>Typecheck green; domain zod schemas exported; mechanicOrAdminProcedure exported; signerSnapshot + maintenanceAuthority + workOrderBooks helpers compile; banned-term lint does not fire on maintenanceKindLabels.ts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: All admin CAMP routers + API integration tests</name>
  <files>
    packages/api/src/routers/admin/maintenance.ts,
    packages/api/src/routers/admin/ads.ts,
    packages/api/src/routers/admin/components.ts,
    packages/api/src/routers/admin/workOrders.ts,
    packages/api/src/routers/admin/parts.ts,
    packages/api/src/routers/admin/logbook.ts,
    packages/api/src/routers/admin/maintenanceTemplates.ts,
    packages/api/src/routers/admin/overruns.ts,
    packages/api/src/routers/admin/squawks.ts,
    packages/api/src/routers/admin/index.ts,
    tests/api/phase4-camp-api.test.ts
  </files>
  <behavior>
    MAINTENANCE:
      - admin.maintenance.list({aircraftId}) returns items with computed status via maintenance_next_due
      - admin.maintenance.create validates interval_rule via intervalRuleSchema and rejects malformed JSON
      - admin.maintenance.complete({itemId, completedAtHours, completedAt, workOrderId?, signerAuthority}) requires mechanicOrAdmin, builds signer snapshot, updates last_completed_*, writes logbook entries for airframe book, triggers recompute_maintenance_status

    ADS:
      - admin.ads.list / get / create / update (admin only for catalog edits)
      - admin.ads.applyToFleet({adId}) calls apply_ads_to_aircraft for each fleet aircraft and returns {newComplianceRows: N}
      - admin.ads.recordCompliance({aircraftId, adId, method, completedAtHours, notes}) builds signer snapshot (ia required), inserts ad_compliance_history row, updates aircraft_ad_compliance.status

    COMPONENTS:
      - admin.components.install({aircraftId, kind, serialNumber, lifeLimitHours?, lifeLimitMonths?, installedAtHours}) inserts aircraft_component (bridge trigger creates maintenance_item)
      - admin.components.overhaul builds signer snapshot, inserts aircraft_component_overhaul row, resets life clock
      - admin.components.remove({componentId, reason}) sets removed_at (bridge trigger soft-deletes maintenance_item)

    WORKORDERS:
      - admin.workOrders.list cursor-pagination via (created_at, id) keyset
      - admin.workOrders.create from squawk, from maintenance_item, or blank
      - admin.workOrders.addTask validates required_authority via taskKindRequiredAuthority
      - admin.workOrders.consumePart uses SELECT FOR UPDATE on part and part_lot (RESEARCH Pattern 4); rejects insufficient quantity; rejects missing lot for lot-tracked parts
      - admin.workOrders.signOff: CEREMONY mutation. Requires all tasks completed. Determines required authority = max of task.required_authority. Calls buildSignerSnapshot(tx, ctx.userId, maxAuthority). Updates work_order.status='closed', signed_off_at, signed_off_by, signer_snapshot, return_to_service_time. Writes one logbook_entry per book in booksTouchedByTaskKinds(tasks) with signer_snapshot AND sealed=true. If source_maintenance_item_id: updates last_completed_at/hours/by. If source_squawk_id: updates aircraft_squawk.status='returned_to_service' + returned_to_service_signer_snapshot. Calls recompute_maintenance_status which may clear grounded_at.

    PARTS:
      - admin.parts.list / get / create / update
      - admin.parts.receiveLot({partId, lotNumber, serialNumber?, receivedQty, expiresAt?, supplier, invoiceRef}) inserts part_lot; trigger keeps part.on_hand_qty in sync
      - admin.parts.listLots({partId}), admin.parts.consumptionHistory({partId})

    LOGBOOK:
      - admin.logbook.list({aircraftId, bookKind}) returns entries ordered by entry_date desc
      - admin.logbook.createDraft (non-sealed) + admin.logbook.sign (sets sealed=true, signer_snapshot, signed_at)
      - admin.logbook.correct({originalEntryId, description}) creates new entry with corrects_entry_id FK (the original STAYS sealed)

    MAINTENANCE TEMPLATES:
      - admin.maintenanceTemplates.list / get / create / applyToAircraft({templateId, aircraftId}) copies template lines into maintenance_item rows

    OVERRUNS:
      - admin.overruns.grant({aircraftId, itemId, justification, maxAdditionalHours}) requires IA authority (requireMechanicAuthority + buildSignerSnapshot with 'ia'). Validates item.kind = 'hundred_hour_inspection' in TS AND relies on DB trigger fn_maintenance_overrun_validate_kind for defense in depth (PITFALL 5). Validates justification length >= 20, max 1-10. Validates no active overrun exists on this item (DB partial unique index enforces). Inserts maintenance_overrun row and emits audit.
      - admin.overruns.revoke({overrunId, reason}) sets revoked_at; recompute_maintenance_status may re-ground aircraft.
      - admin.overruns.active({aircraftId}) lists unexpired unrevoked overruns.

    SQUAWKS (EXTEND Phase 3 router):
      - admin.squawks.list / get — unchanged signatures, now return the full 5-state status
      - admin.squawks.triage({squawkId, severity, action: 'defer'|'in_work', deferredUntil?, deferralJustification?}) requires mechanicOrAdmin + a_and_p/ia; sets triaged_at/triaged_by + transitions status
      - admin.squawks.defer (shortcut) + admin.squawks.moveToInWork
      - admin.squawks.recordFixed({squawkId, workOrderId, notes}) status fixed
      - admin.squawks.returnToService({squawkId}) requires mechanicOrAdmin + a_and_p/ia; builds signer snapshot; updates status='returned_to_service', returned_to_service_at, returned_to_service_signer_snapshot; trigger trg_squawk_rts_maybe_unground fires
      - admin.squawks.cancel({squawkId, reason})

    Register all new routers in packages/api/src/routers/admin/index.ts.

    INTEGRATION TESTS in tests/api/phase4-camp-api.test.ts (reuse Phase 3 tRPC test harness):
      1. Student caller is REJECTED from admin.maintenance.create
      2. Instructor (not mechanic/admin) is REJECTED from admin.squawks.triage
      3. a_and_p caller passes admin.squawks.triage
      4. a_and_p caller is REJECTED from admin.overruns.grant (ia required)
      5. ia caller succeeds admin.overruns.grant on a 100-hour item
      6. ia caller is REJECTED from admin.overruns.grant on an annual item (TS + DB double-enforcement)
      7. admin.maintenance.create with `interval_rule: {clock:'hobbs', hours:-10}` → BAD_REQUEST
      8. Historical signature integrity: sign a squawk RTS as mechanic M1 (cert# X), then update M1's cert# to Y, re-read the squawk, assert returned_to_service_signer_snapshot.certificate_number still === X
      9. admin.workOrders.signOff ceremony: annual WO with 2 IA tasks + 1 A&P task completed, ia user signs → work_order.status='closed', logbook_entry written for airframe+engine+prop all with sealed=true, aircraft.grounded_at cleared if it was the last cause
      10. admin.workOrders.consumePart concurrent decrement: Promise.all two consumePart calls on the same lot with qty 5 each when lot has 7 → exactly one succeeds and the other gets BAD_REQUEST "Insufficient lot qty"
      11. admin.ads.applyToFleet for a Cessna 172 AD against a fleet with one C172 and one PA-28 inserts exactly one aircraft_ad_compliance row
  </action>
  <verify>cd "/Users/christopher/Desktop/Part 61 School" &amp;&amp; . scripts/dev-env.sh &amp;&amp; pnpm --filter @part61/api typecheck &amp;&amp; pnpm --filter @part61/api test -- phase4-camp-api &amp;&amp; pnpm --filter @part61/rls-tests test</verify>
  <done>All routers compile and type-check. All 11 integration tests pass. Full RLS suite remains green (no regressions introduced by new triggers firing via router calls). Phase 3 squawks router changes are additive — Phase 3 tests still pass.</done>
</task>

</tasks>

<verification>
- Full monorepo typecheck green
- phase4-camp-api.test.ts green with all 11 scenarios
- Phase 3 test count unchanged (98/98)
- Historical signer snapshot integrity proven
- Concurrency correctness proven on parts consumption
</verification>

<success_criteria>
Every CAMP mutation flows through a typed, authorized, audit-logged tRPC procedure. The UI layer in 04-04/04-05 can consume these routers without touching SQL.
</success_criteria>

<output>
Create `.planning/phases/04-camp-maintenance/04-03-SUMMARY.md` listing final router surface, any scope adjustments, the 11 test scenarios and their results, and the exact shape of mechanicOrAdminProcedure + buildSignerSnapshot as built.
</output>
