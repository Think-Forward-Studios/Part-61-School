---
phase: 04-camp-maintenance
plan: 02
type: execute
wave: 2
depends_on: ['04-01']
files_modified:
  - packages/db/migrations/0011_phase4_functions_triggers.sql
  - packages/db/migrations/0012_phase4_replace_is_airworthy_at.sql
  - supabase/migrations/0011_phase4_functions_triggers.sql
  - supabase/migrations/0012_phase4_replace_is_airworthy_at.sql
  - tests/rls/phase4-camp-functions.test.ts
  - tests/rls/phase4-camp-is-airworthy.test.ts
autonomous: true
requirements: [MNT-02, MNT-03, MNT-11]
must_haves:
  truths:
    - "maintenance_next_due(item_id) returns correct status for hours / calendar / combined clocks"
    - "component_life_remaining(id) returns zero/negative when life limit exceeded"
    - "recompute_maintenance_status(aircraft_id) updates all item statuses and grounds the aircraft if any item overdue"
    - "A flight_log_entry insert that crosses a 100-hour limit grounds the aircraft in the same transaction"
    - "maintenance_overrun consumed_hours recomputes from authoritative flight_out/flight_in deltas (not incrementally)"
    - "is_airworthy_at returns false for aircraft with overdue items, true when a valid overrun exists, and identical answers to Phase 3 stub for all Phase 3 test fixtures"
    - "Concurrent flight_log_entry inserts across a limit produce exactly one ground event (SELECT FOR UPDATE on aircraft row)"
    - "All Phase 3 tests (98/98) remain green after is_airworthy_at body swap"
  artifacts:
    - path: "packages/db/migrations/0011_phase4_functions_triggers.sql"
      provides: "All SQL functions + business-logic triggers"
    - path: "packages/db/migrations/0012_phase4_replace_is_airworthy_at.sql"
      provides: "Real is_airworthy_at body (signature frozen)"
  key_links:
    - from: "trg_flightlog_refresh_maintenance"
      to: "recompute_maintenance_status(aircraft_id)"
      via: "AFTER INSERT OR UPDATE on flight_log_entry"
    - from: "trg_component_bridge_maintenance"
      to: "maintenance_item (kind=component_life)"
      via: "AFTER INSERT on aircraft_component creates bridged row"
    - from: "trg_ad_bridge_maintenance"
      to: "maintenance_item (kind=airworthiness_directive)"
      via: "AFTER INSERT on aircraft_ad_compliance creates bridged row"
    - from: "trg_flightlog_consume_overrun"
      to: "maintenance_overrun.consumed_hours"
      via: "Recompute from flight_out/flight_in pairs since granted_at"
    - from: "is_airworthy_at"
      to: "maintenance_overrun"
      via: "Active overrun suppresses grounding-from-item for 100-hour only"
---

<objective>
Ship the CAMP SQL function + trigger layer that makes the schema from 04-01 actually compute airworthiness, and replace the Phase 3 `is_airworthy_at` stub body in-place without breaking any Phase 3 test.

Purpose: SQL-first is the only way RLS policies, triggers, and application code all see one authoritative answer. The dispatch screen's airworthiness gate silently upgrades from stub to real rules the moment this plan lands.
Output: migrations 0011 (functions + triggers) and 0012 (is_airworthy_at replace), plus two new test files that exercise function correctness, concurrency, and Phase 3 regression.
</objective>

<execution_context>
@/Users/christopher/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christopher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-camp-maintenance/04-CONTEXT.md
@.planning/phases/04-camp-maintenance/04-RESEARCH.md
@.planning/phases/04-camp-maintenance/04-01-SUMMARY.md
@packages/db/migrations/0007_phase3_scheduling_dispatch.sql
@packages/db/migrations/0008_phase3_view_update.sql
@tests/rls/phase3-scheduling.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration 0011 — SQL functions + business triggers</name>
  <files>
    packages/db/migrations/0011_phase4_functions_triggers.sql,
    supabase/migrations/0011_phase4_functions_triggers.sql,
    tests/rls/phase4-camp-functions.test.ts
  </files>
  <behavior>
    - maintenance_next_due(item_id): hours-only item with last_completed_hours.tach=200 + rule.hours=100 returns next_due_hours=300 and status 'current' when aircraft tach=250, 'due_soon' when tach=295 (warn=10), 'overdue' when tach=305
    - maintenance_next_due: calendar-only item completed 11 months ago with rule.months=12 returns status 'due_soon', 12.5 months ago returns 'overdue'
    - maintenance_next_due: combined/whichever_first returns the SOONER of hours vs calendar
    - component_life_remaining: component with life_limit_hours=2000 installed at airframe=0 with aircraft airframe=1995 returns 5 hours_remaining
    - component_life_remaining: component removed_at NOT NULL returns null/negative (closed)
    - recompute_maintenance_status(aircraft_id) takes SELECT FOR UPDATE on aircraft row and updates every item's status via maintenance_next_due; if any status='overdue' AND item is not overrun-eligible OR has no active overrun → set aircraft.grounded_at, grounded_reason, grounded_by_item_id
    - apply_ads_to_aircraft(aircraft_id): loops ADs catalog, matches applicability jsonb (make/model/year_range), inserts aircraft_ad_compliance rows for new matches
    - aircraft_next_grounding_forecast(aircraft_id): returns earliest event from maintenance_items + ad_compliance + components; writes to aircraft_downtime_forecast cache
    - Bridge trigger: INSERT on aircraft_component with life_limit_hours NOT NULL creates maintenance_item(kind='component_life', component_id=..., interval_rule=derived)
    - Bridge trigger: INSERT on aircraft_ad_compliance creates maintenance_item(kind='airworthiness_directive', ad_compliance_id=...)
    - Soft-close trigger: UPDATE aircraft_component SET removed_at=now() soft-deletes bridged maintenance_item
    - Overrun consume trigger: INSERT/UPDATE on flight_log_entry recomputes consumed_hours for ALL active overruns on that aircraft as sum of (flight_in.hobbs_in - flight_out.hobbs_out) since overrun.granted_at; if consumed_hours >= max_additional_hours, sets revoked_at=now()
    - Flight log refresh trigger: AFTER INSERT OR UPDATE on flight_log_entry calls recompute_maintenance_status(NEW.aircraft_id)
    - Squawk RTS un-ground trigger: UPDATE aircraft_squawk → returned_to_service clears grounded_at IF no other grounding cause remains
    - Concurrency: two parallel flight_log_entry inserts that together cross a 100-hour limit produce exactly one grounded_at (SELECT FOR UPDATE serializes)
    - Trigger cascade: maintenance_item update refreshes aircraft_downtime_forecast but does NOT refire maintenance_item (no infinite loop)
  </behavior>
  <action>
    Write 0011_phase4_functions_triggers.sql with:

    1. FUNCTIONS (CREATE OR REPLACE):
       - maintenance_next_due(p_item_id uuid) returns table(next_due_at timestamptz, next_due_hours numeric, status maintenance_item_status) — use RESEARCH Example 1 as starting template; resolve aircraft_current_totals column names against migration 0008 (the view defines airframe_hours, tach, hobbs, engine_1_hours, etc.); handle combined clock via whichever_first/last; warn windows = 10 hours / 30 days default.
       - component_life_remaining(p_component_id uuid) returns table(hours_remaining numeric, days_remaining int, status component_status) — reads aircraft_current_totals; subtracts installed_at_hours[clock] from current hours; calendar via life_limit_months from installed_at_date.
       - recompute_maintenance_status(p_aircraft_id uuid) returns void language plpgsql: SELECT FOR UPDATE on aircraft row (concurrency serializer); loops maintenance_item where aircraft_id and deleted_at is null; calls maintenance_next_due and UPDATEs status + next_due_at + next_due_hours; then checks if any row has status in ('overdue','grounding') WITHOUT an active unexpired overrun — if yes AND aircraft not already grounded, set grounded_at=now(), grounded_reason='Maintenance: '||title||' overdue', grounded_by_item_id; if all items current/due_soon AND no grounding squawks AND aircraft was grounded by maintenance, clear grounded_at.
       - apply_ads_to_aircraft(p_aircraft_id uuid) returns int: loops airworthiness_directive WHERE (school_id is null OR school_id = aircraft school); for each, evaluates applicability jsonb against aircraft make/model/year/serial; INSERTs aircraft_ad_compliance rows with ON CONFLICT DO NOTHING.
       - aircraft_next_grounding_forecast(p_aircraft_id uuid) returns table(next_event_at timestamptz, next_event_hours numeric, reason text, confidence text): queries maintenance_item (current/due_soon), aircraft_ad_compliance, aircraft_component (life_remaining>0); picks the MIN by (next_due_at, next_due_hours converted via schedule estimate); returns the winning row.
       - refresh_aircraft_downtime_forecast(p_aircraft_id uuid) returns void: calls aircraft_next_grounding_forecast and UPSERTs into aircraft_downtime_forecast.
       - fn_build_signer_snapshot_sql(p_user_id uuid, p_required mechanic_authority_kind) returns jsonb — OPTIONAL SQL mirror of the TS helper for use inside triggers; the authoritative version is TS in 04-03. Skip if not needed by any trigger.

    2. TRIGGERS (CREATE OR REPLACE):
       - trg_flightlog_refresh_maintenance AFTER INSERT OR UPDATE ON flight_log_entry FOR EACH ROW EXECUTE FUNCTION fn_flightlog_refresh_maintenance(); function body: call recompute_maintenance_status(NEW.aircraft_id); call refresh_aircraft_downtime_forecast(NEW.aircraft_id).
       - trg_flightlog_consume_overrun AFTER INSERT OR UPDATE ON flight_log_entry: for each active overrun on NEW.aircraft_id, recompute consumed_hours as SUM of matched flight_out/flight_in deltas since granted_at (authoritative, NOT incremental — addresses PITFALL 7); if consumed_hours >= max_additional_hours, UPDATE revoked_at=now().
       - trg_component_bridge_maintenance AFTER INSERT ON aircraft_component WHEN (NEW.life_limit_hours IS NOT NULL OR NEW.life_limit_months IS NOT NULL): INSERT maintenance_item with kind='component_life', component_id=NEW.id, interval_rule derived from component life limits, initial status computed via maintenance_next_due.
       - trg_component_soft_close_bridge AFTER UPDATE ON aircraft_component WHEN (OLD.removed_at IS NULL AND NEW.removed_at IS NOT NULL): UPDATE maintenance_item SET deleted_at=now() WHERE component_id=NEW.id.
       - trg_ad_bridge_maintenance AFTER INSERT ON aircraft_ad_compliance: INSERT maintenance_item with kind='airworthiness_directive', ad_compliance_id=NEW.id.
       - trg_ad_status_refresh AFTER UPDATE ON aircraft_ad_compliance: UPDATE bridged maintenance_item.status to match; call recompute_maintenance_status(NEW.aircraft_id) to propagate grounding.
       - trg_squawk_rts_maybe_unground AFTER UPDATE ON aircraft_squawk WHEN (OLD.status != 'returned_to_service' AND NEW.status = 'returned_to_service'): if no other grounding squawks exist AND aircraft.grounded_by_item_id is null, call recompute_maintenance_status (which may clear grounded_at).
       - trg_mi_refresh_forecast AFTER INSERT OR UPDATE ON maintenance_item: call refresh_aircraft_downtime_forecast(NEW.aircraft_id). GUARD against cascade: the forecast function must only write to aircraft_downtime_forecast, NEVER back to maintenance_item (PITFALL 4).

    3. OVERRUN KIND CHECK: create fn_maintenance_overrun_validate_kind() BEFORE INSERT on maintenance_overrun: SELECT kind FROM maintenance_item WHERE id=NEW.item_id; if kind != 'hundred_hour_inspection' raise exception '§91.409(b) overrun only applies to 100-hour inspections' (addresses PITFALL 5).

    Mirror verbatim to supabase/migrations/.

    Then create tests/rls/phase4-camp-functions.test.ts covering every `<behavior>` bullet above. Use the Phase 3 test harness pattern (raw postgres-js, set GUCs, seed fixtures). INCLUDE the concurrency test (Promise.all two flight_log_entry inserts that together cross a limit; assert exactly one grounded_at and correct end state) — addresses PITFALL 8.
  </action>
  <verify>
    <automated>cd "/Users/christopher/Desktop/Part 61 School" && . scripts/dev-env.sh && pnpm --filter @part61/db exec drizzle-kit push 2>&1 | tail -20 && pnpm --filter @part61/rls-tests test -- phase4-camp-functions</automated>
  </verify>
  <done>Migration 0011 applies cleanly; all behavior bullets pass; concurrency test proves serializer correctness.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migration 0012 — replace is_airworthy_at body + Phase 3 regression test</name>
  <files>
    packages/db/migrations/0012_phase4_replace_is_airworthy_at.sql,
    supabase/migrations/0012_phase4_replace_is_airworthy_at.sql,
    tests/rls/phase4-camp-is-airworthy.test.ts
  </files>
  <behavior>
    - Signature of is_airworthy_at(p_aircraft_id uuid, p_at timestamptz) returns boolean is UNCHANGED
    - Returns false when aircraft.deleted_at is not null
    - Returns false when aircraft.grounded_at <= p_at
    - Returns false when any aircraft_squawk.severity='grounding' AND opened_at <= p_at AND (returned_to_service_at IS NULL OR returned_to_service_at > p_at)
    - Returns false when any maintenance_item status in ('overdue','grounding') AND no active unexpired overrun consuming it
    - Returns true when a 100-hour inspection is overdue BUT a maintenance_overrun exists with granted_at <= p_at, revoked_at IS NULL, expires_at > p_at, consumed_hours < max_additional_hours
    - Returns false when any aircraft_ad_compliance.status in ('overdue','grounding') AND first_due_at <= p_at
    - Returns false when any aircraft_component with removed_at IS NULL has component_life_remaining(id).hours_remaining <= 0
    - Phase 3 regression: every existing is_airworthy_at call site in tests/rls/phase3-scheduling.test.ts returns the same boolean as before (PITFALL 3)
    - All 98 Phase 1-3 tests remain green
  </behavior>
  <action>
    Write 0012_phase4_replace_is_airworthy_at.sql with CREATE OR REPLACE FUNCTION public.is_airworthy_at(p_aircraft_id uuid, p_at timestamptz) returns boolean — use RESEARCH Pattern 1 as the authoritative starting template. Body is the `select case ... end` structure with five short-circuit rules in this order: deleted→grounded→grounding_squawk→overdue_maintenance_item(minus active overrun)→overdue_ad→zero_life_component. Language sql stable security invoker.

    Mirror to supabase/migrations/.

    Create tests/rls/phase4-camp-is-airworthy.test.ts covering each rule with a dedicated fixture:
      1. Plain aircraft with no maintenance → true
      2. Grounded aircraft → false
      3. Open grounding squawk → false; same squawk marked RTS → true
      4. Maintenance item overdue → false
      5. Maintenance item overdue with active §91.409 overrun → true
      6. Overrun with consumed_hours >= max_additional_hours → false (auto-revoke path)
      7. Overrun on a non-100-hour item → mutation REJECTED at insert by fn_maintenance_overrun_validate_kind
      8. AD compliance overdue → false
      9. Component with zero life remaining → false

    PITFALL 3 GUARD — before running phase4 tests, explicitly:
      a. Audit tests/rls/phase3-scheduling.test.ts for any test aircraft seeded WITHOUT a fresh "current" maintenance state.
      b. Ensure those fixtures seed either (i) zero maintenance_items, or (ii) items with far-future due dates, so the new is_airworthy_at body returns the same answer as the stub.
      c. If a fixture must change to preserve behavior, add a clarifying comment "Phase 4 is_airworthy_at guard: item seeded far-future so stub-era assertion still holds."
      d. Run `pnpm --filter @part61/rls-tests test -- phase3-scheduling` to prove 98/98 still pass.

    If the Phase 3 suite regresses, the fix is ALWAYS to adjust the Phase 3 fixture (it was incomplete), NOT the is_airworthy_at body (per CONTEXT Boundary: body replaces stub, signature frozen).
  </action>
  <verify>
    <automated>cd "/Users/christopher/Desktop/Part 61 School" && . scripts/dev-env.sh && pnpm --filter @part61/db exec drizzle-kit push 2>&1 | tail -10 && pnpm --filter @part61/rls-tests test -- phase4-camp-is-airworthy && pnpm --filter @part61/rls-tests test -- phase3-scheduling && pnpm --filter @part61/rls-tests test</automated>
  </verify>
  <done>is_airworthy_at body replaced; all 9 phase4-camp-is-airworthy behaviors pass; Phase 3 suite green; full RLS test suite green (no regressions from 98/98).</done>
</task>

</tasks>

<verification>
- Migrations 0011 and 0012 apply cleanly in sequence on top of 0010
- phase4-camp-functions tests green (including concurrency serializer test)
- phase4-camp-is-airworthy tests green (all 9 behaviors)
- phase3-scheduling tests remain 98/98 green (regression guard)
- is_airworthy_at signature unchanged (psql \df public.is_airworthy_at)
</verification>

<success_criteria>
The dispatch screen's airworthiness gate now silently uses real CAMP rules. Any tRPC / UI layer built in 04-03+ can trust that maintenance_item.status, aircraft.grounded_at, and is_airworthy_at all agree.
</success_criteria>

<output>
Create `.planning/phases/04-camp-maintenance/04-02-SUMMARY.md` documenting: final function signatures, trigger list, concurrency test result, Phase 3 test count (must be 98/98), and any Phase 3 fixtures adjusted under the PITFALL 3 guard.
</output>
