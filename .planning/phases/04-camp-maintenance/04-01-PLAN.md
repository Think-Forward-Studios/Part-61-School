---
phase: 04-camp-maintenance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/db/migrations/0009_phase4_enums.sql
  - packages/db/migrations/0010_phase4_camp_tables.sql
  - supabase/migrations/0009_phase4_enums.sql
  - supabase/migrations/0010_phase4_camp_tables.sql
  - packages/db/src/schema/maintenance_item.ts
  - packages/db/src/schema/ads.ts
  - packages/db/src/schema/aircraft_component.ts
  - packages/db/src/schema/work_order.ts
  - packages/db/src/schema/part.ts
  - packages/db/src/schema/logbook_entry.ts
  - packages/db/src/schema/maintenance_overrun.ts
  - packages/db/src/schema/downtime_forecast.ts
  - packages/db/src/schema/aircraft.ts
  - packages/db/src/schema/aircraft_squawk.ts
  - packages/db/src/schema/index.ts
  - tests/rls/phase4-camp.test.ts
autonomous: true
requirements: [MNT-01, MNT-04, MNT-06, MNT-07, MNT-08, MNT-09, MNT-10]
must_haves:
  truths:
    - "All Phase 4 tables exist with RLS enforced by school_id + base_id"
    - "squawk_status enum extended to open|triaged|deferred|in_work|fixed|returned_to_service|cancelled"
    - "Every safety-relevant CAMP table has audit trigger + hard-delete blocker"
    - "logbook_entry cannot be UPDATEd once sealed=true"
    - "Cross-tenant reads are blocked on every new CAMP table (RLS harness)"
  artifacts:
    - path: "packages/db/migrations/0009_phase4_enums.sql"
      provides: "All new enums + ALTER TYPE squawk_status"
    - path: "packages/db/migrations/0010_phase4_camp_tables.sql"
      provides: "All CAMP tables + RLS + audit/hard-delete/seal triggers"
    - path: "tests/rls/phase4-camp.test.ts"
      provides: "Cross-tenant RLS coverage for every new CAMP table"
  key_links:
    - from: "0009_phase4_enums.sql"
      to: "0010_phase4_camp_tables.sql"
      via: "enum values referenced in column definitions — must be separate migration (Phase 2 enum caveat)"
    - from: "packages/db/src/schema/aircraft_squawk.ts"
      to: "0009 ALTER TYPE squawk_status"
      via: "Drizzle enum definition mirrors DB enum (extended, not rewritten)"
    - from: "logbook_entry.sealed trigger"
      to: "fn_logbook_entry_block_update"
      via: "BEFORE UPDATE trigger raising exception"
---

<objective>
Ship the entire Phase 4 CAMP data model: enums, tables, RLS policies, audit triggers, hard-delete blockers, logbook seal trigger, and cross-tenant RLS tests. No SQL functions and no business-logic triggers yet — those ship in Plan 04-02.

Purpose: every subsequent plan (functions, tRPC, UI, PDF) needs these tables to exist. Consolidating the full schema into one plan mirrors the Phase 2-01 / 3-01 pattern that worked.
Output: 2 migrations (0009 enums, 0010 tables+RLS+audit+seal), 10 new Drizzle schema files, extended aircraft + aircraft_squawk schemas, phase4 RLS test suite green.
</objective>

<execution_context>
@/Users/christopher/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christopher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-camp-maintenance/04-CONTEXT.md
@.planning/phases/04-camp-maintenance/04-RESEARCH.md
@packages/db/src/schema/aircraft.ts
@packages/db/src/schema/aircraft_squawk.ts
@packages/db/migrations/0007_phase3_scheduling_dispatch.sql
@tests/rls/phase3-scheduling.test.ts
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enums migration 0009 + table migration 0010 with RLS, audit, hard-delete, seal triggers</name>
  <files>
    packages/db/migrations/0009_phase4_enums.sql,
    packages/db/migrations/0010_phase4_camp_tables.sql,
    supabase/migrations/0009_phase4_enums.sql,
    supabase/migrations/0010_phase4_camp_tables.sql
  </files>
  <action>
    MIGRATION 0009 (enums ONLY — no use sites; Phase 2 enum caveat per PITFALL 2):
    - CREATE TYPE maintenance_item_kind AS ENUM ('annual_inspection','hundred_hour_inspection','airworthiness_directive','oil_change','transponder_91_413','pitot_static_91_411','elt_battery','elt_91_207','vor_check','component_life','manufacturer_service_bulletin','custom')
    - CREATE TYPE maintenance_item_status AS ENUM ('current','due_soon','overdue','grounding')
    - CREATE TYPE maintenance_item_clock AS ENUM ('hobbs','tach','airframe','engine','calendar','combined')
    - CREATE TYPE mechanic_authority_kind AS ENUM ('a_and_p','ia') — only if not already present in Phase 2 (check first; reuse if exists)
    - CREATE TYPE aircraft_component_kind AS ENUM ('magneto','prop','vacuum_pump','alternator','elt','elt_battery','starter','mag_points','spark_plug','custom')
    - CREATE TYPE component_status AS ENUM ('current','due_soon','overdue','grounding')
    - CREATE TYPE work_order_status AS ENUM ('draft','open','in_progress','pending_signoff','closed','cancelled')
    - CREATE TYPE work_order_kind AS ENUM ('annual','100_hour','ad_compliance','squawk_repair','component_replacement','oil_change','custom')
    - CREATE TYPE ad_compliance_status AS ENUM ('not_applicable','current','due_soon','overdue','grounding')
    - CREATE TYPE logbook_book_kind AS ENUM ('airframe','engine','prop')
    - CREATE TYPE part_kind AS ENUM ('consumable','overhaul_item','life_limited','hardware')
    - CREATE TYPE part_unit AS ENUM ('each','qt','gal','ft','oz','lb')
    - ALTER TYPE squawk_status ADD VALUE IF NOT EXISTS 'triaged' (before 'in_work'); ADD VALUE IF NOT EXISTS 'deferred'; ADD VALUE IF NOT EXISTS 'fixed' (after 'in_work'); ADD VALUE IF NOT EXISTS 'returned_to_service'; ADD VALUE IF NOT EXISTS 'cancelled'. Order matters for readability but not correctness.

    MIGRATION 0010 (tables, RLS, audit, hard-delete blocker, seal trigger). Every new CAMP table must:
      - have school_id uuid not null, base_id uuid nullable (where meaningful), deleted_at timestamptz nullable, audit columns (created_at/updated_at/created_by/updated_by)
      - be attached via audit.attach('<table>')
      - be attached to fn_block_hard_delete BEFORE DELETE trigger
      - have pgPolicy for SELECT/INSERT/UPDATE/DELETE scoped by auth.jwt()->>'school_id' AND current_setting('app.base_id', true) nullable-fallback (mirror 02-01 / 03-01 pattern; USE the string literal 'authenticated' in pgPolicy to: clause — NOT sql`authenticated`)

    Tables to create in 0010:
      1. maintenance_item (see CONTEXT: id, school_id, base_id, aircraft_id FK, engine_id FK nullable, component_id FK nullable, ad_compliance_id FK nullable, kind maintenance_item_kind, title, description, interval_rule jsonb NOT NULL, last_completed_at timestamptz, last_completed_hours jsonb, last_completed_by_user_id, last_work_order_id, next_due_at timestamptz, next_due_hours numeric, status maintenance_item_status NOT NULL DEFAULT 'current', notes, soft-del, audit)
      2. maintenance_item_template (id, school_id nullable for global, name, aircraft_make, aircraft_model_pattern, description, soft-del, audit)
      3. maintenance_item_template_line (id, template_id FK, kind, title, interval_rule jsonb, required_authority mechanic_authority_kind nullable, default_warning_days int)
      4. airworthiness_directive (id, school_id nullable, ad_number text, title, summary, effective_date, compliance_method, applicability jsonb, superseded_by_ad_id FK nullable, soft-del, audit; UNIQUE(school_id, ad_number) — allow NULL school_id global)
      5. aircraft_ad_compliance (id, school_id, base_id, aircraft_id FK, ad_id FK, applicable boolean NOT NULL, first_due_at, first_due_hours, recurrence_rule jsonb, status ad_compliance_status, notes, soft-del, audit; UNIQUE(aircraft_id, ad_id) partial on deleted_at IS NULL)
      6. ad_compliance_history (id, compliance_record_id FK, complied_at, complied_at_hours jsonb, method_used, work_order_id FK nullable, signer_snapshot jsonb NOT NULL, notes, created_at). Append-only: pgPolicy INSERT only.
      7. aircraft_component (id, school_id, base_id, aircraft_id FK, engine_id FK nullable, kind aircraft_component_kind, serial_number, part_number, manufacturer, installed_at_hours jsonb, installed_at_date, life_limit_hours numeric nullable, life_limit_months int nullable, overhaul_interval_hours numeric, last_overhaul_at_hours jsonb, removed_at timestamptz nullable, removed_reason text, soft-del, audit)
      8. aircraft_component_overhaul (id, component_id FK, overhauled_at, overhauled_at_hours jsonb, work_order_id FK nullable, signer_snapshot jsonb NOT NULL, notes, created_at)
      9. work_order (id, school_id, base_id, aircraft_id FK, status work_order_status NOT NULL DEFAULT 'draft', kind work_order_kind NOT NULL, title, description, assigned_to_user_id FK users nullable, source_squawk_id FK nullable, source_maintenance_item_id FK nullable, started_at, completed_at, signed_off_at, signed_off_by FK users nullable, signer_snapshot jsonb nullable, return_to_service_time jsonb nullable, soft-del, audit)
      10. work_order_task (id, work_order_id FK, position int, description, required_authority mechanic_authority_kind NOT NULL, completed_at timestamptz nullable, completed_by_user_id FK nullable, completion_signer_snapshot jsonb nullable, notes)
      11. work_order_part_consumption (id, work_order_id FK, part_id FK, part_lot_id FK nullable, quantity numeric NOT NULL CHECK(quantity > 0), consumed_at timestamptz NOT NULL DEFAULT now(), consumed_by FK users)
      12. part (id, school_id, base_id, part_number text, description, manufacturer, kind part_kind NOT NULL, unit part_unit NOT NULL, on_hand_qty numeric NOT NULL DEFAULT 0, min_reorder_qty numeric nullable, preferred_supplier text, notes, soft-del, audit; UNIQUE(school_id, part_number) partial on deleted_at IS NULL)
      13. part_lot (id, part_id FK, lot_number text, serial_number text nullable, received_at, received_by FK users, received_qty numeric NOT NULL, qty_remaining numeric NOT NULL, expires_at timestamptz nullable, supplier, invoice_ref, notes, soft-del, audit)
      14. logbook_entry (id, school_id, base_id, aircraft_id FK, engine_id FK nullable, book_kind logbook_book_kind NOT NULL, entry_date date NOT NULL, hobbs numeric, tach numeric, airframe_time numeric, engine_time numeric nullable, description text NOT NULL, work_order_id FK nullable, maintenance_item_id FK nullable, corrects_entry_id FK self nullable, signer_snapshot jsonb nullable, created_at, created_by_user_id FK, signed_at timestamptz nullable, sealed boolean NOT NULL DEFAULT false). NO deleted_at — forbidden per CONTEXT retention contract.
      15. maintenance_overrun (id, school_id, base_id, aircraft_id FK, item_id FK maintenance_item, authority_cfr_cite text NOT NULL DEFAULT '§91.409(b)', justification text NOT NULL CHECK(length(justification) >= 20), max_additional_hours int NOT NULL CHECK(max_additional_hours BETWEEN 1 AND 10), granted_at timestamptz NOT NULL DEFAULT now(), granted_by_user_id FK users, signer_snapshot jsonb NOT NULL, consumed_hours numeric NOT NULL DEFAULT 0, expires_at timestamptz NOT NULL, revoked_at timestamptz nullable, audit). UNIQUE partial index on (item_id) WHERE revoked_at IS NULL — "once-only per compliance cycle" invariant.
      16. aircraft_downtime_forecast (id, aircraft_id FK UNIQUE, next_event_at timestamptz nullable, next_event_hours numeric nullable, reason text, confidence text, refreshed_at timestamptz NOT NULL DEFAULT now()) — cache table.

    ALTER aircraft ADD COLUMN grounded_reason text, ADD COLUMN grounded_by_item_id uuid REFERENCES maintenance_item(id) ON DELETE SET NULL.
    ALTER aircraft_squawk ADD COLUMN triaged_at timestamptz, ADD COLUMN triaged_by uuid REFERENCES users(id), ADD COLUMN deferred_until date, ADD COLUMN deferral_justification text, ADD COLUMN work_order_id uuid REFERENCES work_order(id), ADD COLUMN returned_to_service_at timestamptz, ADD COLUMN returned_to_service_signer_snapshot jsonb.

    Attach audit.attach() + fn_block_hard_delete to: maintenance_item, maintenance_item_template, airworthiness_directive, aircraft_ad_compliance, aircraft_component, work_order, work_order_task, work_order_part_consumption, part, part_lot, logbook_entry, maintenance_overrun. Skip audit-only (no hard-delete blocker) for: aircraft_downtime_forecast (cache), ad_compliance_history (insert-only), aircraft_component_overhaul (insert-only), maintenance_item_template_line.

    Logbook seal trigger — create fn_logbook_entry_block_update() per RESEARCH Pattern 5: BEFORE UPDATE on logbook_entry raises exception if OLD.sealed=true; if NEW.sealed=true AND OLD.sealed=false, require signer_snapshot IS NOT NULL AND signed_at IS NOT NULL.

    ad_compliance_history INSERT-only enforcement: pgPolicy ALL USING (false) + pgPolicy FOR INSERT TO authenticated WITH CHECK (school_id = auth.jwt()->>'school_id'::uuid). Mirror audit_log pattern from Phase 1.

    Mirror both files verbatim to supabase/migrations/ (Phase 1-3 convention).
  </action>
  <verify>
    <automated>cd "/Users/christopher/Desktop/Part 61 School" && . scripts/dev-env.sh && pnpm --filter @part61/db exec drizzle-kit push 2>&1 | tail -30 && psql "$DATABASE_URL" -c "\dT+ maintenance_item_kind" -c "\d maintenance_item" -c "\d logbook_entry" -c "\d aircraft" | grep -E "grounded_reason|grounded_by_item_id|maintenance_item_kind|sealed"</automated>
  </verify>
  <done>Both migrations apply cleanly on a fresh DB; all 16 new tables exist; squawk_status enum has all 7 values; aircraft has grounded_reason + grounded_by_item_id; aircraft_squawk has triage/RTS columns; logbook seal trigger rejects UPDATE on sealed row.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Drizzle schema files + index re-exports + cross-tenant RLS test suite</name>
  <files>
    packages/db/src/schema/maintenance_item.ts,
    packages/db/src/schema/ads.ts,
    packages/db/src/schema/aircraft_component.ts,
    packages/db/src/schema/work_order.ts,
    packages/db/src/schema/part.ts,
    packages/db/src/schema/logbook_entry.ts,
    packages/db/src/schema/maintenance_overrun.ts,
    packages/db/src/schema/downtime_forecast.ts,
    packages/db/src/schema/aircraft.ts,
    packages/db/src/schema/aircraft_squawk.ts,
    packages/db/src/schema/index.ts,
    tests/rls/phase4-camp.test.ts
  </files>
  <behavior>
    - Every new table in the schema package has a Drizzle definition that matches the SQL migration exactly (column names, types, nullability, FKs, enums).
    - Extending aircraft.ts + aircraft_squawk.ts adds new columns without removing existing ones.
    - index.ts re-exports every new schema file.
    - RLS test: School A user cannot SELECT maintenance_item / work_order / logbook_entry / part / aircraft_ad_compliance / aircraft_component rows owned by School B (zero rows returned with valid JWT claims).
    - RLS test: Hard DELETE on maintenance_item / work_order / part / logbook_entry raises exception (hard-delete blocker attached).
    - RLS test: Attempting UPDATE on sealed logbook_entry raises "sealed and cannot be modified" exception.
    - RLS test: INSERT into ad_compliance_history succeeds; UPDATE/DELETE fail.
  </behavior>
  <action>
    Create 10 new Drizzle schema files mirroring the 16 tables (co-locate related tables in one file: maintenance_item.ts holds maintenance_item + templates + template_line; ads.ts holds airworthiness_directive + aircraft_ad_compliance + ad_compliance_history; work_order.ts holds work_order + task + part_consumption; part.ts holds part + part_lot; aircraft_component.ts holds component + overhaul; maintenance_overrun.ts, logbook_entry.ts, downtime_forecast.ts are solo).

    Use the exact pgEnum() / pgTable() / pgPolicy() pattern from packages/db/src/schema/aircraft_squawk.ts (Phase 3). Policies use to: 'authenticated' as string literal. school_id + base_id predicates mirror 02-01 nullable-fallback pattern.

    Extend packages/db/src/schema/aircraft.ts: add groundedReason: text('grounded_reason') and groundedByItemId: uuid('grounded_by_item_id').references(() => maintenanceItem.id, { onDelete: 'set null' }).

    Extend packages/db/src/schema/aircraft_squawk.ts: extend the squawkStatusEnum definition with new values (must match DB ordering). Add new columns (triaged_at, triaged_by, deferred_until, deferral_justification, work_order_id, returned_to_service_at, returned_to_service_signer_snapshot).

    Update packages/db/src/schema/index.ts to re-export every new file.

    Create tests/rls/phase4-camp.test.ts modeled on tests/rls/phase3-scheduling.test.ts. Use the raw postgres-js harness pattern (NOT drizzle, NOT supabase-js) so we can set request.jwt.claims GUC per call. Cover: cross-tenant isolation for all new tables; hard-delete blocker on 4 representative tables; logbook seal trigger rejection; ad_compliance_history append-only policy.

    Run `pnpm --filter @part61/rls-tests test -- phase4-camp` until green. If Phase 3 tests regress, revert — this plan must not touch them.
  </action>
  <verify>
    <automated>cd "/Users/christopher/Desktop/Part 61 School" && . scripts/dev-env.sh && pnpm --filter @part61/db typecheck && pnpm --filter @part61/rls-tests test -- phase4-camp && pnpm --filter @part61/rls-tests test -- phase3-scheduling</automated>
  </verify>
  <done>Typecheck green across packages/db; phase4-camp RLS suite green; Phase 3 RLS suite still green (regression check); 98/98 Phase 1-3 tests preserved.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @part61/db typecheck` green
- `pnpm --filter @part61/rls-tests test` green for phase4-camp AND phase3-scheduling
- psql introspection confirms all 16 tables + 12 enums + aircraft/squawk extensions
- No Phase 3 test regression
</verification>

<success_criteria>
Phase 4 data model is ready for Plan 04-02 (SQL functions + business triggers + is_airworthy_at body swap) to layer on top.
</success_criteria>

<output>
After completion, create `.planning/phases/04-camp-maintenance/04-01-SUMMARY.md` documenting: final table list, any deviations from CONTEXT, enum ordering decisions, RLS pattern confirmations, and the list of Phase 3 tests re-run (must be green).
</output>
