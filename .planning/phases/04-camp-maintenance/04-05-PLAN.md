---
phase: 04-camp-maintenance
plan: 05
type: execute
wave: 5
depends_on: ['04-04']
files_modified:
  - apps/web/package.json
  - apps/web/app/(app)/admin/aircraft/[id]/logbook/[book]/page.tsx
  - apps/web/app/(app)/admin/aircraft/[id]/logbook/[book]/export.pdf/route.ts
  - apps/web/app/(app)/admin/aircraft/[id]/logbook/[book]/pdf/LogbookPdfDocument.tsx
  - apps/web/app/(app)/admin/aircraft/[id]/logbook/[book]/pdf/README.md
  - packages/db/migrations/0013_phase4_seed_templates.sql
  - supabase/migrations/0013_phase4_seed_templates.sql
  - tests/api/phase4-logbook-pdf.test.ts
autonomous: false
requirements: [MNT-10]
must_haves:
  truths:
    - "A PDF library is pinned that works under Next.js 15 + React 19 in BOTH dev and prod build"
    - "GET /admin/aircraft/[id]/logbook/airframe/export.pdf returns a valid application/pdf response"
    - "PDF contains aircraft tail, book type, entries table (date, description, hobbs/tach/airframe, signer name + cert type + cert number), page numbers, generated-at timestamp"
    - "Separate airframe / engine / prop PDFs are generated from logbook_entry rows filtered by book_kind"
    - "System maintenance templates are seeded (C172 for-hire, C152, PA-28, generic SEL)"
    - "End-to-end human verification passes: log into dev server, walk a full CAMP scenario, confirm every Phase 4 success criterion"
    - "All phase 1-4 automated tests green"
  artifacts:
    - path: "apps/web/app/(app)/admin/aircraft/[id]/logbook/[book]/export.pdf/route.ts"
      provides: "Route Handler returning PDF stream"
    - path: "apps/web/app/(app)/admin/aircraft/[id]/logbook/[book]/pdf/LogbookPdfDocument.tsx"
      provides: "PDF document component (or pdfkit imperative builder if fallback)"
    - path: "packages/db/migrations/0013_phase4_seed_templates.sql"
      provides: "C172 / C152 / PA-28 / generic SEL maintenance templates"
  key_links:
    - from: "export.pdf/route.ts"
      to: "LogbookPdfDocument + logbook_entry query"
      via: "renderToStream (react-pdf) OR pdfkit stream (fallback)"
    - from: "logbook/[book]/page.tsx"
      to: "export.pdf/route.ts"
      via: "Big 'Export PDF' button linking to the route"
---

<objective>
Close out Phase 4 with the logbook PDF export, seeded maintenance templates, and a single end-of-phase human-verify checkpoint that walks the entire CAMP ceremony.

Purpose: the PDF library is the only genuine technical risk in Phase 4 (PITFALL 1). Isolating it in the final plan means a late pivot to pdfkit does not destabilize any earlier plan. The human-verify checkpoint is the MNT-01..11 end-to-end proof.
Output: pinned PDF lib, 4 new web files, 1 seed migration, 1 API test for the PDF route, and a passing end-of-phase checkpoint.
</objective>

<execution_context>
@/Users/christopher/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christopher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-camp-maintenance/04-CONTEXT.md
@.planning/phases/04-camp-maintenance/04-RESEARCH.md
@.planning/phases/04-camp-maintenance/04-04-SUMMARY.md
@apps/web/app/(app)/admin/aircraft/[id]/page.tsx
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: PDF library spike + pin + logbook view page + export route + seed templates</name>
  <files>
    apps/web/package.json,
    apps/web/app/(app)/admin/aircraft/[id]/logbook/[book]/page.tsx,
    apps/web/app/(app)/admin/aircraft/[id]/logbook/[book]/export.pdf/route.ts,
    apps/web/app/(app)/admin/aircraft/[id]/logbook/[book]/pdf/LogbookPdfDocument.tsx,
    apps/web/app/(app)/admin/aircraft/[id]/logbook/[book]/pdf/README.md,
    packages/db/migrations/0013_phase4_seed_templates.sql,
    supabase/migrations/0013_phase4_seed_templates.sql,
    tests/api/phase4-logbook-pdf.test.ts
  </files>
  <action>
    SPIKE FIRST (5-15 min, BEFORE any other work — PITFALL 1):
      1. `pnpm --filter @part61/web add @react-pdf/renderer` — pin latest compatible with React 19 (check https://github.com/diegomura/react-pdf/releases for React 19 support notes; as of late 2024 @react-pdf/renderer 4.0+ claims support but verify).
      2. Create a throwaway minimal page at apps/web/app/(app)/admin/_pdf-spike/route.ts that uses `renderToStream(<Document><Page><View><Text>Hello</Text></View></Page></Document>)` and returns it as a Response.
      3. Run `pnpm --filter @part61/web dev` and curl the route — must return a valid PDF (`file` command reports PDF).
      4. Run `pnpm --filter @part61/web build && pnpm --filter @part61/web start` and curl again — must STILL return a valid PDF in production mode.
      5. If BOTH succeed: delete the spike route, keep the @react-pdf/renderer dependency with an EXACT version pin in package.json (no ^).
      6. If EITHER fails (SECRET_INTERNALS crash, renderToStream returns undefined, React internals stack trace): `pnpm --filter @part61/web remove @react-pdf/renderer && pnpm --filter @part61/web add pdfkit @types/pdfkit` and pivot to pdfkit. Document the pivot in pdf/README.md with the error message and the version attempted.

    LOGBOOK VIEW PAGE apps/web/app/(app)/admin/aircraft/[id]/logbook/[book]/page.tsx:
      - Server component. Reads logbook_entry rows via admin.logbook.list({aircraftId, bookKind}).
      - Columns: entry_date, description, hobbs/tach/airframe_time, signer (full_name + certificate_type + certificate_number if sealed).
      - Shows "DRAFT" badge on unsealed rows. Sealed rows are read-only.
      - Top of page: aircraft header + big "Export PDF" button → href="./export.pdf" opening in new tab.
      - Tab-style book kind switcher: airframe / engine / prop (for ME aircraft, multiple engine books).

    EXPORT ROUTE apps/web/app/(app)/admin/aircraft/[id]/logbook/[book]/export.pdf/route.ts:
      - `export const runtime = 'nodejs'` and `export const dynamic = 'force-dynamic'` (NOT edge — PDF libs need Node APIs).
      - GET handler: await auth (reuse server helper that mirrors tRPC ctx — mechanicOrAdminProcedure equivalent; if caller lacks role, return 403).
      - Fetch logbook entries for this aircraft+book.
      - If @react-pdf/renderer: `const stream = await renderToStream(<LogbookPdfDocument data={data} />); return new Response(stream as unknown as ReadableStream, {headers: {'content-type':'application/pdf','content-disposition':`inline; filename="${tail}-${book}-logbook.pdf"`}});`
      - If pdfkit fallback: build a PDFDocument imperatively, pipe to a PassThrough, return the stream. Use `doc.font('Helvetica')`, `doc.fontSize(10)`, add table rows manually, add page numbers via `doc.on('pageAdded', ...)`.

    PDF DOCUMENT apps/web/app/(app)/admin/aircraft/[id]/logbook/[book]/pdf/LogbookPdfDocument.tsx (or LogbookPdfBuilder.ts if pdfkit):
      - Header on every page: aircraft registration (tail), make/model/year/serial, book kind ("Airframe Logbook" / "Engine Logbook" / "Propeller Logbook"), current total times.
      - Table: entry_date, description, hobbs, tach, airframe_time, signer ("Jane Mechanic, IA 3001234567").
      - Footer on every page: generated-at timestamp, page "X of N", disclaimer "This export is a true copy of entries sealed on the dates shown."
      - Narrow print-friendly margins. Helvetica / sans-serif. Black text on white.
      - NO banned terms in any static string.

    API TEST tests/api/phase4-logbook-pdf.test.ts:
      - Seed one aircraft + 3 sealed logbook entries.
      - Call the route handler function directly (import and invoke with a fake Request).
      - Assert response headers content-type === 'application/pdf'.
      - Read the response body and assert the first 4 bytes are `%PDF` (valid PDF magic).
      - Assert the PDF binary size > 1KB (sanity).

    SEED MIGRATION packages/db/migrations/0013_phase4_seed_templates.sql:
      - INSERT system maintenance_item_template rows (school_id NULL):
        * 'C172 for-hire standard' — lines: annual (calendar 12mo), hundred_hour (tach 100h), elt_91_207 (calendar 12mo), elt_battery (calendar 12mo-50%), transponder_91_413 (calendar 24mo), pitot_static_91_411 (calendar 24mo, IFR), vor_check (calendar 30d, IFR), oil_change (tach 50h).
        * 'C152 standard' — annual, hundred_hour, elt_91_207, elt_battery, transponder_91_413, oil_change.
        * 'PA-28 standard' — annual, hundred_hour, elt_91_207, elt_battery, transponder_91_413, oil_change.
        * 'Generic single-engine' — annual, elt_91_207.
      - Each template line has kind (from maintenance_item_kind enum), title (no banned terms), interval_rule jsonb, default_warning_days.
      - Mirror to supabase/migrations/.
  </action>
  <verify>cd "/Users/christopher/Desktop/Part 61 School" &amp;&amp; . scripts/dev-env.sh &amp;&amp; pnpm --filter @part61/db exec drizzle-kit push 2>&amp;1 | tail -5 &amp;&amp; pnpm --filter @part61/web typecheck &amp;&amp; pnpm --filter @part61/web lint &amp;&amp; pnpm --filter @part61/web build 2>&amp;1 | tail -10 &amp;&amp; pnpm --filter @part61/api test -- phase4-logbook-pdf</verify>
  <done>PDF library pinned and working in both dev and prod build; logbook view page renders entries; export.pdf route returns a valid %PDF response; 4 seed templates in DB; test suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-of-phase human verification — full CAMP ceremony walkthrough</name>
  <what-built>
    Complete Phase 4 CAMP engine: all tables, SQL functions, tRPC routers, admin UI pages, logbook PDF export, and seeded templates. The Phase 3 `is_airworthy_at` stub has been replaced with real rules. Phase 1-3 automated tests (98/98) still pass.
  </what-built>
  <how-to-verify>
    Start the dev server: `cd "/Users/christopher/Desktop/Part 61 School" && . scripts/dev-env.sh && pnpm --filter @part61/web dev` (http://localhost:3000).

    Walk through the full CAMP ceremony as admin with mechanic_authority='ia':

    1. MAINTENANCE DASHBOARD: Visit /admin/maintenance. Confirm cross-fleet list shows items grouped by aircraft with status chips. Sort works.

    2. AIRCRAFT PROFILE: Visit /admin/aircraft/{id}. Confirm MaintenancePanel renders next to EnginesPanel + RecentFlightsPanel. Confirm "Next grounding" countdown is present. Confirm any grounded aircraft shows a red banner with the cause.

    3. TEMPLATE APPLY: Create a new Cessna 172. On the success page, confirm the prompt offers "Apply C172 for-hire standard template — creates 8 maintenance items?" Accept. Verify 8 maintenance_items appear on the new aircraft's /maintenance tab.

    4. SQUAWK LIFECYCLE: From the dispatch close-out flow, open a new squawk with severity='grounding'. Confirm the aircraft auto-grounds. Visit /admin/squawks/{id}. Triage → In Work → create Work Order → complete tasks → Mark Fixed → Sign and Return to Service (as IA). Confirm the aircraft un-grounds automatically.

    5. WORK ORDER CEREMONY: On the WO detail page, confirm the sign-off button is disabled until all tasks are complete. Complete tasks. Click "Sign and Return to Service" — confirm the modal shows your IA cert number and legally-binding language. Submit. Confirm WO.status='closed', logbook entries written, aircraft unlocked.

    6. §91.409 OVERRUN: Create a test aircraft with a 100-hour inspection item that is OVERDUE. Confirm it is auto-grounded. Visit the aircraft page as an IA user. Click "IA: Request §91.409 overrun". Fill in justification (>= 20 chars) + max hours (5). Confirm the red countdown banner appears and the aircraft is now airworthy. Verify on the dispatch screen.

    7. §91.409 GATE: Try to trigger the overrun button on an ANNUAL inspection (not 100-hour). Confirm the button does NOT render. Try via direct API call — confirm it is rejected.

    8. PARTS INVENTORY: Visit /admin/parts. Create a lot-tracked part. Receive a lot. Consume some from a work order. Confirm the lot qty_remaining decrements and part.on_hand_qty stays in sync.

    9. AD TRACKING: Visit /admin/ads. Create a new AD for Cessna 172. Click "Apply to Fleet". Visit the AD detail and confirm the C172 from step 3 appears with "not yet complied". Record compliance (IA signer). Confirm compliance history row appears.

    10. LOGBOOK PDF: Visit /admin/aircraft/{id}/logbook/airframe. Confirm entries from the work order ceremony appear with signer details. Click "Export PDF". Confirm a valid PDF downloads/opens with the correct header, table, and footer. Open in a PDF reader and confirm it's not corrupt. Repeat for engine and prop books.

    11. DISPATCH MEL BADGE: Create a deferred squawk on an aircraft. Visit /dispatch. Confirm the yellow "MEL: 1 deferred" badge renders next to that aircraft's tail number without blocking dispatch.

    12. GROUNDING FORECAST: Visit /admin/maintenance (dashboard). Confirm each aircraft shows a "next grounding" projection with a confidence level.

    13. REGRESSION: Run `pnpm --filter @part61/rls-tests test` in a second terminal. Confirm all 98 Phase 1-3 tests still pass. Run `pnpm --filter @part61/api test`. Confirm Phase 4 tests (camp-api + logbook-pdf) also pass.

    14. LINT / BUILD: Run `pnpm --filter @part61/web lint` (banned-term check) and `pnpm --filter @part61/web build` (prod build). Both must be clean.

    If ANY step fails or looks wrong, describe the issue in your response and the plan will be revised. If all 14 steps pass, respond "approved" and Phase 4 is complete.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues encountered</resume-signal>
</task>

</tasks>

<verification>
Phase 4 success criteria from ROADMAP.md all proven:
1. Typed maintenance items with clocks + deterministic due — verified in step 2, 3
2. Auto-ground + IA §91.409 overrun — verified in step 6, 7
3. Squawk lifecycle with signer snapshots — verified in step 4
4. Work order → parts → sign-off → logbook PDF — verified in step 5, 8, 10
5. Downtime forecast on dashboard + aircraft profile — verified in step 2, 12
6. is_airworthy_at replaced (Phase 3 tests still green) — verified in step 13
Plus: seeded templates (step 3), AD tracking (step 9), MEL badge on dispatch (step 11).
</verification>

<success_criteria>
All 14 human-verify steps pass. Automated test suites remain green across all phases. Phase 4 is complete and MNT-01..11 are satisfied.
</success_criteria>

<output>
After the human-verify checkpoint passes, create `.planning/phases/04-camp-maintenance/04-05-SUMMARY.md` documenting: PDF library chosen + version pinned, any pivot from @react-pdf/renderer to pdfkit, final seed template list, and the human-verify result. Then create `.planning/phases/04-camp-maintenance/04-PHASE-SUMMARY.md` rolling up the whole phase.
</output>
