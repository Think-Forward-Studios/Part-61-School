---
phase: 05-syllabus-model-grading-records
plan: 02
type: execute
wave: 2
depends_on: ['05-01']
files_modified:
  - packages/db/migrations/0019_phase5_seed_endorsements.sql
  - packages/db/migrations/0020_phase5_seed_courses.sql
  - supabase/migrations/20260409000006_phase5_seed_endorsements.sql
  - supabase/migrations/20260409000007_phase5_seed_courses.sql
  - supabase/seed.sql
  - tests/db/phase5-seed.test.ts
autonomous: true
requirements:
  - SYL-02
  - SYL-03
  - SYL-09
must_haves:
  truths:
    - 'endorsement_template table contains the full AC 61-65K standard endorsement catalog (A.1–A.17 + B.1–B.11 at minimum)'
    - '3 system courses exist with school_id=null: Private Pilot, Instrument Rating, Commercial Single-Engine'
    - 'Each seeded course has at least 1 course_version with published_at set, depth = 5 or 6 levels populated, absolute_ipm grading_scale'
    - 'Private Pilot seed has ~3 stages, ~25 lessons, ~120 line items (tolerance ±20%)'
    - 'Seeds survive `supabase db reset` because they are re-applied via supabase/seed.sql after CASCADE'
    - "FAA text 'approved' / 'Part 141' only appears in .sql files (banned-term lint glob excludes them)"
  artifacts:
    - path: 'packages/db/migrations/0019_phase5_seed_endorsements.sql'
      provides: 'AC 61-65K endorsement template catalog'
      contains: 'insert into public.endorsement_template'
    - path: 'packages/db/migrations/0020_phase5_seed_courses.sql'
      provides: '3 seeded courses + course_versions + tree'
      contains: 'insert into public.course'
    - path: 'supabase/seed.sql'
      provides: 're-seed block that survives TRUNCATE schools CASCADE'
      contains: 'DO $phase5_seeds$'
    - path: 'tests/db/phase5-seed.test.ts'
      provides: 'asserts seeded counts (≥20 endorsements, 3 courses, ≥60 lessons total)'
  key_links:
    - from: 'course (school_id=null)'
      to: 'course_version → stage → lesson → line_item'
      via: 'parent FK chain'
      pattern: "references public\\.(course|course_version|stage|lesson)"
    - from: 'supabase/seed.sql'
      to: 'endorsement_template, course, course_version, stage, lesson, line_item'
      via: 're-insert block'
      pattern: "insert into public\\.(endorsement_template|course|course_version|stage|lesson|line_item)"
---

<objective>
Seed the AC 61-65K endorsement catalog and the 3 foundational system courses (PPL, IR, Commercial SEL) as `school_id=null` global templates. Mirror the Phase 4 Plan 05 seed pattern that handles `TRUNCATE schools CASCADE` via `supabase/seed.sql` re-insertion.

Purpose: Without seeds, no school can enroll a student, no instructor can issue an endorsement, and no PDF has content. These are the table stakes data for downstream plans.
Output: 2 migrations + supabase/seed.sql re-seed block + a seed-count test that runs on CI.
</objective>

<execution_context>
@/Users/christopher/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christopher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-syllabus-model-grading-records/05-CONTEXT.md
@.planning/phases/05-syllabus-model-grading-records/05-RESEARCH.md
@.planning/phases/05-syllabus-model-grading-records/05-01-PLAN.md
@.planning/phases/04-camp-maintenance/04-05-SUMMARY.md

<!-- Phase 4 Plan 05 proved the pattern for survival across supabase db reset -->

<interfaces>
From 05-01 (Wave 1):
- endorsement_template columns: id, code, title, body_template markdown, category enum, ac_reference text, deleted_at
- course columns: id, school_id (NULLABLE for system templates), code, title, rating_sought enum, description, created_at, created_by, deleted_at
- course_version columns: id, course_id FK, version_label, published_at, published_by, grading_scale enum, min_levels int, notes, superseded_at
- Course tree: stage → course_phase (optional) → unit (optional) → lesson → line_item with exclusive parent CHECK constraints
- lesson.kind enum: ground | flight | simulator | oral | written_test
- line_item.classification enum: required | optional | must_pass

Rating sought enum values to use:

- private_pilot, instrument_rating, commercial_single_engine

Grading scale for all 3 seeds: absolute_ipm
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seed AC 61-65K endorsement templates (migration 0019)</name>
  <files>
    packages/db/migrations/0019_phase5_seed_endorsements.sql,
    supabase/migrations/20260409000006_phase5_seed_endorsements.sql,
    supabase/seed.sql
  </files>
  <action>
    Create migration 0019: INSERT rows into `endorsement_template` covering the AC 61-65K catalog. Source of truth: AC 61-65K Appendix A (student pilot + solo) and Appendix B (sport/recreational/private/instrument/commercial/ATP/CFI). At minimum include:

    Appendix A (student pilot / solo):
    - A.1  Pre-solo aeronautical knowledge test (§61.87(b))
    - A.2  Pre-solo flight training (§61.87(c)(1) and (2))
    - A.3  Solo flight — student (§61.87(n))
    - A.4  Solo takeoffs and landings at another airport (§61.93(b)(1))
    - A.5  Initial solo cross-country (§61.93(c)(1) and (2))
    - A.6  Solo cross-country (each flight) (§61.93(c)(3))
    - A.7  Repeated solo cross-country not more than 50 nm (§61.93(b)(2))
    - A.8  Solo flight in Class B airspace (§61.95(a))
    - A.9  Solo to/from/at an airport in Class B (§61.95(b))

    Appendix B selections:
    - B.1  Flight review (§61.56(a) and (c))
    - B.2  Instrument proficiency check (§61.57(d))
    - B.3  Additional training — complex airplane (§61.31(e))
    - B.4  Additional training — high performance (§61.31(f))
    - B.5  Additional training — pressurized / high altitude (§61.31(g))
    - B.6  Additional training — tailwheel (§61.31(i))
    - B.7  Retesting after failure (§61.49)
    - B.8  Completion of prerequisites for practical test
    - B.9  Aeronautical knowledge test (recommendation)
    - B.10 Practical test (recommendation)
    - B.11 Launch procedures for glider towing (§61.69)

    For each row supply: code ('A.1' etc.), title (FAA canonical title — the literal FAA text may contain 'approved'; this is fine in .sql which is outside the banned-term lint glob), `body_template` markdown with placeholders `{{student_name}}`, `{{student_cert_number}}`, `{{aircraft_make_model}}`, `{{date}}`, `{{instructor_name}}`, `{{instructor_cfi_number}}`, `{{instructor_cfi_expiration}}` — the body text should match the literal AC 61-65K template text for that endorsement, `category` (student_pilot | solo | xc | aircraft_class_rating | flight_review | ipc | practical_test | knowledge_test | other), `ac_reference` ('AC 61-65K, A.1' etc.).

    Mirror byte-identical to supabase/migrations/20260409000006_*.sql.

    Append a re-seed block to `supabase/seed.sql` (before the `session_replication_role = origin` switch) inside `DO $phase5_endorsements$ … END $phase5_endorsements$;`. Use `ON CONFLICT (code) DO NOTHING` so the block is idempotent. Comment inline citing Phase 4-05 pattern: the TRUNCATE schools CASCADE wipes endorsement_template because... wait — endorsement_template has no school_id FK, so it's NOT cascaded. Verify by checking the 05-01 schema. If endorsement_template has NO school_id column, it survives CASCADE and no re-seed is needed — in that case, add a brief comment in seed.sql explaining why no re-seed is required for endorsements.

  </action>
  <verify>
    <automated>pnpm dlx supabase db reset 2>&1 | tail -10 && psql "$DATABASE_URL" -c "select count(*) from public.endorsement_template" | grep -E "^\s+[2-9][0-9]" && pnpm -r lint</automated>
  </verify>
  <done>≥20 endorsement_template rows after `supabase db reset`. Banned-term lint clean (FAA text only in .sql files, not .ts/.tsx).</done>
</task>

<task type="auto">
  <name>Task 2: Seed 3 system courses (PPL / IR / Comm SEL) + seed-count test (migration 0020)</name>
  <files>
    packages/db/migrations/0020_phase5_seed_courses.sql,
    supabase/migrations/20260409000007_phase5_seed_courses.sql,
    supabase/seed.sql,
    tests/db/phase5-seed.test.ts
  </files>
  <action>
    Create migration 0020: seed 3 courses with `school_id=null` (global templates). For each course, create exactly 1 `course_version` with `published_at = now()`, `grading_scale = 'absolute_ipm'`, `min_levels = 5` (Stage → Unit → Lesson → Line Item depth; skip course_phase for simplicity), `version_label = 'v1.0 (Reference)'`.

    **Private Pilot (code 'PPL-SE', rating_sought='private_pilot')** — 3 stages, ~25 lessons, ~120 line items. Description must cite source: "Derived from publicly-available Louisiana Tech / Auburn University TCO materials and FAA PPL-A ACS (FAA-S-ACS-6B). Schools should fork and customize before use." Stage structure:
      - Stage 1 "Pre-Solo" (~9 lessons: intro, preflight, ground ops, basic maneuvers, slow flight, stalls, emergency procedures, traffic pattern, first solo prep)
      - Stage 2 "Cross-Country & Night" (~9 lessons: dual XC, night intro, night XC, solo XC short, solo XC long, navigation by pilotage, dead reckoning, radio nav, IFR hood intro)
      - Stage 3 "Checkride Prep" (~7 lessons: short/soft field, power-off 180, performance takeoffs, ACS review, mock checkride, stage 3 check, end-of-course)
    For each lesson: kind, ~4-6 line_items with classification (most `required`, some `optional`, key maneuvers `must_pass`). Line items should name ACS tasks (e.g. "Steep turns — PA.V.A", "Power-off stalls — PA.VII.B") so they map cleanly to the standard.

    **Instrument Rating (code 'IR-SE', rating_sought='instrument_rating')** — 3 stages, ~20 lessons, ~100 line items. Stages: "Basic Attitude Instrument Flying", "Navigation & Approaches", "Checkride Prep". Source citation same pattern, referencing FAA IR-A ACS.

    **Commercial Single-Engine (code 'CSEL', rating_sought='commercial_single_engine')** — 3 stages, ~18 lessons, ~90 line items. Stages: "Commercial Maneuvers", "Cross-Country & Complex Ops", "Checkride Prep". Source citation referencing FAA Comm-A ACS.

    All line_item.objectives and completion_standards populated with short markdown strings (1-2 sentences per field — quality is "minimum viable starting point" not "comprehensive course"). Do NOT use the banned words "approved" or "Part 141" in description/objectives/completion_standards (these ARE .sql but they get rendered into UI later — use "authorized" / "required" / "sanctioned"). Exception: if citing a CFR section literally (e.g. "per §61.129(a)"), that's a citation not a banned term.

    Mirror byte-identical to supabase/migrations/20260409000007_*.sql.

    Append re-seed DO block to `supabase/seed.sql` for the courses. Because course has a school_id column (nullable) but `public.schools` is TRUNCATEd CASCADE at the top of seed.sql, and course.school_id is a FK to schools, PostgreSQL's TRUNCATE CASCADE WILL wipe the course table (same as Phase 4 templates). So the re-seed is REQUIRED. Use `ON CONFLICT (id) DO NOTHING` or build it so it re-inserts cleanly. Match the Phase 4-05 pattern exactly.

    Create `tests/db/phase5-seed.test.ts` (extends existing Vitest DB harness):
    - After supabase db reset: assert `select count(*) from endorsement_template >= 20`
    - Assert 3 courses exist with school_id is null and rating_sought in the 3 expected values
    - Assert each course has exactly 1 published course_version
    - Assert total lesson count across all 3 courses is ≥ 50 (tolerance)
    - Assert total line_item count across all 3 courses is ≥ 250
    - Assert clone_course_version(PPL course_version_id, <fake school uuid>) returns a new uuid and the clone has the same lesson count as the source

  </action>
  <verify>
    <automated>pnpm dlx supabase db reset 2>&1 | tail -10 && pnpm --filter @part61/rls-tests test -- phase5-seed 2>&1 | tail -20 && pnpm -r lint && pnpm -r typecheck</automated>
  </verify>
  <done>3 system courses seeded + survive db reset via seed.sql re-insert. phase5-seed.test.ts green. clone_course_version round-trips. Banned-term lint clean.</done>
</task>

</tasks>

<verification>
- `pnpm dlx supabase db reset` clean
- `pnpm --filter @part61/rls-tests test` all green including new phase5-seed tests
- `pnpm -r lint` banned-term clean
</verification>

<success_criteria>

- endorsement_template populated with AC 61-65K catalog
- 3 system courses (PPL, IR, CSEL) with full tree, each a published v1.0 course_version
- Seeds survive `supabase db reset` via supabase/seed.sql re-insert block
- Seed count test passes
- Phase 1-4 regression intact
  </success_criteria>

<output>
Create `.planning/phases/05-syllabus-model-grading-records/05-02-SUMMARY.md`.
</output>
