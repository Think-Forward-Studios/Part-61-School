---
phase: 05-syllabus-model-grading-records
plan: 03
type: execute
wave: 2
depends_on: ['05-01']
files_modified:
  - packages/api/src/procedures.ts
  - packages/api/src/routers/admin/courses.ts
  - packages/api/src/routers/admin/enrollments.ts
  - packages/api/src/routers/admin/stageChecks.ts
  - packages/api/src/routers/admin/endorsements.ts
  - packages/api/src/routers/admin/studentCurrencies.ts
  - packages/api/src/routers/gradeSheet.ts
  - packages/api/src/routers/flightLog.ts
  - packages/api/src/routers/record.ts
  - packages/api/src/routers/schedule.ts
  - packages/api/src/routers/index.ts
  - packages/api/src/helpers/buildInstructorSignerSnapshot.ts
  - packages/domain/src/schemas/gradingLabels.ts
  - packages/domain/src/schemas/lessonKindLabels.ts
  - packages/domain/src/schemas/endorsementCategoryLabels.ts
  - tests/api/phase5-routers.test.ts
autonomous: true
requirements:
  - SYL-01
  - SYL-03
  - SYL-04
  - SYL-05
  - SYL-06
  - SYL-07
  - SYL-08
  - SYL-09
  - SYL-12
  - SYL-13
  - SYL-14
  - SYL-25
  - SCH-12
must_haves:
  truths:
    - 'Admin can CRUD courses, course_versions, stages, phases, units, lessons, line_items via tRPC (guarded by adminOrChiefInstructorProcedure)'
    - 'Instructor can create a draft lesson_grade_sheet from a reservation, edit line_item_grades, and seal it — after sealing, mutations fail server-side'
    - "Stage check cannot be created where checker_user_id equals the student's primary_instructor_id"
    - 'Student endorsement is issued with fully-rendered text snapshotted from placeholders'
    - 'schedule.checkStudentCurrency(lessonId, studentId) returns { blockers: [] } when currencies satisfy lesson.required_currencies, and { blockers: [...] } otherwise'
    - 'gradeSheet.sealGradeSheet fails if any Required line_item has no grade'
    - 'record.me / record.myFlightLog / record.myCurrencies / record.myCourseProgress return ONLY rows matching ctx.session.userId'
    - 'Test grades (SYL-25) can be recorded against any course component via gradeSheet.recordTestGrade'
  artifacts:
    - path: 'packages/api/src/routers/admin/courses.ts'
      provides: 'course / course_version / stage / lesson / line_item CRUD'
      exports: ['coursesRouter']
    - path: 'packages/api/src/routers/gradeSheet.ts'
      provides: 'grade sheet create/update/seal + test grade entry'
      exports: ['gradeSheetRouter']
    - path: 'packages/api/src/routers/record.ts'
      provides: 'student-facing read-only queries scoped to ctx.session.userId'
      exports: ['recordRouter']
    - path: 'packages/api/src/procedures.ts'
      provides: 'adminOrChiefInstructorProcedure + instructorProcedure'
      contains: 'adminOrChiefInstructorProcedure'
    - path: 'packages/api/src/helpers/buildInstructorSignerSnapshot.ts'
      provides: 'signer snapshot for grade sheet / stage check / endorsement'
      exports: ['buildInstructorSignerSnapshot']
  key_links:
    - from: 'schedule.approve (Phase 3)'
      to: 'schedule.checkStudentCurrency'
      via: 'additive call in approve flow'
      pattern: 'checkStudentCurrency'
    - from: 'gradeSheet.seal'
      to: 'buildInstructorSignerSnapshot'
      via: 'direct helper call at seal time'
      pattern: 'buildInstructorSignerSnapshot'
    - from: 'admin.endorsements.issue'
      to: "endorsement_template + student profile + today's date"
      via: 'placeholder substitution producing rendered_text'
      pattern: "\\{\\{student_name\\}\\}"
---

<objective>
Build the tRPC surface for Phase 5: admin routers for courses / enrollments / stage checks / endorsements / student currencies, instructor routers for grade sheets and flight time categorization, and student-facing read-only record router. Add `adminOrChiefInstructorProcedure`, the instructor signer-snapshot helper, domain label files (to dodge the banned-term lint), and SCH-12's `schedule.checkStudentCurrency`. Wire Phase 3's `schedule.approve` to call it.

Purpose: UI and PDF routes in 05-04 and 05-05 consume this API. Without it, nothing ships.
Output: ~10 router files, 1 helper, 3 label files, API integration tests.
</objective>

<execution_context>
@/Users/christopher/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christopher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-syllabus-model-grading-records/05-CONTEXT.md
@.planning/phases/05-syllabus-model-grading-records/05-01-PLAN.md
@.planning/phases/03-scheduling-dispatch-execution/03-02-SUMMARY.md
@.planning/phases/04-camp-maintenance/04-03-SUMMARY.md

<interfaces>
From 05-01 (schema):
- All Phase 5 tables listed in 05-01 must_haves
- clone_course_version(source_uuid, new_school_id) PL/pgSQL function
- user_flight_log_totals view
- Seal triggers enforce immutability DB-side — routers rely on them as defense-in-depth

From Phase 2/3/4 established API patterns:

- `protectedProcedure`, `adminProcedure`, `mechanicOrAdminProcedure` in packages/api/src/procedures.ts
- `withTenantTx(ctx, async tx => ...)` sets app.school_id + app.base_id GUCs
- `buildSignerSnapshot(ctx, requiredAuthority)` — Phase 4 helper for mechanics; Phase 5 adds `buildInstructorSignerSnapshot(ctx)` that validates active_role='instructor' (or admin) and copies person_profile.first_name/last_name + users.active_role into a JSONB
- Routers exported from `packages/api/src/routers/index.ts` as `appRouter`
- All mutations go through `withTenantTx`

Banned-term caveat:

- User-facing strings in UI code must NOT use "approved" / "Part 141" / "certified course"
- Display labels live in packages/domain/src/schemas/\*Labels.ts which is outside the lint glob
- Internal enum values like lesson.kind='written_test' are fine (not user-facing text)

Phase 3 schedule.approve already exists in packages/api/src/routers/schedule.ts — Phase 5 ADDS a call to checkStudentCurrency(lessonId, studentId) in that flow IF reservation.lesson_id is set.

Chief instructor flag: `user_roles.is_chief_instructor boolean` — if it doesn't exist yet, add it in a micro-migration at the top of this plan via `ALTER TABLE public.user_roles ADD COLUMN is_chief_instructor boolean not null default false;` (call it migration 0021_phase5_chief_instructor.sql + supabase mirror).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Procedures + helper + admin routers (courses, enrollments, stageChecks, endorsements, studentCurrencies) + migration 0021 chief instructor flag</name>
  <files>
    packages/db/migrations/0021_phase5_chief_instructor.sql,
    supabase/migrations/20260409000008_phase5_chief_instructor.sql,
    packages/api/src/procedures.ts,
    packages/api/src/helpers/buildInstructorSignerSnapshot.ts,
    packages/api/src/routers/admin/courses.ts,
    packages/api/src/routers/admin/enrollments.ts,
    packages/api/src/routers/admin/stageChecks.ts,
    packages/api/src/routers/admin/endorsements.ts,
    packages/api/src/routers/admin/studentCurrencies.ts,
    packages/domain/src/schemas/gradingLabels.ts,
    packages/domain/src/schemas/lessonKindLabels.ts,
    packages/domain/src/schemas/endorsementCategoryLabels.ts
  </files>
  <action>
    Write migration 0021: `ALTER TABLE public.user_roles ADD COLUMN is_chief_instructor boolean not null default false;` plus mirror to supabase/migrations.

    Add to `packages/api/src/procedures.ts`:
    - `adminOrChiefInstructorProcedure` — extends protectedProcedure, checks `ctx.session.activeRole === 'admin' OR (ctx.session.activeRole === 'instructor' AND user_roles.is_chief_instructor = true)`. Throws FORBIDDEN otherwise.
    - `instructorProcedure` — requires activeRole in ('instructor','admin').

    Create `packages/api/src/helpers/buildInstructorSignerSnapshot.ts`:
    ```
    export async function buildInstructorSignerSnapshot(ctx, tx): Promise<SignerSnapshot>
    ```
    Reads person_profile.first_name/last_name + users.faa_airman_cert_number + active_role. Validates active_role is instructor or admin. Returns `{ user_id, full_name, certificate_type: 'cfi'|'cfii'|'mei'|'admin', certificate_number, signed_at: new Date().toISOString() }`. Snapshot is COPIED, never referenced later.

    Create label files in `packages/domain/src/schemas/`:
    - `gradingLabels.ts`: maps grading_scale enum + grade_value → display strings. absolute_ipm: {I:'Introduce', P:'Practice', PM:'Perform', M:'Mastered'}; relative_5: 1-5; pass_fail: {pass:'Pass', fail:'Fail'}. Use only neutral language — NO banned terms.
    - `lessonKindLabels.ts`: {ground:'Ground', flight:'Flight', simulator:'Simulator', oral:'Oral', written_test:'Written Test'}
    - `endorsementCategoryLabels.ts`: maps category enum to display strings

    Create `packages/api/src/routers/admin/courses.ts`:
    - `list` — returns system templates (school_id IS NULL) + school's own courses
    - `get(id)` — returns course + versions
    - `getVersion(versionId)` — returns full tree (stages, phases, units, lessons, line_items) in a nested shape
    - `createCourse({ code, title, rating_sought, description })`
    - `fork({ sourceVersionId, newCode, newTitle })` — calls `clone_course_version()` SQL function
    - `createVersion({ courseId, versionLabel, grading_scale, min_levels })` — draft state
    - `publish(versionId)` — sets published_at (seal trigger locks editing)
    - `createStage / createCoursePhase / createUnit / createLesson / createLineItem` — each validates the parent version is still draft (published_at IS NULL)
    - `updateLesson / updateLineItem` — same draft check
    - `deleteStage / deleteLesson` — soft delete (deleted_at)
    All gated by adminOrChiefInstructorProcedure. All routes through withTenantTx.

    Create `packages/api/src/routers/admin/enrollments.ts`:
    - `list({ studentId? })` — returns enrollments with course_version joined
    - `enroll({ studentUserId, courseVersionId, startedAt, primaryInstructorId })`
    - `migrate({ enrollmentId, targetVersionId, justification })` — audit-logged
    - `complete(enrollmentId)` / `withdraw(enrollmentId, reason)`

    Create `packages/api/src/routers/admin/stageChecks.ts`:
    - `list({ studentId?, status? })`
    - `schedule({ studentEnrollmentId, stageId, checkerUserId, scheduledAt })` — server-side different-instructor check BEFORE insert (DB trigger is defense-in-depth)
    - `record({ stageCheckId, status: 'passed'|'failed', remarks })` — seals with signer snapshot

    Create `packages/api/src/routers/admin/endorsements.ts`:
    - `listTemplates()` — returns endorsement_template rows, filterable by category
    - `listStudentEndorsements({ studentUserId })`
    - `issue({ studentUserId, templateId?, rendered_text, expires_at?, aircraftContext?, notes })` — if templateId set, server pre-renders the body_template with placeholder substitution from student profile + instructor profile + today's date, and the caller must confirm the rendered text (passed in). Seals immediately with signer snapshot.
    - `revoke({ endorsementId, reason })` — sets revoked_at (soft, append-only)

    Create `packages/api/src/routers/admin/studentCurrencies.ts`:
    - Mirrors the Phase 2 `people.currencies.*` shape but filters `personnel_currency WHERE subject_kind='student'`
    - `list({ studentUserId })` / `record({ studentUserId, kind, issuedAt, expiresAt, notes })` / `update` / `delete`

  </action>
  <verify>
    <automated>pnpm dlx supabase db reset 2>&1 | tail -5 && pnpm -r typecheck && pnpm -r lint</automated>
  </verify>
  <done>Migration 0021 applied. All 5 admin routers compile. buildInstructorSignerSnapshot helper compiles. Labels files in place. Banned-term lint clean. Typecheck green.</done>
</task>

<task type="auto">
  <name>Task 2: gradeSheet / flightLog / record / schedule.checkStudentCurrency routers + API integration tests</name>
  <files>
    packages/api/src/routers/gradeSheet.ts,
    packages/api/src/routers/flightLog.ts,
    packages/api/src/routers/record.ts,
    packages/api/src/routers/schedule.ts,
    packages/api/src/routers/index.ts,
    tests/api/phase5-routers.test.ts
  </files>
  <action>
    Create `packages/api/src/routers/gradeSheet.ts` (instructorProcedure):
    - `createFromReservation({ reservationId, lessonId, studentEnrollmentId })` — creates a draft lesson_grade_sheet. Fails if one already exists for this (reservation, lesson) pair unless marked as a corrections sheet (pass optional corrects_grade_sheet_id). Pre-fills line_item_grade rows with null grade_value for every line_item in the lesson (ordered by position).
    - `setGrade({ gradeSheetId, lineItemId, grade_value, grade_remarks })` — draft only
    - `setOverallRemarks({ gradeSheetId, overall_remarks, ground_minutes, flight_minutes })`
    - `seal({ gradeSheetId })` — validates: status='draft', every Required line_item has non-null grade_value, every must_pass line_item has a passing grade (for absolute_ipm: 'PM' or 'M'; for relative_5: >=3; for pass_fail: 'pass'). Then calls buildInstructorSignerSnapshot, sets sealed_at, status='sealed'. The DB seal trigger enforces immutability going forward.
    - `recordTestGrade({ courseComponentKind, componentId, studentEnrollmentId, score, maxScore, testKind, remarks })` — writes to a dedicated `test_grade` table. **If test_grade table does not exist from 05-01 schema, add it via a micro-migration at the top of this plan** — id, school_id, base_id, student_enrollment_id FK, component_kind enum (course|stage|phase|unit|lesson|line_item), component_id uuid, test_kind enum (knowledge|oral|end_of_stage|practical), score numeric, max_score numeric, recorded_at, recorded_by, remarks, signer_snapshot jsonb, sealed bool, audit. (If missing, add migration 0022_phase5_test_grade.sql + supabase mirror.) This covers SYL-25.

    Create `packages/api/src/routers/flightLog.ts`:
    - `categorize({ reservationId, flightLogEntryId, entries: [{ userId, kind, dayMinutes, nightMinutes, crossCountryMinutes, instrumentActualMinutes, instrumentSimulatedMinutes, timeInMakeModel, dayLandings, nightLandings }] })` — writes 1..N flight_log_time rows in a single transaction. Validates the ±6 min tolerance vs paired hobbs delta. Called from the Phase 3 dispatch.closeOut form (wired in 05-04).

    Create `packages/api/src/routers/record.ts` (protectedProcedure — EVERY query MUST scope WHERE user_id = ctx.session.userId):
    - `me()` — returns current user's training record summary: active enrollments + recent grade sheets + pending stage checks + currencies + current endorsements
    - `myFlightLog({ fromDate?, toDate?, cursor? })` — returns flight_log_time rows filtered to caller
    - `myCurrencies()` — personnel_currency rows WHERE user_id=ctx.session.userId AND subject_kind='student'
    - `myCourseProgress({ enrollmentId })` — enrollment details + chronological grade sheets + stage checks + endorsements for this enrollment, computed progress (lessons completed / total)
    - `myFlightLogTotals()` — reads user_flight_log_totals view WHERE user_id=ctx.session.userId

    Extend `packages/api/src/routers/schedule.ts`:
    - Add `checkStudentCurrency({ lessonId, studentUserId })` — reads lesson.required_currencies jsonb array, checks personnel_currency for each kind WHERE user_id=studentUserId AND subject_kind='student' AND (expires_at IS NULL OR expires_at > now()). Returns `{ blockers: Array<{ kind, reason: 'missing'|'expired', expires_at? }> }`. This is the SCH-12 hook.
    - Modify the existing `approve` mutation: IF `reservation.lesson_id` is set AND `reservation.student_id` is set, call `checkStudentCurrency(reservation.lesson_id, reservation.student_id)`. If blockers.length > 0, throw TRPCError PRECONDITION_FAILED with a message listing the blockers (use "currency" language, not "approved"). This is additive — existing Phase 3 approve tests with no lesson_id still pass.

    Register all new routers in `packages/api/src/routers/index.ts`.

    Create `tests/api/phase5-routers.test.ts` — integration tests using the Phase 3-4 API test harness:
    - Admin can fork a system course and publish
    - Published course_version rejects further edits (router returns CONFLICT)
    - Instructor creates draft grade sheet from reservation, sets all Required grades, seals successfully
    - Sealing fails when a Required line_item is ungraded (PRECONDITION_FAILED)
    - Sealed grade sheet rejects setGrade (CONFLICT — seal trigger raises; router maps to CONFLICT)
    - Stage check with checker=primary_instructor rejected
    - Endorsement.issue writes rendered_text snapshot (not a template reference)
    - Student calling record.myFlightLog sees only their own rows (cross-user isolation)
    - schedule.checkStudentCurrency returns blockers for expired medical
    - schedule.approve of a reservation with lesson_id but missing student currency rejects with PRECONDITION_FAILED
    - recordTestGrade persists a row and seals it

  </action>
  <verify>
    <automated>pnpm dlx supabase db reset 2>&1 | tail -5 && pnpm --filter @part61/rls-tests test 2>&1 | tail -40 && pnpm -r typecheck && pnpm -r lint && pnpm --filter ./apps/web build 2>&1 | tail -5</automated>
  </verify>
  <done>All Phase 5 routers compile and pass integration tests. Phase 3 approve regression still green when called without a lesson_id. Banned-term lint clean. Phase 1-4 tests all green.</done>
</task>

</tasks>

<verification>
- typecheck / lint / build green monorepo-wide
- Phase 1-4 regression 151+ tests still green
- New phase5-routers.test.ts all green
</verification>

<success_criteria>

- 9 new routers + 1 helper + 3 label files committed
- adminOrChiefInstructorProcedure available
- SCH-12 wired into Phase 3 schedule.approve (additive)
- Sealing contracts enforced server-side AND by DB triggers
- Student-facing record.\* router scopes every query to ctx.session.userId
  </success_criteria>

<output>
Create `.planning/phases/05-syllabus-model-grading-records/05-03-SUMMARY.md`.
</output>
