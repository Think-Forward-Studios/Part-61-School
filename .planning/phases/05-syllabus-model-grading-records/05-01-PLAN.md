---
phase: 05-syllabus-model-grading-records
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/db/migrations/0014_phase5_currency_rename.sql
  - packages/db/migrations/0015_phase5_currency_kinds.sql
  - packages/db/migrations/0016_phase5_course_tree.sql
  - packages/db/migrations/0017_phase5_grade_stage_endorsement.sql
  - packages/db/migrations/0018_phase5_functions_triggers_views.sql
  - supabase/migrations/20260409000001_phase5_currency_rename.sql
  - supabase/migrations/20260409000002_phase5_currency_kinds.sql
  - supabase/migrations/20260409000003_phase5_course_tree.sql
  - supabase/migrations/20260409000004_phase5_grade_stage_endorsement.sql
  - supabase/migrations/20260409000005_phase5_functions_triggers_views.sql
  - packages/db/src/schema/syllabus.ts
  - packages/db/src/schema/grading.ts
  - packages/db/src/schema/endorsements.ts
  - packages/db/src/schema/personnelCurrency.ts
  - packages/db/src/schema/flightLogTime.ts
  - packages/db/src/schema/index.ts
  - packages/db/src/schema/reservations.ts
  - packages/db/src/schema/enrollment.ts
  - tests/rls/phase5-syllabus.test.ts
autonomous: true
requirements:
  - SYL-01
  - SYL-04
  - SYL-06
  - SYL-13
  - SYL-14
  - SYL-12
must_haves:
  truths:
    - 'Every Phase 5 table (course, course_version, stage, course_phase, unit, lesson, line_item, lesson_grade_sheet, line_item_grade, stage_check, endorsement_template, student_endorsement, flight_log_time, personnel_currency) is isolated per school_id by RLS'
    - 'A published course_version cannot be edited (seal trigger raises exception)'
    - 'A sealed lesson_grade_sheet / stage_check / student_endorsement cannot be UPDATEd'
    - 'clone_course_version(source_id) deep-copies the tree in a single transaction'
    - 'instructor_currency has been renamed to personnel_currency with subject_kind discriminator; Phase 2 queries continue to work via alias or updated references'
    - 'Phase 1-4 cross-tenant tests still pass (151/151 regression)'
  artifacts:
    - path: 'packages/db/migrations/0016_phase5_course_tree.sql'
      provides: '6-level course hierarchy tables + course_version + enrollment FK migration'
      contains: 'create table public.course'
    - path: 'packages/db/migrations/0017_phase5_grade_stage_endorsement.sql'
      provides: 'lesson_grade_sheet, line_item_grade, stage_check, endorsement_template, student_endorsement, flight_log_time tables'
      contains: 'create table public.lesson_grade_sheet'
    - path: 'packages/db/migrations/0018_phase5_functions_triggers_views.sql'
      provides: 'clone_course_version, seal triggers, user_flight_log_totals view, compute_recency_currency function, different-instructor stage_check trigger'
      contains: 'create or replace function public.clone_course_version'
    - path: 'tests/rls/phase5-syllabus.test.ts'
      provides: 'cross-tenant isolation tests for every new Phase 5 table'
      contains: "describe('phase 5 syllabus RLS'"
  key_links:
    - from: 'reservation.lesson_id'
      to: 'lesson.id'
      via: 'FK added in migration 0016'
      pattern: 'lesson_id uuid references public.lesson'
    - from: 'student_course_enrollment.course_version_id'
      to: 'course_version.id'
      via: 'rename of course_descriptor + FK'
      pattern: "course_version_id uuid.*references public\\.course_version"
    - from: 'lesson_grade_sheet'
      to: 'reservation, lesson, student_enrollment'
      via: 'FKs'
      pattern: "references public\\.(reservation|lesson|student_course_enrollment)"
---

<objective>
Land the entire Phase 5 schema foundation: 6-level course tree + versioning, grading tables, stage checks, endorsement catalog + per-issuance rows, flight time categorization, student currency via `personnel_currency` rename+discriminator, all seal/hard-delete/audit triggers, the `clone_course_version` PL/pgSQL helper, the `user_flight_log_totals` view, and cross-tenant RLS tests for every new table. This mirrors the Phase 2/3/4 "one big schema plan" pattern.

Purpose: Every downstream Phase 5 plan (seeds, tRPC, UI, PDFs) depends on this schema existing and being RLS-safe.
Output: 5 hand-authored migrations (0014-0018) mirrored to supabase/migrations, Drizzle schema files, cross-tenant Vitest harness, all Phase 1-4 tests still green (151/151).
</objective>

<execution_context>
@/Users/christopher/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christopher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-syllabus-model-grading-records/05-CONTEXT.md
@.planning/phases/05-syllabus-model-grading-records/05-RESEARCH.md
@CLAUDE.md

<!-- Phase 2/3/4 schema plans established the pattern mirrored here -->

@.planning/phases/02-personnel-admin-fleet/02-01-SUMMARY.md
@.planning/phases/03-scheduling-dispatch-execution/03-01-SUMMARY.md
@.planning/phases/04-camp-maintenance/04-01-SUMMARY.md

<interfaces>
Phase 2-4 established contracts the executor MUST mirror:

- `pgPolicy(name, { to: 'authenticated', ... })` uses STRING literal for `to:`, never `sql\`authenticated\``
- Views that flow RLS through: `WITH (security_invoker = true)`
- Every new safety-relevant table gets: school_id + base_id columns, audit.attach() trigger, fn_block_hard_delete trigger, cross-tenant RLS test
- Signer snapshot JSONB shape (from Phase 4 logbook_entry, reused for grade sheet / stage check / endorsement):
  ```
  { user_id, full_name, certificate_type, certificate_number, signed_at }
  ```
- Seal pattern: `sealed boolean` + `sealed_at timestamptz` + BEFORE UPDATE trigger raises P0001 if sealed=true; false→true transition requires signer_snapshot+sealed_at set.
- Postgres enum-in-transaction caveat: adding enum values and USING them must be in separate migrations (hence 0014 rename, 0015 enum kinds, 0016+ tables).
- `withTenantTx` wraps every write with `SET LOCAL app.school_id` and `SET LOCAL app.base_id` GUCs.
- `aircraft_current_totals` view is queryable and RLS-flows-through (Phase 2).
- Phase 2 `instructor_currency` schema (to be renamed): { id, user_id, kind currency_kind, issued_at, expires_at, warning_days, notes, school_id, ... }
- Phase 2 `student_course_enrollment` currently has `course_descriptor text` — Phase 5 replaces with `course_version_id uuid` FK.
- Phase 3 `reservation` table — add nullable `lesson_id` FK + `student_enrollment_id` FK for grade-sheet wiring.
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Migrations 0014-0016 — currency rename, enum extension, course tree + enrollment FK</name>
  <files>
    packages/db/migrations/0014_phase5_currency_rename.sql,
    packages/db/migrations/0015_phase5_currency_kinds.sql,
    packages/db/migrations/0016_phase5_course_tree.sql,
    supabase/migrations/20260409000001_phase5_currency_rename.sql,
    supabase/migrations/20260409000002_phase5_currency_kinds.sql,
    supabase/migrations/20260409000003_phase5_course_tree.sql,
    packages/db/src/schema/syllabus.ts,
    packages/db/src/schema/personnelCurrency.ts,
    packages/db/src/schema/reservations.ts,
    packages/db/src/schema/enrollment.ts,
    packages/db/src/schema/index.ts
  </files>
  <action>
    Write migration 0014: `ALTER TABLE public.instructor_currency RENAME TO personnel_currency; ALTER TABLE public.personnel_currency ADD COLUMN subject_kind text NOT NULL DEFAULT 'instructor' CHECK (subject_kind IN ('instructor','student'));` plus create a `public.instructor_currency` updatable VIEW aliasing personnel_currency WHERE subject_kind='instructor' so Phase 2 callers keep compiling until their next touch. Update RLS policies to attach to the new table name. Rename index names.

    Write migration 0015: `ALTER TYPE currency_kind ADD VALUE IF NOT EXISTS 'medical_class_1'` etc. for every new student kind: medical_class_1, medical_class_2, medical_class_3, basicmed, flight_review, ipc, solo_endorsement_scope, day_passenger_currency, night_passenger_currency, instrument_currency, tailwheel_currency, high_performance_currency, complex_currency. Separate migration per enum-in-transaction caveat. Insert defaults into currency_kind_config (medical 30d, BFR 60d, IPC 60d, solo 90d, night 30d).

    Write migration 0016: create the 6 course tree tables per CONTEXT decisions. Exact tables with columns: `course`, `course_version`, `stage`, `course_phase` (optional), `unit` (optional — FK to course_phase_id OR stage_id exclusive via CHECK), `lesson` (FK to unit OR course_phase OR stage exclusive), `line_item`. Every table: school_id (nullable on `course` to allow system templates with school_id=null), base_id (where relevant — most are school-wide, base_id nullable), deleted_at, audit columns, pgPolicy RLS for authenticated (school_id match OR school_id IS NULL for templates), audit.attach, fn_block_hard_delete attach. Add `lesson.required_currencies jsonb` column. Add `line_item.classification` enum (`required | optional | must_pass`), `line_item.grading_scale_override` enum nullable. Add `course_version.grading_scale` enum (`absolute_ipm | relative_5 | pass_fail`), `course_version.published_at`, `course_version.published_by`, `course_version.min_levels int`, `course_version.superseded_at`.

    In the SAME migration 0016 (after the tables exist): `ALTER TABLE public.student_course_enrollment ADD COLUMN course_version_id uuid REFERENCES public.course_version(id); ALTER TABLE public.student_course_enrollment DROP COLUMN course_descriptor;` (Phase 2 enrollment had no FK yet; any existing rows in dev can be cleared as part of migration since Phase 2 ships 0 enrollments). ALSO: `ALTER TABLE public.reservation ADD COLUMN lesson_id uuid REFERENCES public.lesson(id), ADD COLUMN student_enrollment_id uuid REFERENCES public.student_course_enrollment(id);`.

    Mirror all 3 migrations to `supabase/migrations/2026040900000{1,2,3}_*.sql` byte-identical.

    Create `packages/db/src/schema/syllabus.ts` with Drizzle tables matching migration 0016 exactly. Add pgPolicy RLS definitions (to:'authenticated' string literal). Create `personnelCurrency.ts` renaming from instructor currency. Update `reservations.ts` to add lesson_id + student_enrollment_id columns. Update `enrollment.ts` to replace course_descriptor with course_version_id FK. Export everything from `index.ts`.

  </action>
  <verify>
    <automated>pnpm --filter ./packages/db exec drizzle-kit check && pnpm dlx supabase db reset 2>&1 | tail -30 && pnpm --filter ./apps/web typecheck && pnpm -r lint</automated>
  </verify>
  <done>Migrations 0014-0016 applied cleanly on supabase db reset. Drizzle schema typechecks. Banned-term lint passes. personnel_currency table visible in Supabase with subject_kind column.</done>
</task>

<task type="auto">
  <name>Task 2: Migrations 0017-0018 — grading/stage/endorsement/flight_log_time + functions/triggers/views + cross-tenant RLS tests</name>
  <files>
    packages/db/migrations/0017_phase5_grade_stage_endorsement.sql,
    packages/db/migrations/0018_phase5_functions_triggers_views.sql,
    supabase/migrations/20260409000004_phase5_grade_stage_endorsement.sql,
    supabase/migrations/20260409000005_phase5_functions_triggers_views.sql,
    packages/db/src/schema/grading.ts,
    packages/db/src/schema/endorsements.ts,
    packages/db/src/schema/flightLogTime.ts,
    packages/db/src/schema/index.ts,
    tests/rls/phase5-syllabus.test.ts
  </files>
  <action>
    Write migration 0017: create tables per CONTEXT:
    - `lesson_grade_sheet` (id, school_id, base_id, reservation_id nullable FK, student_enrollment_id FK, lesson_id FK, conducted_at, conducted_by_user_id, ground_minutes, flight_minutes, overall_remarks, status enum draft/signed/sealed, signer_snapshot jsonb, sealed_at, corrects_grade_sheet_id nullable self-FK, deleted_at, audit cols)
    - `line_item_grade` (id, grade_sheet_id FK, line_item_id FK, grade_value text, grade_remarks, position, created_at)
    - `stage_check` (id, school_id, base_id, student_enrollment_id FK, stage_id FK, checker_user_id FK, scheduled_at, conducted_at, status enum scheduled/passed/failed, remarks, signer_snapshot jsonb, sealed_at, deleted_at, audit)
    - `endorsement_template` (id, code, title, body_template, category enum student_pilot/solo/xc/aircraft_class_rating/flight_review/ipc/practical_test/knowledge_test/other, ac_reference text, deleted_at)
    - `student_endorsement` (id, school_id, base_id, student_user_id FK, template_id nullable FK, rendered_text, issued_at, issued_by_user_id, signer_snapshot jsonb, expires_at nullable, aircraft_context nullable, notes, sealed bool, sealed_at, revoked_at nullable, deleted_at, audit)
    - `flight_log_time` (id, reservation_id FK, flight_log_entry_id FK, user_id FK, kind enum dual_received/dual_given/pic/sic/solo, day_minutes, night_minutes, cross_country_minutes, instrument_actual_minutes, instrument_simulated_minutes, time_in_make_model text, day_landings int, night_landings int, notes, audit; CHECK (day_minutes+night_minutes) within ±6 tolerance of paired hobbs delta)

    Every table gets: pgPolicy select/insert/update/delete `to: 'authenticated'` scoped by school_id, audit.attach, fn_block_hard_delete attach (except endorsement_template which is reference data).

    Write migration 0018 (functions/triggers/views — in a SEPARATE file because it USES tables from 0017):
    - `create or replace function public.clone_course_version(source_version_id uuid, new_school_id uuid) returns uuid` — PL/pgSQL, deep-copies course_version → stages → phases → units → lessons → line_items in one transaction, returns new course_version_id. Sets published_at=null (draft).
    - Seal trigger function `public.fn_syllabus_seal_guard()` — BEFORE UPDATE on lesson_grade_sheet / stage_check / student_endorsement / course_version: if OLD.sealed_at IS NOT NULL (or OLD.published_at for course_version) raise P0001 'row is sealed and immutable'. Transition false→true must supply signer_snapshot + sealed_at. Attach trigger to all 4 tables.
    - `public.fn_stage_check_different_instructor()` BEFORE INSERT/UPDATE on stage_check: look up student_enrollment.primary_instructor_id and RAISE if checker_user_id matches.
    - `public.compute_recency_currency(user_id uuid, kind currency_kind) returns table(last_qualifying_event timestamptz, expires_at timestamptz)` — reads flight_log_time to compute night/day/instrument recency. Return empty result for non-recency kinds.
    - `create or replace view public.user_flight_log_totals with (security_invoker = true) as select user_id, sum(day_minutes+night_minutes) as total_minutes, sum(...) as pic_minutes, sum(...) as dual_received, sum(...) as solo, sum(...) as cross_country, sum(...) as night, sum(...) as instrument_actual, sum(...) as instrument_simulated, sum(day_landings), sum(night_landings) from public.flight_log_time group by user_id;`

    Mirror both to supabase/migrations.

    Create Drizzle schemas `grading.ts`, `endorsements.ts`, `flightLogTime.ts`. Export from index.

    Create `tests/rls/phase5-syllabus.test.ts` — extend the Phase 1-4 cross-tenant pattern. For each new table in 0016/0017: seed two schools, set request.jwt.claims to school A, insert a row, switch to school B, confirm SELECT returns 0 rows and INSERT with school A's school_id fails. Also test: (a) clone_course_version produces a deep copy with new ids, (b) stage_check with checker_user_id = primary_instructor_id is rejected, (c) sealed lesson_grade_sheet cannot be UPDATEd, (d) published course_version cannot be edited.

  </action>
  <verify>
    <automated>pnpm dlx supabase db reset 2>&1 | tail -20 && pnpm --filter @part61/rls-tests test 2>&1 | tail -40 && pnpm -r typecheck && pnpm -r lint && pnpm --filter ./apps/web build 2>&1 | tail -5</automated>
  </verify>
  <done>supabase db reset applies all 5 migrations cleanly. phase5-syllabus.test.ts passes. Phase 1-4 regression 151/151 still green (new total ≥ 151 + Phase 5 additions). Monorepo typecheck/lint/build green.</done>
</task>

</tasks>

<verification>
- `pnpm dlx supabase db reset` clean
- `pnpm --filter @part61/rls-tests test` — all prior 151 tests + new Phase 5 tests pass
- `pnpm -r typecheck` green across 6 workspaces
- `pnpm -r lint` green (banned-term clean)
- `pnpm --filter ./apps/web build` green
</verification>

<success_criteria>

- 10 migration files written (5 in packages/db/migrations, 5 in supabase/migrations byte-identical)
- 5 new Drizzle schema files + reservations/enrollment updates + index exports
- All Phase 5 tables have RLS policies and pass cross-tenant tests
- clone_course_version, seal trigger, different-instructor trigger, compute_recency_currency, user_flight_log_totals view all exist in the database
- Phase 1-4 regression: zero test failures
  </success_criteria>

<output>
Create `.planning/phases/05-syllabus-model-grading-records/05-01-SUMMARY.md` following the template.
</output>
