---
phase: 05-syllabus-model-grading-records
plan: 04
type: execute
wave: 3
depends_on: ['05-02', '05-03']
files_modified:
  - apps/web/app/(app)/admin/courses/page.tsx
  - apps/web/app/(app)/admin/courses/[id]/page.tsx
  - apps/web/app/(app)/admin/courses/[id]/ForkCourseButton.tsx
  - apps/web/app/(app)/admin/courses/[id]/versions/[versionId]/page.tsx
  - apps/web/app/(app)/admin/courses/[id]/versions/[versionId]/VersionTreeEditor.tsx
  - apps/web/app/(app)/admin/courses/[id]/versions/[versionId]/PublishVersionButton.tsx
  - apps/web/app/(app)/admin/courses/[id]/versions/[versionId]/lessons/[lessonId]/page.tsx
  - apps/web/app/(app)/admin/courses/[id]/versions/[versionId]/lessons/[lessonId]/LessonEditor.tsx
  - apps/web/app/(app)/admin/enrollments/page.tsx
  - apps/web/app/(app)/admin/enrollments/EnrollStudentForm.tsx
  - apps/web/app/(app)/admin/stage-checks/page.tsx
  - apps/web/app/(app)/admin/stage-checks/[id]/page.tsx
  - apps/web/app/(app)/admin/stage-checks/[id]/RecordStageCheck.tsx
  - apps/web/app/(app)/admin/endorsements/page.tsx
  - apps/web/app/(app)/admin/endorsements/IssueEndorsementForm.tsx
  - apps/web/app/(app)/admin/students/[id]/StudentCurrenciesPanel.tsx
  - apps/web/app/(app)/admin/students/[id]/record/page.tsx
  - apps/web/app/(app)/dispatch/close/[id]/LessonPickerSection.tsx
  - apps/web/app/(app)/dispatch/close/[id]/GradeSheetEditor.tsx
  - apps/web/app/(app)/dispatch/close/[id]/FlightTimeCategorization.tsx
  - apps/web/app/(app)/dispatch/close/[id]/page.tsx
autonomous: true
requirements:
  - SYL-02
  - SYL-03
  - SYL-05
  - SYL-07
  - SYL-08
  - SYL-09
  - SYL-12
  - SYL-13
  - SYL-14
  - SYL-25
  - STU-03
must_haves:
  truths:
    - 'Admin can browse system courses + school courses at /admin/courses'
    - 'Admin can fork a system course into a school draft and edit its tree'
    - 'Chief instructor can publish a course_version; published versions are visibly locked (edit disabled)'
    - "Instructor at /dispatch/close/[id] sees a lesson picker filtered to the student's enrolled course_version, picks a lesson, grades line items inline, categorizes flight time, and seals the sheet"
    - 'Admin can schedule a stage check and record pass/fail with different-instructor enforcement'
    - 'Admin can issue an endorsement from the AC 61-65 template catalog with rendered text preview before signing'
    - "StudentCurrenciesPanel on the student profile reuses the Phase 2 CurrenciesPanel pattern with subjectKind='student'"
    - 'Every new .tsx file is banned-term-clean (displays use gradingLabels.ts, lessonKindLabels.ts, endorsementCategoryLabels.ts)'
  artifacts:
    - path: 'apps/web/app/(app)/admin/courses/page.tsx'
      provides: 'course catalog listing system + school courses'
    - path: 'apps/web/app/(app)/admin/courses/[id]/versions/[versionId]/VersionTreeEditor.tsx'
      provides: 'expandable tree editor with inline edit when draft, locked when published'
    - path: 'apps/web/app/(app)/dispatch/close/[id]/GradeSheetEditor.tsx'
      provides: 'inline per-line-item grading UI with objectives/completion standards visible'
    - path: 'apps/web/app/(app)/dispatch/close/[id]/FlightTimeCategorization.tsx'
      provides: '61.51(e) bucket entry form integrated into close-out'
    - path: 'apps/web/app/(app)/admin/students/[id]/StudentCurrenciesPanel.tsx'
      provides: 'student currency tracking UI mirroring Phase 2 instructor panel'
  key_links:
    - from: 'dispatch/close/[id]/page.tsx'
      to: 'gradeSheet + flightLog routers'
      via: 'trpc react hooks'
      pattern: "trpc\\.gradeSheet|trpc\\.flightLog"
    - from: 'admin/courses/[id]/ForkCourseButton.tsx'
      to: 'admin.courses.fork mutation'
      via: 'trpc mutation'
      pattern: "admin\\.courses\\.fork"
    - from: 'admin/endorsements/IssueEndorsementForm.tsx'
      to: 'admin.endorsements.issue'
      via: 'rendered-text preview before sign'
      pattern: "admin\\.endorsements\\.issue"
---

<objective>
Ship the entire admin-facing Phase 5 UI: course catalog + version tree editor + lesson editor, enrollment management, stage-check workflow, endorsement issuance with rendered-text preview, student currency panel, admin training record view. Replace the Phase 3 stub "Grade lesson" button on `/dispatch/close/[id]` with a real lesson picker + inline grade sheet + 61.51(e) flight-time categorization form.

Purpose: Without UI, the routers from 05-03 are unreachable. This plan closes the admin-side of SYL-02, 03, 05, 07, 08, 09, 12, 13, 14, 25 plus STU-03's categorization data entry.
Output: ~21 new .tsx files + modified dispatch close-out page. 45 routes → ~55+ routes after this plan.
</objective>

<execution_context>
@/Users/christopher/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christopher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-syllabus-model-grading-records/05-CONTEXT.md
@.planning/phases/05-syllabus-model-grading-records/05-03-PLAN.md
@.planning/phases/04-camp-maintenance/04-04-SUMMARY.md
@.planning/phases/03-scheduling-dispatch-execution/03-04-SUMMARY.md

<interfaces>
From 05-03 (router surface):
- `trpc.admin.courses.list / get / getVersion / fork / createVersion / publish / createStage / createLesson / createLineItem / updateLesson / updateLineItem`
- `trpc.admin.enrollments.list / enroll / migrate / complete / withdraw`
- `trpc.admin.stageChecks.list / schedule / record`
- `trpc.admin.endorsements.listTemplates / listStudentEndorsements / issue / revoke`
- `trpc.admin.studentCurrencies.list / record / update / delete`
- `trpc.gradeSheet.createFromReservation / setGrade / setOverallRemarks / seal / recordTestGrade`
- `trpc.flightLog.categorize`

Label files (banned-term dodges):

- `gradingLabels.ts` — grading scale + grade value display strings
- `lessonKindLabels.ts`
- `endorsementCategoryLabels.ts`

Established UI patterns from Phase 2-4:

- Server components fetch via direct `db` + `sql` (no trpcServer caller)
- Client islands use `trpc` react hooks for mutations
- Route group `(app)` applies the protected layout
- Ceremonial sign actions: red border, "this is legally binding" language, explicit confirm checkbox
- Phase 2 `CurrenciesPanel` at `apps/web/app/(app)/admin/people/[id]/CurrenciesPanel.tsx` — COPY with subjectKind='student' prop, filters personnel_currency.subject_kind='student'
- Phase 3 `/dispatch/close/[id]/page.tsx` has a stub "Grade lesson" button — REPLACE with the new lesson picker + grade sheet editor + flight time categorization

Package filter: `pnpm --filter ./apps/web` (workspace name is `web`, NOT @part61/web).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Course catalog + version tree editor + lesson editor + enrollments + stage checks + endorsements + StudentCurrenciesPanel + admin student record view</name>
  <files>
    apps/web/app/(app)/admin/courses/page.tsx,
    apps/web/app/(app)/admin/courses/[id]/page.tsx,
    apps/web/app/(app)/admin/courses/[id]/ForkCourseButton.tsx,
    apps/web/app/(app)/admin/courses/[id]/versions/[versionId]/page.tsx,
    apps/web/app/(app)/admin/courses/[id]/versions/[versionId]/VersionTreeEditor.tsx,
    apps/web/app/(app)/admin/courses/[id]/versions/[versionId]/PublishVersionButton.tsx,
    apps/web/app/(app)/admin/courses/[id]/versions/[versionId]/lessons/[lessonId]/page.tsx,
    apps/web/app/(app)/admin/courses/[id]/versions/[versionId]/lessons/[lessonId]/LessonEditor.tsx,
    apps/web/app/(app)/admin/enrollments/page.tsx,
    apps/web/app/(app)/admin/enrollments/EnrollStudentForm.tsx,
    apps/web/app/(app)/admin/stage-checks/page.tsx,
    apps/web/app/(app)/admin/stage-checks/[id]/page.tsx,
    apps/web/app/(app)/admin/stage-checks/[id]/RecordStageCheck.tsx,
    apps/web/app/(app)/admin/endorsements/page.tsx,
    apps/web/app/(app)/admin/endorsements/IssueEndorsementForm.tsx,
    apps/web/app/(app)/admin/students/[id]/StudentCurrenciesPanel.tsx,
    apps/web/app/(app)/admin/students/[id]/record/page.tsx
  </files>
  <action>
    `/admin/courses/page.tsx` — Server component. Fetches via drizzle: system courses (school_id IS NULL) + school courses. Two-section table view. Each row links to `/admin/courses/[id]`.

    `/admin/courses/[id]/page.tsx` — Server component showing course details (code, title, rating_sought, description) + course_version list (draft / published). "Fork this course" client button on system templates. "New version" for owned courses (adminOrChiefInstructor only).

    `ForkCourseButton.tsx` — Client component. Modal asking for new code + title. Calls `trpc.admin.courses.fork.useMutation()`. On success, router.push to new version edit page.

    `/versions/[versionId]/page.tsx` — Server component. Fetches full tree via `trpc.admin.courses.getVersion` (server-side call pattern from Phase 2-4 — use direct db). Shows tree via `VersionTreeEditor` client island. Shows `PublishVersionButton` (disabled if already published).

    `VersionTreeEditor.tsx` — Client component. Renders Stage→(CoursePhase)→(Unit)→Lesson→LineItem as expandable tree. Inline add/edit/delete via trpc mutations when `published_at is null`. When published, the tree is read-only with a lock icon. Use native `<details>`/`<summary>` for expand/collapse to avoid adding a library. Each lesson row links to `./lessons/[lessonId]`.

    `PublishVersionButton.tsx` — Client. Confirm modal: "Publishing locks this version. Students can be enrolled. You cannot edit after publishing." Calls `trpc.admin.courses.publish`. Displays `gradingLabels` + `lessonKindLabels` from the domain package (banned-term safe).

    `/lessons/[lessonId]/page.tsx` — Server component fetching lesson + line_items.
    `LessonEditor.tsx` — Client component. Edit lesson fields (code, title, kind using lessonKindLabels, objectives markdown, completion_standards markdown, min_hours, required_resources jsonb, required_currencies jsonb). List of line_items with inline add/edit (position, code, title, description, objectives, completion_standards, classification enum, grading_scale_override). Draft-only edits.

    `/admin/enrollments/page.tsx` — Server component listing enrollments for the active school, joined with student name + course_version label + primary instructor. Filter by status.
    `EnrollStudentForm.tsx` — Client. Pick student + course_version (published only) + primary_instructor. Calls `trpc.admin.enrollments.enroll`.

    `/admin/stage-checks/page.tsx` — Server component. Two sections: scheduled (future) + completed (recent). Links to detail.
    `/admin/stage-checks/[id]/page.tsx` — Server component showing stage check + student + stage + checker.
    `RecordStageCheck.tsx` — Client component. Pass/fail radio + remarks textarea + ceremonial "Sign and record stage check" button (red border, "this is legally binding" language, explicit confirm). Calls `trpc.admin.stageChecks.record`.

    `/admin/endorsements/page.tsx` — Server component. Two sections: "Issue endorsement" + "Recently issued". Lists endorsement_template catalog (grouped by category using endorsementCategoryLabels). Each has an "Issue" button that opens `IssueEndorsementForm`.
    `IssueEndorsementForm.tsx` — Client. Fields: student picker, template picker (or "Custom" for free-form), **live rendered-text preview** (template body with placeholders substituted from student profile + current instructor profile + today's date), optional expires_at, optional aircraft_context, notes. Ceremonial "Sign and issue" button. Calls `trpc.admin.endorsements.issue` passing the rendered_text (which the server validates/re-renders as defense-in-depth). No banned terms in any string literal — all FAA text flows through template data, not code.

    `StudentCurrenciesPanel.tsx` under `/admin/students/[id]/` — COPY of Phase 2 `CurrenciesPanel.tsx` with `subjectKind='student'` prop and using `trpc.admin.studentCurrencies.*`. Shows current + expired + upcoming, each with status chip (green/yellow/red). Add/edit/delete actions.

    `/admin/students/[id]/record/page.tsx` — Server component admin view of any student's training record (read-mostly — can add stage check / endorsement / currency but not edit sealed history). Shows: enrollment list, chronological grade sheets (sealed only), stage checks, endorsements (active + expired), currencies, flight log totals from `user_flight_log_totals` view. "Download 141.101 PDF" button placeholder (wired in 05-05). "Download IACRA" placeholder.

    Banned-term lint: every .tsx file uses "training record", "sign and record", "issue", "current", "published", "confirmed" — never "approved" or "Part 141". FAA citations like "14 CFR 141.101" and "AC 61-65K" are fine (citations, not banned literal terms — verify with lint).

  </action>
  <verify>
    <automated>pnpm --filter ./apps/web typecheck && pnpm --filter ./apps/web lint && pnpm --filter ./apps/web build 2>&1 | tail -15</automated>
  </verify>
  <done>All 17 new .tsx files compile. Banned-term lint clean. Build green with the new admin routes visible in the route list.</done>
</task>

<task type="auto">
  <name>Task 2: Replace dispatch close-out "Grade lesson" stub with real lesson picker + grade sheet editor + flight time categorization</name>
  <files>
    apps/web/app/(app)/dispatch/close/[id]/LessonPickerSection.tsx,
    apps/web/app/(app)/dispatch/close/[id]/GradeSheetEditor.tsx,
    apps/web/app/(app)/dispatch/close/[id]/FlightTimeCategorization.tsx,
    apps/web/app/(app)/dispatch/close/[id]/page.tsx
  </files>
  <action>
    Read the existing `apps/web/app/(app)/dispatch/close/[id]/page.tsx` to find the stubbed "Grade lesson" button (Phase 3-04 placeholder). Replace with:

    `LessonPickerSection.tsx` — Client component. Props: `reservationId`, `studentId`, `studentEnrollmentId`. Calls `trpc.admin.enrollments.list({ studentId })` to find the student's active enrollment. Then calls `trpc.admin.courses.getVersion(enrollment.course_version_id)` (need to expose this query as protected, not adminOrChief, since instructors need it — if 05-03 gated it admin-only, add a lightweight `trpc.courses.getVersionForInstructor` mirror or widen the procedure). Renders a searchable combobox of lessons from that version. On pick, calls `trpc.gradeSheet.createFromReservation` and hands the resulting grade_sheet_id to `GradeSheetEditor`. Supports "Add another lesson" button that creates a second grade sheet on the same reservation.

    `GradeSheetEditor.tsx` — Client component. Props: `gradeSheetId`. Fetches the sheet + line_item_grades + the underlying lesson's objectives/completion_standards. Renders each line_item as a card with:
      - Line item title + classification chip (Required / Optional / Must Pass — use gradingLabels.classificationLabels)
      - Objectives markdown + Completion standards markdown visible (not in a tooltip)
      - Grade selector (chip buttons for absolute_ipm / number picker for relative_5 / pass-fail toggle — driven by the course_version.grading_scale from props)
      - Remarks textarea
      - Auto-save every ~10s via debounced trpc.gradeSheet.setGrade call
    Bottom of the editor: overall_remarks + ground_minutes + flight_minutes inputs (calls setOverallRemarks). Big red ceremonial "Sign and seal grade sheet" button that opens confirm modal ("This is legally binding, you cannot edit after sealing") and calls `trpc.gradeSheet.seal`. On success, the sheet renders read-only with a lock icon.

    `FlightTimeCategorization.tsx` — Client component. Props: `reservationId`, `flightLogEntryId`, `studentId`, `instructorId`, `hobbsDeltaMinutes`. Renders a form to split the flight hobbs time into 61.51(e) buckets:
      - For the student row: kind dropdown (dual_received default), day_minutes + night_minutes (must sum within ±6 of hobbsDeltaMinutes, live validation), XC toggle + cross_country_minutes, instrument_actual_minutes, instrument_simulated_minutes, time_in_make_model (pre-filled from aircraft), day_landings, night_landings, notes
      - For the instructor row: kind dropdown (dual_given PIC default), similar fields
      - "Save flight time categorization" button calls `trpc.flightLog.categorize` with both rows in one mutation
    Smart defaults: if reservation.time_range straddles civil twilight, split; else all-day. XC toggle defaults from reservation.route_string presence. Should feel like one-click if defaults are right.

    Update `/dispatch/close/[id]/page.tsx` — server component. After the existing Hobbs/tach/squawks form: render `<LessonPickerSection />` and `<FlightTimeCategorization />` side by side (or stacked on mobile). If no student or no enrollment, show a "no lesson available — this reservation has no enrolled student" info card instead of the lesson picker. Remove the stub "Grade lesson" button. Keep the Phase 3 close-out Submit button unchanged.

    Confirm Phase 3 close-out tests still pass (they don't test grading, so they should).

    Banned-term lint: every new .tsx uses "sign and seal", "sealed", "draft", "confirmed", "flight time categorization". No banned terms. Grade display uses gradingLabels.

  </action>
  <verify>
    <automated>pnpm --filter ./apps/web typecheck && pnpm --filter ./apps/web lint && pnpm --filter ./apps/web build 2>&1 | tail -15 && pnpm --filter @part61/rls-tests test 2>&1 | tail -10</automated>
  </verify>
  <done>Close-out page shows lesson picker + grade sheet editor + flight time categorization. Phase 3 close-out regression tests still pass. Banned-term lint clean.</done>
</task>

</tasks>

<verification>
- `pnpm --filter ./apps/web build` green with 50+ routes
- banned-term lint clean across all new .tsx files
- Phase 1-4 regression tests green
</verification>

<success_criteria>

- All 21 admin + close-out UI files created
- Phase 3 stub "Grade lesson" button replaced with real grading flow
- StudentCurrenciesPanel wired
- Admin student record page placeholder-links to PDF routes (wired in 05-05)
  </success_criteria>

<output>
Create `.planning/phases/05-syllabus-model-grading-records/05-04-SUMMARY.md`.
</output>
