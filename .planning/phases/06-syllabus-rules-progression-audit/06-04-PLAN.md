---
phase: 06-syllabus-rules-progression-audit
plan: 04
type: execute
wave: 4
depends_on: ['06-03']
files_modified:
  - apps/web/src/app/record/page.tsx
  - apps/web/src/app/record/_components/StudentNextActivityChip.tsx
  - apps/web/src/app/record/_components/StudentMinimumsPanel.tsx
  - apps/web/src/app/record/_components/StudentProgressForecastPanel.tsx
  - apps/web/src/app/record/_components/StudentRolloverQueuePanel.tsx
autonomous: false
requirements: [SYL-15, SYL-21, SYL-22, SYL-23, SCH-14]
must_haves:
  truths:
    - 'Student at /record sees the next suggested activity, their course minimums tracker, ahead/behind forecast, and any outstanding rollover line items'
    - "Student cannot view any other student's progress data (enforced by record.* procedures' ctx.user scoping)"
    - 'Human verifier confirms all 14 Phase 6 requirements behave end-to-end'
  artifacts:
    - path: 'apps/web/src/app/record/page.tsx'
      provides: 'Extended student record dashboard with four Phase 6 components'
    - path: 'apps/web/src/app/record/_components/StudentNextActivityChip.tsx'
      provides: 'Student-facing SCH-14 surface'
  key_links:
    - from: '/record page'
      to: 'record.getMyProgressForecast + record.getMyMinimumsStatus + schedule.suggestNextActivity'
      via: 'trpc hooks bound to ctx.user enrollment'
      pattern: 'getMyProgressForecast'
---

<objective>
Extend the student-facing /record dashboard with the four Phase 6 surfaces (NextActivityChip, MinimumsStatusPanel, ProgressForecastPanel, RolloverQueuePanel) and close the phase with a single human-verify checkpoint covering all 14 requirements.

Purpose: students need to see what's next and how they're doing; chief instructor needs to walk through the full phase 6 experience once before Phase 7.

Output: student-facing components mirroring the admin panels but scoped to ctx.user's active enrollment(s), plus a comprehensive end-of-phase human verification checkpoint.
</objective>

<execution_context>
@/Users/christopher/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christopher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/06-syllabus-rules-progression-audit/06-CONTEXT.md
@.planning/phases/06-syllabus-rules-progression-audit/06-03-SUMMARY.md
@.planning/phases/05-syllabus-model-grading-records/05-05-SUMMARY.md
@apps/web/src/app/record/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build student-facing Phase 6 record components and wire into /record dashboard</name>
  <files>
    apps/web/src/app/record/page.tsx,
    apps/web/src/app/record/_components/StudentNextActivityChip.tsx,
    apps/web/src/app/record/_components/StudentMinimumsPanel.tsx,
    apps/web/src/app/record/_components/StudentProgressForecastPanel.tsx,
    apps/web/src/app/record/_components/StudentRolloverQueuePanel.tsx
  </files>
  <action>
Build four student-facing components that mirror their admin twins from plan 06-03 but use the student-scoped trpc procedures (`record.getMyMinimumsStatus`, `record.getMyProgressForecast`, `schedule.suggestNextActivity` with the student's own active enrollment id resolved server-side).

Key behavioral differences from admin versions:

- **StudentNextActivityChip**: the "Schedule this lesson" button routes to `/schedule/request?lessonId=...` pre-filling the student field (ctx.user) server-side. No student-picker.
- **StudentProgressForecastPanel**: encouragement-first copy. "Behind by 1 week" renders in neutral (not amber). Only ">2 weeks behind" goes amber. Never red for students — use supportive language: "Let's add a lesson this week to get back on track".
- **StudentMinimumsPanel**: same progress bars as admin, but no refresh button (students don't need it).
- **StudentRolloverQueuePanel**: displayed as "Items to re-attempt in your next lesson (N)". Encouragement-first copy.

Wire into `/record/page.tsx` at the top of the page, above the existing Phase 5 flight log + training record sections:

```
<StudentNextActivityChip />
<div className="grid md:grid-cols-2 gap-4">
  <StudentProgressForecastPanel />
  <StudentMinimumsPanel />
</div>
<StudentRolloverQueuePanel />
```

If the student has no active enrollment, render a friendly empty state: "You are not currently enrolled in a course. Contact your chief instructor to enroll."

If the student has multiple active enrollments, render the four components scoped to the MOST-RECENT active enrollment only (ordered by `enrolled_at DESC, completed_at IS NULL FIRST`). DO NOT build a tab group — multi-enrollment progress view is explicitly deferred to v2 per 06-CONTEXT.md Deferred Ideas. Render a small informational note above the components:

```
You have {N} active enrollments. Showing: {course title}.
```

No switcher. v1 ships a single-view progress surface.

Banned-term discipline unchanged — use "authorized" / "granted" / "chief instructor granted".

Atomic commits:

1. `feat(06-04): student-facing StudentNextActivityChip + StudentMinimumsPanel`
2. `feat(06-04): StudentProgressForecastPanel + StudentRolloverQueuePanel`
3. `feat(06-04): wire four components into /record dashboard (most-recent enrollment only)`
   </action>
   <verify>
   <automated>pnpm --filter web typecheck && pnpm --filter web lint && pnpm --filter web test && pnpm --filter web build && pnpm --filter @part61/api test && pnpm --filter @part61/db test</automated>
   </verify>
   <done>Student dashboard renders all four components scoped to the most-recent active enrollment with an informational note when the student has more than one. Empty state friendly. Banned-term lint clean. Full monorepo test suite (187 Phase 1-5 + new Phase 6 tests) green.</done>
   </task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-of-phase human verification covering all 14 Phase 6 requirements</name>
  <what-built>
Phase 6 complete: 11 migrations + SQL rules engine + pg_cron nightly audit + tRPC wrappers + extended schedule.approve and gradeSheet.createFromReservation + admin UI (audit dashboard, overrides page, 4 student/enrollment panels, dashboard IPF-06 panel, inline blocker list + grant-override dialog on /schedule/request) + student /record dashboard extensions.
  </what-built>
  <how-to-verify>
Start the dev server (`. scripts/dev-env.sh && pnpm --filter web dev`) and walk through each requirement. Use the seeded PPL course and a seeded student from Phase 5 seeds. If seeds lack a suitable test scenario, create one via `/admin/people` and `/admin/enrollments` first.

**SYL-15 (rollover):**

1. As instructor, grade a sealed lesson with a failing Required line item for an enrolled student.
2. Create a new grade sheet for the student's next lesson.
3. Confirm the failed line item appears in the new grade sheet with a "Re-attempt" / "Rolled forward from Lesson X" badge.
4. Grade it passing, seal the sheet. Create a further grade sheet — the line item no longer rolls over.

**SYL-16 (prerequisites):**

1. Define a lesson with `prerequisite_lesson_ids` pointing to an unsealed prior lesson.
2. As student, try to request a reservation for that lesson. Verify the /schedule/request blocker list shows the prerequisite blocker.
3. As admin, try schedule.approve — it should fail with PRECONDITION_FAILED (visible via toast).

**SYL-17 + IPF-06 (override + IPF-06 panel):**

1. With the prereq blocker still present, as chief instructor open the grant-override dialog.
2. Fill justification (min 20 chars), confirm signer snapshot, submit.
3. Re-approve the reservation — it now succeeds (override_active path).
4. Open /admin/overrides — the override appears in the list.
5. Open /admin (dashboard) — the ManagementOverridesPanel shows the recent override.

**SYL-18/SYL-19/SCH-11 (rules engine + instructor quals):**

1. Add `required_instructor_qualifications: ["CFII"]` to a lesson via seed-data update or admin UI if available.
2. Assign a non-CFII instructor to the reservation. Verify the blocker list shows "Instructor missing qualification: CFII".

**SYL-20 (authorized repeats):**

1. Set `lesson.max_repeats = 2` on a lesson. Create two sealed grade sheets for the same enrollment+lesson.
2. Attempt a third — should surface the repeat_limit blocker and offer override (kind: repeat_limit_exceeded).

**SYL-21 (course minimums):**

1. Open /admin/enrollments/[id] — MinimumsStatusPanel shows FAA §61 category bars populated from course_version.minimum_hours.
2. Log additional flight time via flight close-out, refresh — the actual values advance.

**SYL-22 + SYL-23 (ahead/behind + projected dates):**

1. Same enrollment — ProgressForecastPanel shows "Ahead by X weeks" or "Behind by X weeks" chip and projected checkride + completion dates.
2. Change the enrollment's plan_cadence_hours_per_week via admin UI — the forecast updates (trigger refreshes cache).

**SYL-24 (nightly audit):**

1. Manually run the audit via /admin/audit/training-records "Run audit now" button.
2. Verify exceptions appear in the table for any intentionally-seeded gaps (e.g. hours deficit).
3. Mark one resolved — it disappears from the open list.
4. (Optional) Verify via `psql`: `select * from cron.job where jobname = 'phase6_nightly_training_record_audit'`.

**SCH-05 (student currency/prereq block):**

1. Expire a student's medical via admin UI. Try to approve a reservation for a lesson requiring medical. Blocker should cite student_qualifications.

**SCH-14 (next-activity suggestion):**

1. Open the student's /record page — StudentNextActivityChip shows the next lesson with reasoning.
2. Open /admin/people/[id] — NextActivityChip shows the same suggestion with "Schedule this lesson" button that deep-links to /schedule/request pre-filled.

**Regression checks:**

1. Approve a reservation WITHOUT a lesson_id (misc/sim activity) — must still work unchanged.
2. Create a grade sheet for a lesson with no failing prior sheets — behaves exactly like Phase 5 (no rollover rows).
3. Run `pnpm --filter @part61/db test && pnpm --filter @part61/api test && pnpm --filter web test` — all 187+ tests still green.

**Banned-term check:**

1. `pnpm --filter web lint` — no "approved" in .tsx sources.

Report any failures with repro steps.
</how-to-verify>
<resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
All 14 Phase 6 requirements verified end-to-end by human walkthrough. Full regression (Phase 1-5) green. Banned-term lint clean.
</verification>

<success_criteria>
Phase 6 marked complete in ROADMAP.md. Ready to start Phase 7 (ADS-B integration) or Phase 8 work.
</success_criteria>

<output>
After completion, create `.planning/phases/06-syllabus-rules-progression-audit/06-04-SUMMARY.md`. Include the verification walkthrough outcome, any bugs found + fixed during verification, and hand off to Phase 7 planning.
</output>
