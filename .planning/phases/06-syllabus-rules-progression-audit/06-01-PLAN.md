---
phase: 06-syllabus-rules-progression-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/db/migrations/0023_phase6_enums.sql
  - packages/db/migrations/0024_phase6_lesson_columns.sql
  - packages/db/migrations/0025_phase6_course_version_columns.sql
  - packages/db/migrations/0026_phase6_enrollment_columns.sql
  - packages/db/migrations/0027_phase6_line_item_grade_columns.sql
  - packages/db/migrations/0028_phase6_tables.sql
  - packages/db/migrations/0029_phase6_views.sql
  - packages/db/migrations/0030_phase6_functions.sql
  - packages/db/migrations/0031_phase6_triggers.sql
  - packages/db/migrations/0032_phase6_pg_cron.sql
  - packages/db/migrations/0033_phase6_seed_minimums.sql
  - supabase/migrations/YYYYMMDDNNNNNN_phase6_enums.sql
  - supabase/migrations/YYYYMMDDNNNNNN_phase6_lesson_columns.sql
  - supabase/migrations/YYYYMMDDNNNNNN_phase6_course_version_columns.sql
  - supabase/migrations/YYYYMMDDNNNNNN_phase6_enrollment_columns.sql
  - supabase/migrations/YYYYMMDDNNNNNN_phase6_line_item_grade_columns.sql
  - supabase/migrations/YYYYMMDDNNNNNN_phase6_tables.sql
  - supabase/migrations/YYYYMMDDNNNNNN_phase6_views.sql
  - supabase/migrations/YYYYMMDDNNNNNN_phase6_functions.sql
  - supabase/migrations/YYYYMMDDNNNNNN_phase6_triggers.sql
  - supabase/migrations/YYYYMMDDNNNNNN_phase6_pg_cron.sql
  - supabase/migrations/YYYYMMDDNNNNNN_phase6_seed_minimums.sql
  - packages/db/src/schema/lesson.ts
  - packages/db/src/schema/courseVersion.ts
  - packages/db/src/schema/studentCourseEnrollment.ts
  - packages/db/src/schema/lineItemGrade.ts
  - packages/db/src/schema/lessonOverride.ts
  - packages/db/src/schema/trainingRecordAuditException.ts
  - packages/db/src/schema/studentProgressForecastCache.ts
  - packages/db/src/schema/index.ts
  - packages/db/tests/phase6-rls.test.ts
  - packages/db/tests/phase6-override-race.test.ts
  - packages/db/tests/phase6-pg-cron.test.ts
  - packages/db/tests/phase6-rollover.test.ts
autonomous: true
requirements:
  [SYL-15, SYL-16, SYL-17, SYL-18, SYL-19, SYL-20, SYL-21, SYL-22, SYL-23, SYL-24, SCH-11, IPF-06]
must_haves:
  truths:
    - 'pg_cron extension is enabled and the nightly training-record audit job is registered'
    - 'All six check_* SQL functions and evaluate_lesson_eligibility exist with the signatures locked in CONTEXT.md'
    - 'compute_rollover_line_items correctly suppresses older failures when a later sealed sheet passes the same line item'
    - 'Two concurrent gradeSheet.createFromReservation-style attempts against the same active lesson_override result in exactly one consumption'
    - 'Cross-tenant SELECT/INSERT on lesson_override, training_record_audit_exception, and student_progress_forecast_cache is denied by RLS'
    - 'course_version.minimum_hours is backfilled for the three seeded system courses (PPL, IR, Comm-SEL)'
    - 'is_passing_grade(scale, value) SQL helper exists and matches the TS isPassingGrade behavior'
    - 'Phase 1-5 test suite (187/187) still passes after all migrations applied'
  artifacts:
    - path: 'packages/db/migrations/0023_phase6_enums.sql'
      provides: 'lesson_override_kind, audit_exception_kind, audit_exception_severity enums isolated from usage'
    - path: 'packages/db/migrations/0028_phase6_tables.sql'
      provides: 'lesson_override, training_record_audit_exception, student_progress_forecast_cache tables with RLS'
    - path: 'packages/db/migrations/0030_phase6_functions.sql'
      provides: 'is_passing_grade, all check_*, evaluate_lesson_eligibility, compute_rollover_line_items, student_progress_forecast, suggest_next_activity, run_training_record_audit'
    - path: 'packages/db/migrations/0032_phase6_pg_cron.sql'
      provides: 'create extension if not exists pg_cron + cron.schedule registration'
    - path: 'packages/db/migrations/0033_phase6_seed_minimums.sql'
      provides: 'minimum_hours jsonb backfill for PPL/IR/Comm-SEL'
    - path: 'packages/db/src/schema/lessonOverride.ts'
      provides: 'Drizzle schema mirror with pgPolicy RLS'
    - path: 'packages/db/tests/phase6-override-race.test.ts'
      provides: 'Concurrency proof that exactly one of two parallel consume attempts wins'
  key_links:
    - from: 'evaluate_lesson_eligibility'
      to: 'all six check_* functions'
      via: 'deterministic ordered invocation with jsonb blocker accumulation'
      pattern: 'check_lesson_prerequisites.*check_student_qualifications.*check_instructor_qualifications.*check_resource_requirements.*check_lesson_repeat_limit'
    - from: 'compute_rollover_line_items'
      to: 'is_passing_grade + lesson_grade_sheet.sealed_at'
      via: 'CTE with later-sealed suppression'
      pattern: 'is_passing_grade'
    - from: 'pg_cron job phase6_nightly_training_record_audit'
      to: 'run_training_record_audit()'
      via: "cron.schedule('0 7 * * *', ...)"
      pattern: "cron\\.schedule.*run_training_record_audit"
---

<objective>
Land the entire Phase 6 database layer in a single disciplined plan: 11 hand-authored migrations (enums → column additions → new tables → views → functions → triggers → pg_cron → seed backfill), their Drizzle schema mirrors, and four database-level test suites (cross-tenant RLS, override consumption race, pg_cron smoke, rollover correctness). Task 2 is split into atomic sub-tasks 2a/2b/2c to keep execution boundaries clear.

Purpose: every Phase 6 rules-engine call path (SYL-15..24, SCH-11, IPF-06) depends on these SQL functions and tables. Getting them right in one plan avoids the rework that mid-phase schema churn would cause.

Output: 11 migrations applied cleanly, Drizzle typecheck + lint green, Phase 1-5 regression (187/187) preserved, 4 new test files green, pg_cron job visible via `select * from cron.job`.

Mirror-file rule: each hand-authored migration under `packages/db/migrations/` MUST have exactly one corresponding file under `supabase/migrations/` with the Phase 1-5 timestamped naming convention. The total count in `supabase/migrations/` added by this plan MUST equal 11 (one per SQL file listed in files_modified).
</objective>

<execution_context>
@/Users/christopher/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christopher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/06-syllabus-rules-progression-audit/06-CONTEXT.md
@.planning/phases/06-syllabus-rules-progression-audit/06-RESEARCH.md
@.planning/phases/05-syllabus-model-grading-records/05-01-SUMMARY.md
@.planning/phases/04-camp-maintenance/04-02-SUMMARY.md
@packages/db/migrations/0010_phase4_camp_tables.sql
@packages/db/migrations/0011_phase4_functions_triggers.sql
@packages/db/migrations/0016_phase5_course_tree.sql
@packages/db/migrations/0017_phase5_grade_stage_endorsement.sql
@packages/db/migrations/0018_phase5_functions_triggers_views.sql
@packages/db/src/schema/index.ts
@packages/domain/src/grading/isPassingGrade.ts

<interfaces>
<!-- Key contracts from Phase 5 that Phase 6 builds on. -->

From packages/db/migrations/0017_phase5_grade_stage_endorsement.sql:

- lesson_grade_sheet(id, student_enrollment_id, lesson_id, reservation_id, sealed_at, signer_snapshot, ...)
- line_item_grade(id, grade_sheet_id, line_item_id, grade_value, graded_at, ...) — append-only, sealed via parent
- line_item(id, lesson_id, classification enum('required','optional','must_pass'), grading_scale, ...)

From packages/db/migrations/0016_phase5_course_tree.sql:

- course, course_version, stage, phase, unit, lesson, line_item tree
- student_course_enrollment(id, school_id, base_id, student_user_id, course_version_id, enrolled_at, completed_at, withdrawn_at, ...)

From packages/db/migrations/0018_phase5_functions_triggers_views.sql:

- personnel_currency(user_id, kind, status, expires_at, ...)
- user_flight_log_totals view — used by minimums tracker
- currency_status() STABLE function

From packages/domain/src/grading/isPassingGrade.ts:

```typescript
export function isPassingGrade(scale: GradingScale, value: string | number | null): boolean;
// Scales: 'introduce_practice_perform_mastered' | 'relative_1_5'
// Passing: 'perform'|'mastered' OR value >= 3
```

From packages/db/migrations/0010_phase4_camp_tables.sql — maintenance_overrun pattern:

- Partial unique index on (item_id) WHERE revoked_at IS NULL AND deleted_at IS NULL
- signer_snapshot jsonb, justification text CHECK (length >= 20), expires_at, consumed_at
- BEFORE DELETE hard-delete blocker trigger
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Migrations 0023-0027 — enum + column additions on existing tables</name>
  <files>
    packages/db/migrations/0023_phase6_enums.sql,
    packages/db/migrations/0024_phase6_lesson_columns.sql,
    packages/db/migrations/0025_phase6_course_version_columns.sql,
    packages/db/migrations/0026_phase6_enrollment_columns.sql,
    packages/db/migrations/0027_phase6_line_item_grade_columns.sql,
    supabase/migrations/YYYYMMDDNNNNNN_phase6_enums.sql,
    supabase/migrations/YYYYMMDDNNNNNN_phase6_lesson_columns.sql,
    supabase/migrations/YYYYMMDDNNNNNN_phase6_course_version_columns.sql,
    supabase/migrations/YYYYMMDDNNNNNN_phase6_enrollment_columns.sql,
    supabase/migrations/YYYYMMDDNNNNNN_phase6_line_item_grade_columns.sql,
    packages/db/src/schema/lesson.ts,
    packages/db/src/schema/courseVersion.ts,
    packages/db/src/schema/studentCourseEnrollment.ts,
    packages/db/src/schema/lineItemGrade.ts,
    packages/db/src/schema/index.ts
  </files>
  <action>
Author five hand-authored SQL migration files numbered 0023-0027 and mirror each verbatim to `supabase/migrations/` with Phase 1-5 timestamp convention. Exactly 5 new files under `supabase/migrations/` this task.

**0023_phase6_enums.sql** — Create three enums in isolation (separate file per Phase 2-5 "enum extensions isolated from usage" caveat):

- `lesson_override_kind` with values `prerequisite_skip, repeat_limit_exceeded, currency_waiver`
- `audit_exception_kind` with values `missing_lessons, hours_deficit, missing_endorsements, missing_stage_checks, stale_rollovers, expired_overrides`
- `audit_exception_severity` with values `info, warn, critical`

**0024_phase6_lesson_columns.sql** — ALTER TABLE public.lesson ADD COLUMN for:

- `prerequisite_lesson_ids uuid[] not null default '{}'::uuid[]` (SYL-16)
- `max_repeats int` nullable (SYL-20)
- `required_instructor_qualifications jsonb not null default '[]'::jsonb` (SYL-18/SCH-11)
- `required_instructor_currencies jsonb not null default '[]'::jsonb` (SYL-18/SCH-11)
- `required_student_qualifications jsonb not null default '[]'::jsonb` (SYL-18)
- `required_aircraft_equipment jsonb not null default '[]'::jsonb` (SYL-18)
- `required_aircraft_type text` nullable (SYL-18)
- `required_sim_kind text` nullable (SYL-18)

Also ALTER TABLE public.line_item ADD COLUMN `max_repeats int` nullable (SYL-20).

**0025_phase6_course_version_columns.sql** — ALTER TABLE public.course_version ADD COLUMN:

- `minimum_hours jsonb not null default '{}'::jsonb` (SYL-21)
- `default_plan_cadence_hours_per_week numeric(5,2)` nullable (SYL-22)

**0026_phase6_enrollment_columns.sql** — ALTER TABLE public.student_course_enrollment ADD COLUMN:

- `plan_cadence_hours_per_week numeric(5,2)` nullable (SYL-22)

**0027_phase6_line_item_grade_columns.sql** — ALTER TABLE public.line_item_grade ADD COLUMN:

- `rollover_from_grade_sheet_id uuid references public.lesson_grade_sheet(id) on delete set null` nullable (SYL-15)
- Index: `create index line_item_grade_rollover_idx on public.line_item_grade(rollover_from_grade_sheet_id) where rollover_from_grade_sheet_id is not null;`

Mirror Drizzle schemas in `packages/db/src/schema/lesson.ts`, `courseVersion.ts`, `studentCourseEnrollment.ts`, `lineItemGrade.ts` to add the new columns. Follow the Phase 4 forward-FK pattern.

Apply via `. scripts/dev-env.sh && pnpm --filter @part61/db db:push`. Verify via `psql` that each column exists.

Commit: `feat(06-01): phase 6 enums + column additions (0023-0027)`.
</action>
<verify>
<automated>. scripts/dev-env.sh && pnpm --filter @part61/db exec drizzle-kit check && pnpm --filter @part61/db typecheck && pnpm --filter web typecheck</automated>
</verify>
<done>All 5 migration files applied cleanly (plus 5 supabase/migrations mirrors), Drizzle schema files updated, monorepo typecheck green, Phase 1-5 tests still pass (`pnpm --filter @part61/db test` = 187/187).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2a: New tables (lesson_override, training_record_audit_exception, student_progress_forecast_cache) + Drizzle schemas + RLS + audit triggers + hard-delete blockers + cross-tenant RLS tests</name>
  <files>
    packages/db/migrations/0028_phase6_tables.sql,
    supabase/migrations/YYYYMMDDNNNNNN_phase6_tables.sql,
    packages/db/src/schema/lessonOverride.ts,
    packages/db/src/schema/trainingRecordAuditException.ts,
    packages/db/src/schema/studentProgressForecastCache.ts,
    packages/db/src/schema/index.ts,
    packages/db/tests/phase6-rls.test.ts
  </files>
  <behavior>
    - phase6-rls: authenticated user in school A cannot SELECT or INSERT into lesson_override / training_record_audit_exception / student_progress_forecast_cache rows belonging to school B. INSERT without app.school_id GUC set is rejected.
    - BEFORE DELETE on lesson_override and training_record_audit_exception raises exception (hard-delete blocker).
    - audit.fn_log_change trigger fires on INSERT/UPDATE for all three tables (verified by querying audit_log after a seeded insert).
  </behavior>
  <action>
**0028_phase6_tables.sql** — Three new tables, each with standard columns (`id uuid pk default gen_random_uuid()`, `school_id uuid not null`, `created_at`, `updated_at` where applicable), RLS enabled, school_id policies mirroring Phase 4/5 precedent:

1. `public.lesson_override` (mirror Phase 4 `maintenance_overrun`):
   - `student_enrollment_id uuid not null references public.student_course_enrollment(id)`
   - `lesson_id uuid not null references public.lesson(id)`
   - `base_id uuid null`
   - `kind lesson_override_kind not null`
   - `justification text not null check (length(justification) >= 20)`
   - `granted_at timestamptz not null default now()`
   - `granted_by_user_id uuid not null references public.users(id)`
   - `signer_snapshot jsonb not null`
   - `expires_at timestamptz not null default now() + interval '30 days'`
   - `consumed_at timestamptz null`
   - `revoked_at timestamptz null`
   - `revoked_by_user_id uuid null references public.users(id)`
   - `revocation_reason text null`
   - Partial unique index: `create unique index lesson_override_single_active_idx on public.lesson_override (student_enrollment_id, lesson_id) where consumed_at is null and revoked_at is null;`
   - RLS: school_id policies; audit.fn_log_change trigger; BEFORE DELETE blocker trigger (raise exception).

2. `public.training_record_audit_exception`:
   - `student_enrollment_id uuid not null references public.student_course_enrollment(id)`
   - `kind audit_exception_kind not null`
   - `severity audit_exception_severity not null`
   - `details jsonb not null default '{}'::jsonb`
   - `first_detected_at timestamptz not null default now()`
   - `last_detected_at timestamptz not null default now()`
   - `resolved_at timestamptz null`
   - Unique index: `create unique index training_record_audit_exception_open_idx on public.training_record_audit_exception (student_enrollment_id, kind) where resolved_at is null;`
   - RLS + audit trigger + hard-delete blocker.

3. `public.student_progress_forecast_cache` (mirror Phase 4 `aircraft_downtime_forecast`):
   - `student_enrollment_id uuid primary key references public.student_course_enrollment(id) on delete cascade`
   - `forecast jsonb not null`
   - `computed_at timestamptz not null default now()`
   - RLS + audit trigger. (No hard-delete blocker — cache is allowed to be evicted by on-delete cascade from enrollment.)

Mirror to supabase/migrations/. Add Drizzle schemas with pgPolicy RLS mirroring Phase 4/5 pattern; register in `packages/db/src/schema/index.ts`.

**phase6-rls.test.ts** — Cross-tenant deny on all three new tables using existing Phase 1/3 postgres-js Vitest harness. Include a test that hard-delete attempts on lesson_override + training_record_audit_exception raise the blocker exception.

Commits:

1. `feat(06-01): phase6 new tables + RLS + audit triggers + hard-delete blockers (0028)`
2. `test(06-01): phase6 cross-tenant RLS tests`
   </action>
   <verify>
   <automated>. scripts/dev-env.sh && pnpm --filter @part61/db db:push && pnpm --filter @part61/db test -- phase6-rls && pnpm --filter @part61/db typecheck</automated>
   </verify>
   <done>Migration 0028 applied, Drizzle schemas registered, phase6-rls.test.ts green, Phase 1-5 regression intact.</done>
   </task>

<task type="auto" tdd="true">
  <name>Task 2b: SQL check functions + is_passing_grade port + evaluate_lesson_eligibility orchestrator + compute_rollover_line_items + suggest_next_activity + function-level tests (rollover + override race)</name>
  <files>
    packages/db/migrations/0029_phase6_views.sql,
    packages/db/migrations/0030_phase6_functions.sql,
    supabase/migrations/YYYYMMDDNNNNNN_phase6_views.sql,
    supabase/migrations/YYYYMMDDNNNNNN_phase6_functions.sql,
    packages/db/tests/phase6-rollover.test.ts,
    packages/db/tests/phase6-override-race.test.ts
  </files>
  <behavior>
    - is_passing_grade SQL returns true for 'perform'/'mastered' on IPPM; true for value >= 3 on 1-5; matches TS helper on full matrix.
    - compute_rollover_line_items: prior fail + later pass → zero rows for that line item. Prior fail + no later pass → returns the row.
    - Two concurrent transactions executing SELECT FOR UPDATE + UPDATE consumed_at on the same active override → exactly one wins; other sees consumed_at IS NOT NULL or partial-unique 23505 (accept either shape, Phase 3 approve-race pattern).
    - evaluate_lesson_eligibility short-circuits on active override; deterministic order prerequisites → student currency → student quals → instructor currency → instructor quals → resource → repeat.
  </behavior>
  <action>
**0029_phase6_views.sql**:
- `student_course_minimums_status` (WITH security_invoker = true) joining `student_course_enrollment` × `course_version` × `user_flight_log_totals`, returning per-category `{ required, actual, remaining, percent }` for each FAA minimum key.
- `management_override_activity` view: last 30 days of lesson_override rows joined with user + student + lesson metadata for the IPF-06 admin dashboard panel.

**0030_phase6_functions.sql** — ALL SQL functions (one file per research Pattern 1):

- `public.is_passing_grade(p_scale text, p_value text) returns boolean` LANGUAGE SQL IMMUTABLE — port TS logic. Comment cites `packages/domain/src/grading/isPassingGrade.ts` as source of truth.
- `public.check_lesson_prerequisites(enrollment_id, lesson_id) returns jsonb` — RESEARCH.md Pattern 1
- `public.check_student_qualifications(enrollment_id, lesson_id) returns jsonb` — join personnel_currency + required_student_qualifications
- `public.check_instructor_qualifications(instructor_user_id, lesson_id) returns jsonb`
- `public.check_resource_requirements(aircraft_id, lesson_id) returns jsonb`
- `public.check_lesson_repeat_limit(enrollment_id, lesson_id) returns jsonb` — count sealed sheets vs lesson.max_repeats
- `public.evaluate_lesson_eligibility(enrollment_id, lesson_id, aircraft_id, instructor_user_id) returns jsonb` — RESEARCH.md Pattern 2; short-circuits on active override; deterministic order as stated.
- `public.compute_rollover_line_items(enrollment_id, target_lesson_id) returns table(source_grade_sheet_id uuid, line_item_id uuid)` — RESEARCH.md Pattern 3 (later-passed suppression CTE).
- `public.suggest_next_activity(enrollment_id) returns jsonb` — recursive CTE walking course tree, prefers rollover lessons first, returns `{ lesson_id, reasoning, blocked_by? }`.

All functions declared `security invoker`. STABLE where possible. NOTE: `student_progress_forecast`, `refresh_student_progress_forecast`, and `run_training_record_audit` are deferred to Task 2c (they depend on the forecast cache trigger wiring and pg_cron).

**phase6-rollover.test.ts**:

- seed enrollment with two sealed sheets for the same lesson: first fails required line item, second passes → compute_rollover_line_items returns zero rows for that line item.
- inverse: failed first, still unpassed → returns the row.
- is_passing_grade SQL matches TS helper on small matrix.

**phase6-override-race.test.ts**:

- Seed an active unconsumed override. Two parallel transactions each run SELECT FOR UPDATE + UPDATE consumed_at. Assert exactly one sets consumed_at; the other sees consumed_at IS NOT NULL or 23505. Either shape acceptable.

Commits:

1. `feat(06-01): phase6 views (0029)`
2. `feat(06-01): phase6 SQL check functions + is_passing_grade + evaluate + rollover + suggest_next (0030)`
3. `test(06-01): phase6 rollover + override race tests`
   </action>
   <verify>
   <automated>. scripts/dev-env.sh && pnpm --filter @part61/db db:push && pnpm --filter @part61/db test -- phase6-rollover && pnpm --filter @part61/db test -- phase6-override-race && pnpm --filter @part61/db typecheck</automated>
   </verify>
   <done>Migrations 0029-0030 applied, all six check\_\* + evaluate + compute_rollover + suggest_next + is_passing_grade callable via psql, rollover + override race test files green, Phase 1-5 regression intact.</done>
   </task>

<task type="auto" tdd="true">
  <name>Task 2c: Forecast cache trigger + student_progress_forecast + refresh + run_training_record_audit + triggers (0031) + pg_cron registration (0032) + seed minimum_hours backfill (0033) + pg_cron smoke test</name>
  <files>
    packages/db/migrations/0031_phase6_triggers.sql,
    packages/db/migrations/0032_phase6_pg_cron.sql,
    packages/db/migrations/0033_phase6_seed_minimums.sql,
    supabase/migrations/YYYYMMDDNNNNNN_phase6_triggers.sql,
    supabase/migrations/YYYYMMDDNNNNNN_phase6_pg_cron.sql,
    supabase/migrations/YYYYMMDDNNNNNN_phase6_seed_minimums.sql,
    packages/db/migrations/0030_phase6_functions.sql,
    packages/db/tests/phase6-pg-cron.test.ts
  </files>
  <behavior>
    - pg_cron smoke (runs FIRST): `select jobname, schedule from cron.job where jobname = 'phase6_nightly_training_record_audit'` returns exactly one row with schedule '0 7 * * *'. If pg_cron is unavailable locally, test surfaces a clear failure message naming the extension.
    - AFTER INSERT on flight_log_time → refresh_student_progress_forecast fires → forecast cache row appears for affected enrollment.
    - course_version.minimum_hours is non-empty for PPL, IR, Comm-SEL after migration 0033 runs.
    - run_training_record_audit() is idempotent: running twice in a row produces the same open-exception set (no duplicates, same last_detected_at update).
  </behavior>
  <action>
**Extend 0030_phase6_functions.sql** (append) OR add a small follow-up migration — add:
- `public.student_progress_forecast(enrollment_id) returns jsonb` — cadence math, ahead/behind, projected dates, confidence.
- `public.refresh_student_progress_forecast(enrollment_id) returns void` — Pattern 5 SELECT FOR UPDATE serialization.
- `public.run_training_record_audit() returns void` — iterates active enrollments; for each computes missing_lessons / hours_deficit / missing_endorsements / missing_stage_checks / stale_rollovers / expired_overrides; UPSERTs via ON CONFLICT WHERE resolved_at IS NULL setting last_detected_at = now(). Also calls refresh_student_progress_forecast per enrollment.

**0031_phase6_triggers.sql**:

- BEFORE DELETE blockers already on lesson_override + training_record_audit_exception (2a) — this file adds runtime wiring only.
- AFTER INSERT OR UPDATE on public.flight_log_time → resolve enrollment(s) on student_user_id + active status, call refresh_student_progress_forecast for each.
- AFTER UPDATE OF plan_cadence_hours_per_week on student_course_enrollment → refresh that enrollment's forecast.
- AFTER UPDATE OF minimum_hours, default_plan_cadence_hours_per_week on course_version → refresh all enrollments on that version.

**0032_phase6_pg_cron.sql**:

```sql
create extension if not exists pg_cron;
select cron.schedule(
  'phase6_nightly_training_record_audit',
  '0 7 * * *',
  $$select public.run_training_record_audit()$$
);
```

Guard with idempotent check against cron.job or cron.unschedule + re-schedule; document in header comment which pattern matches local pg_cron version.

**0033_phase6_seed_minimums.sql** — UPDATE course_version SET minimum_hours = jsonb'{...}', default_plan_cadence_hours_per_week = X for PPL (§61.109), IR (§61.65), Comm-SEL (§61.129). Use WHERE join on course.kind identifier verified from 05-02-SUMMARY.md. Idempotent UPDATE-only.

**phase6-pg-cron.test.ts** — RUNS FIRST to prove pg_cron availability. Smoke test on cron.job row. Also asserts course_version.minimum_hours is populated for the three seeded courses. Idempotency check: call run_training_record_audit() twice, assert open exception counts stable.

Commits:

1. `feat(06-01): phase6 forecast + run_training_record_audit functions`
2. `feat(06-01): phase6 triggers (0031)`
3. `feat(06-01): phase6 pg_cron registration (0032)`
4. `feat(06-01): phase6 seed minimum_hours backfill (0033)`
5. `test(06-01): phase6 pg_cron smoke + audit idempotency`
   </action>
   <verify>
   <automated>. scripts/dev-env.sh && pnpm --filter @part61/db db:push && pnpm --filter @part61/db test -- phase6-pg-cron && pnpm --filter @part61/db test && pnpm --filter web typecheck && pnpm --filter web lint</automated>
   </verify>
   <done>
   All 11 migrations applied in order. Drizzle typecheck green. All four phase6-_.test.ts suites pass. Full Phase 1-5 regression (187/187) still passes. `psql` query `select jobname, schedule from cron.job where jobname = 'phase6_nightly_training_record_audit'` returns exactly one row with schedule `0 7 _ \* \*`. course_version.minimum_hours populated for PPL/IR/Comm-SEL. Banned-term lint stays clean.
   </done>
   </task>

</tasks>

<verification>
- All 11 migrations apply in order on a fresh database
- `supabase/migrations/` contains exactly 11 new Phase 6 mirror files after this plan
- Phase 1-5 regression: 187/187 tests pass unchanged
- Phase 6 test suites: 4 new test files pass
- pg_cron job registered and queryable
- `select public.evaluate_lesson_eligibility(...)` callable; returns jsonb in documented shape
- Cross-tenant RLS blocks all three new tables
- is_passing_grade SQL output matches TS helper on all scale × value combinations
</verification>

<success_criteria>
Phase 6 schema + rules engine is in-database, tested, RLS-safe, and ready for plan 06-02 (tRPC wrappers) to consume. No churn expected in the schema during subsequent plans.
</success_criteria>

<output>
After completion, create `.planning/phases/06-syllabus-rules-progression-audit/06-01-SUMMARY.md` using the standard SUMMARY template. Highlight: migration numbers used (0023-0033), new SQL functions created, the is_passing_grade port decision, any race-test outcome variations (23P01 vs 23505 vs "no active override"), and any Rule 1 deviations from this plan with justification.
</output>
