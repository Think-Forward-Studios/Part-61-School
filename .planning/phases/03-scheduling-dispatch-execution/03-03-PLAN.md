---
phase: 03-scheduling-dispatch-execution
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - apps/web/app/(app)/schedule/page.tsx
  - apps/web/app/(app)/schedule/Calendar.tsx
  - apps/web/app/(app)/schedule/ReservationDrawer.tsx
  - apps/web/app/(app)/schedule/request/page.tsx
  - apps/web/app/(app)/schedule/request/ReservationForm.tsx
  - apps/web/app/(app)/schedule/approvals/page.tsx
  - apps/web/app/(app)/schedule/[id]/page.tsx
  - apps/web/app/(app)/admin/schedule/page.tsx
  - apps/web/app/(app)/admin/rooms/page.tsx
  - apps/web/app/(app)/admin/rooms/[id]/page.tsx
  - apps/web/app/(app)/admin/blocks/page.tsx
  - apps/web/app/(app)/admin/blocks/new/page.tsx
  - apps/web/components/schedule/ActivityChip.tsx
  - apps/web/components/schedule/StatusLabel.tsx
  - apps/web/package.json
autonomous: true
requirements:
  - SCH-01
  - SCH-03
  - SCH-06
  - SCH-07
  - SCH-13
  - SCH-15
  - SCH-16
  - SCH-17

must_haves:
  truths:
    - "Student visiting /schedule sees their own reservations on a calendar plus free/busy on bookable resources"
    - "Clicking an empty slot opens the reservation form pre-filled with that start time"
    - "Reservation form supports activity_type selection, optional XC fields when activity_type='flight', and recurring options"
    - "Instructor visiting /schedule/approvals sees pending requests and can approve/reject"
    - "Admin visiting /admin/schedule sees the full base calendar with by-aircraft / by-instructor / by-room views"
    - "Activity type rendered with color chip; status rendered with text 'Confirmed' (NOT 'Approved') and visual cue"
    - "Admin can CRUD rooms at /admin/rooms and blocks at /admin/blocks"
  artifacts:
    - path: "apps/web/app/(app)/schedule/Calendar.tsx"
      provides: "FullCalendar wrapper with views by aircraft/instructor/student/room/day/week/month"
    - path: "apps/web/app/(app)/schedule/request/ReservationForm.tsx"
      provides: "react-hook-form + zod form with activity type, resource pickers, time range, recurring options, XC fields"
    - path: "apps/web/components/schedule/StatusLabel.tsx"
      provides: "Maps reservation.status to display text — 'approved' → 'Confirmed' to satisfy banned-term rule"
  key_links:
    - from: "Calendar.tsx"
      to: "trpc.schedule.list"
      via: "TanStack Query (React Server Component shell + client island)"
      pattern: "trpc.*schedule\\.list"
    - from: "ReservationForm.tsx"
      to: "trpc.schedule.request"
      via: "react-hook-form mutation"
      pattern: "trpc.*schedule\\.request"
    - from: "StatusLabel.tsx"
      to: "Banned-term-safe display"
      via: "Map approved → 'Confirmed'"
      pattern: "Confirmed"
---

<objective>
Build the calendar + scheduling UI that lets students request, instructors approve, and admins see the full picture. FullCalendar React with MIT plugins only (resource-timeline is premium — fall back to timegrid + a per-resource column layout). Admin CRUD pages for rooms and blocks. Status text mapped to "Confirmed" via a tiny shared StatusLabel component to satisfy the banned-term rule.

Purpose: Visible scheduling — the "calendar exists" milestone.
Output: /schedule, /schedule/request, /schedule/approvals, /schedule/[id], /admin/schedule, /admin/rooms, /admin/blocks routes shipped.
</objective>

<execution_context>
@/Users/christopher/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christopher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-scheduling-dispatch-execution/03-CONTEXT.md
@.planning/phases/03-scheduling-dispatch-execution/03-02-PLAN.md

@apps/web/app/(app)/admin/people/page.tsx
@apps/web/app/(app)/admin/aircraft/page.tsx
@apps/web/app/(app)/admin/aircraft/[id]/page.tsx
@apps/web/app/layout.tsx
@apps/web/components/BaseSwitcher.tsx
@CLAUDE.md

<interfaces>
Banned-term rule: ANY user-facing string in apps/web/** containing "approved" / "Approved" fails CI. Use "Confirmed" / "Confirm" instead. The reservation status DATA value can stay 'approved' — it never appears in JSX text.

Phase 2 admin page pattern:
- page.tsx is a Server Component that calls trpc server-side for initial data
- *Form/*Panel.tsx are Client Components ('use client') with react-hook-form + zod
- TRPCProvider already wired in app/layout.tsx
- BaseSwitcher available in header

FullCalendar packages to add:
  @fullcalendar/react @fullcalendar/daygrid @fullcalendar/timegrid @fullcalendar/interaction
  (do NOT add @fullcalendar/resource-timeline — it is premium per Phase 3 research)

Activity type colors (locked, do NOT make configurable):
  flight=blue, simulator=purple, oral=orange, academic=green, misc=gray
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FullCalendar install + /schedule + /schedule/request + StatusLabel (banned-term safe)</name>
  <files>apps/web/package.json, apps/web/app/(app)/schedule/page.tsx, apps/web/app/(app)/schedule/Calendar.tsx, apps/web/app/(app)/schedule/ReservationDrawer.tsx, apps/web/app/(app)/schedule/request/page.tsx, apps/web/app/(app)/schedule/request/ReservationForm.tsx, apps/web/app/(app)/schedule/[id]/page.tsx, apps/web/components/schedule/ActivityChip.tsx, apps/web/components/schedule/StatusLabel.tsx</files>
  <behavior>
    - Install @fullcalendar/react @fullcalendar/daygrid @fullcalendar/timegrid @fullcalendar/interaction in apps/web
    - StatusLabel: maps {requested:'Pending', approved:'Confirmed', dispatched:'Dispatched', flown:'Flown', closed:'Closed', cancelled_free:'Cancelled', cancelled_late:'Cancelled (late)', no_show:'No-show', scrubbed:'Scrubbed', pending_sign_off:'Awaiting sign-off'} — note the literal 'Confirmed' (NOT the banned word)
    - ActivityChip: small colored chip per activity_type, locked colors per CONTEXT.md
    - /schedule page.tsx: server component that fetches user role + reservations via trpc.schedule.list (mode='mine' for student, 'full' for instructor/admin); renders Calendar.tsx
    - Calendar.tsx ('use client'): wraps FullCalendar with timegrid + daygrid plugins; renders events with ActivityChip + StatusLabel; clicking an event opens ReservationDrawer; clicking a slot navigates to /schedule/request?start=...&end=...
    - For "by aircraft / by instructor / by room" views (no resource-timeline plugin), use a fallback: a select dropdown that filters the timegrid to a single resource at a time (note this fallback in Plan summary; full multi-resource grid is a v2 polish)
    - ReservationDrawer ('use client'): opens with reservation detail + cancel/edit buttons (approve button only if instructor/admin)
    - /schedule/request page + ReservationForm (react-hook-form + zod): activity type select, resource pickers, time range, notes, recurring expander (frequency, days of week, until/count), XC expander (route_string, ete_minutes, stops, fuel_stops, alternate)
    - /schedule/[id]: detail page with audit trail panel (queries audit_log via existing API)
  </behavior>
  <action>
    Match the Phase 2 form pattern (CreatePersonForm.tsx is the cleanest reference). Use react-hook-form + @hookform/resolvers/zod. For the calendar, follow the FullCalendar React quickstart but stay strictly on MIT plugins. The locked activity_type colors must match exactly. Run lint after writing — the banned-term rule will catch any "approved" / "approve" leaks in JSX or string literals.

    NOTE: the verb "Approve" is also banned in user-facing strings. Use "Confirm" / "Confirm Request" on the approval button instead.
  </action>
  <verify>
    <automated>. scripts/dev-env.sh && pnpm --filter ./apps/web lint && pnpm --filter ./apps/web build</automated>
  </verify>
  <done>/schedule, /schedule/request, /schedule/[id] routes build clean; banned-term lint passes; FullCalendar renders events with chips + status labels</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: /schedule/approvals + /admin/schedule + /admin/rooms + /admin/blocks</name>
  <files>apps/web/app/(app)/schedule/approvals/page.tsx, apps/web/app/(app)/admin/schedule/page.tsx, apps/web/app/(app)/admin/rooms/page.tsx, apps/web/app/(app)/admin/rooms/[id]/page.tsx, apps/web/app/(app)/admin/blocks/page.tsx, apps/web/app/(app)/admin/blocks/new/page.tsx</files>
  <behavior>
    - /schedule/approvals (instructor/admin gated by AdminGuard equivalent): list pending requests with Confirm / Reject buttons; calling schedule.approve and showing the friendly conflict error if it fails
    - /admin/schedule: full-school calendar for active base, reuses Calendar.tsx with mode='full'
    - /admin/rooms: table list pattern (mirror /admin/aircraft/page.tsx)
    - /admin/rooms/[id]: edit form with name, capacity, features multi-select
    - /admin/blocks: list of schedule_block rows with create/delete
    - /admin/blocks/new: form to create a recurring block (frequency, days of week, time range, instructor, aircraft, room, valid_from, valid_until)
  </behavior>
  <action>
    Mirror /admin/aircraft and /admin/people patterns from Phase 2 plan 02-04. AdminGuard on /admin/* (already exists). For /schedule/approvals, an instructor-or-admin gate is needed — add a small server-side role check helper or reuse the AdminGuard adapted. The calendar component on /admin/schedule reuses Calendar.tsx but passes a different mode prop.
  </action>
  <verify>
    <automated>. scripts/dev-env.sh && pnpm --filter ./apps/web lint && pnpm --filter ./apps/web build</automated>
  </verify>
  <done>All scheduling and admin scheduling routes build clean; lint clean; manual smoke test works against running dev server</done>
</task>

</tasks>

<verification>
- `. scripts/dev-env.sh && pnpm -r lint` clean (banned terms!)
- `. scripts/dev-env.sh && pnpm --filter ./apps/web build` clean
- `. scripts/dev-env.sh && pnpm -r typecheck` clean
</verification>

<success_criteria>
Calendar and reservation flow visible end-to-end. Student can request, instructor can confirm, admin can see the whole base. No banned-term lint failures.
</success_criteria>

<output>
After completion, create `.planning/phases/03-scheduling-dispatch-execution/03-03-SUMMARY.md`
</output>
