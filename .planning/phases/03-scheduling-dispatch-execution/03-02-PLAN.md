---
phase: 03-scheduling-dispatch-execution
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - packages/api/src/routers/schedule.ts
  - packages/api/src/routers/schedule/reservations.ts
  - packages/api/src/routers/schedule/recurring.ts
  - packages/api/src/routers/schedule/blocks.ts
  - packages/api/src/routers/schedule/freebusy.ts
  - packages/api/src/routers/dispatch.ts
  - packages/api/src/routers/fif.ts
  - packages/api/src/routers/admin/rooms.ts
  - packages/api/src/routers/admin/squawks.ts
  - packages/api/src/routers/_root.ts
  - packages/api/src/procedures.ts
  - packages/domain/src/schedule.ts
  - tests/rls/api-schedule.test.ts
  - tests/rls/api-dispatch.test.ts
  - tests/rls/api-fif.test.ts
autonomous: true
requirements:
  - SCH-01
  - SCH-03
  - SCH-04
  - SCH-06
  - SCH-08
  - SCH-09
  - SCH-13
  - SCH-15
  - SCH-16
  - SCH-18
  - INS-04
  - FTR-02
  - FTR-03
  - FTR-04
  - FTR-05
  - FTR-06
  - FTR-07
  - FTR-08

must_haves:
  truths:
    - "Student can call schedule.request to create a requested reservation; instructor/admin call schedule.approve to move it to approved"
    - "schedule.approve raises is_airworthy_at error when aircraft is grounded"
    - "schedule.list with mode='mine' returns only the caller's reservations; mode='freebusy' returns redacted ranges"
    - "Recurring create materializes N child rows with shared series_id; if any child collides, the whole insert rolls back"
    - "dispatch.dispatch transitions approved → dispatched only when student_checked_in_at and instructor_authorized_at are set, all active FIF notices are acked, and (for flight) Hobbs/tach out provided; writes flight_log_entry kind='flight_out'"
    - "dispatch.closeOut writes flight_log_entry kind='flight_in' paired to the dispatch row, creates squawks, transitions to closed (or pending_sign_off if student-only save)"
    - "schedule.cancel transitions to cancelled_free/cancelled_late based on current time vs start; no_show writes Phase 2 no_show row"
    - "fif.list returns active notices; fif.acknowledge upserts an ack; admin fif.post creates a notice"
    - "Person hold/grounding from Phase 2 person_hold blocks schedule.approve"
  artifacts:
    - path: "packages/api/src/routers/schedule/reservations.ts"
      provides: "request, approve, list (mine|full|freebusy), update, cancel"
    - path: "packages/api/src/routers/dispatch.ts"
      provides: "list (currently flying / about to fly / recently closed), dispatch, markStudentPresent, authorizeRelease, closeOut, openSquawk"
    - path: "packages/api/src/routers/fif.ts"
      provides: "list, acknowledge, post (admin), revoke (admin)"
    - path: "packages/api/src/procedures.ts"
      provides: "instructorOrAdminProcedure"
  key_links:
    - from: "schedule.approve"
      to: "is_airworthy_at + person_hold + reservation status transition"
      via: "tRPC middleware + raw SQL function call"
      pattern: "is_airworthy_at"
    - from: "dispatch.dispatch"
      to: "flight_log_entry insert with kind='flight_out'"
      via: "withTenantTx mutation"
      pattern: "flight_out"
    - from: "dispatch.closeOut"
      to: "paired flight_log_entry kind='flight_in' with paired_entry_id FK"
      via: "withTenantTx mutation"
      pattern: "flight_in.*paired_entry_id"
---

<objective>
tRPC routers for everything Phase 3 needs server-side: scheduling (request/approve/list/cancel/recurring/blocks/freebusy), dispatch (list/dispatch/checkin/authorize/closeOut/openSquawk), FIF (list/ack/post/revoke), and admin CRUD for rooms + squawks. Per-procedure middleware enforces is_airworthy_at, person_hold, FIF-ack, status-transition, and role gates. API integration tests cover the critical paths.

Purpose: Lock the business rules in the API layer so the UI is a thin pass-through. Same pattern as Phase 2 plan 02-03.
Output: Routers exported from _root.ts, integration tests green against live local Supabase.
</objective>

<execution_context>
@/Users/christopher/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christopher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-scheduling-dispatch-execution/03-CONTEXT.md
@.planning/phases/03-scheduling-dispatch-execution/03-RESEARCH.md
@.planning/phases/03-scheduling-dispatch-execution/03-01-PLAN.md

@packages/api/src/routers/_root.ts
@packages/api/src/routers/admin/aircraft.ts
@packages/api/src/routers/admin/people.ts
@packages/api/src/routers/flightLog.ts
@packages/api/src/procedures.ts

<interfaces>
Existing procedures (packages/api/src/procedures.ts):
- publicProcedure
- protectedProcedure (authenticated)
- adminProcedure (admin role)
- All wrapped via withTenantTx → SET LOCAL app.school_id, app.base_id

Phase 2 person_hold table:
- columns: id, user_id, school_id, kind ('hold'|'grounding'), reason, created_at, cleared_at
- Active when cleared_at IS NULL
- Query: SELECT 1 FROM person_hold WHERE user_id=? AND cleared_at IS NULL

Phase 2 no_show table receives a row when reservation closes as no_show.

Reservation status state machine (server-enforced):
  requested → approved | cancelled_free | cancelled_late
  approved → dispatched | cancelled_free | cancelled_late | scrubbed_*
  dispatched → flown | pending_sign_off | closed
  pending_sign_off → closed
  flown → closed
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: schedule.* router (request/approve/list/cancel/recurring/blocks/freebusy)</name>
  <files>packages/api/src/routers/schedule.ts, packages/api/src/routers/schedule/reservations.ts, packages/api/src/routers/schedule/recurring.ts, packages/api/src/routers/schedule/blocks.ts, packages/api/src/routers/schedule/freebusy.ts, packages/api/src/procedures.ts, packages/api/src/routers/_root.ts, packages/domain/src/schedule.ts, packages/api/src/routers/admin/rooms.ts</files>
  <behavior>
    - schedule.request (protectedProcedure): zod input { activityType, aircraftId?, instructorId?, studentId?, roomId?, startsAt, endsAt, notes?, xc? }; inserts reservation with status='requested'
    - schedule.approve (instructorOrAdminProcedure): re-checks is_airworthy_at(aircraftId, startsAt), checks person_hold for student+instructor, attempts UPDATE status='approved'; on 23P01 returns user-friendly conflict error naming the colliding reservation
    - schedule.list (protectedProcedure): mode 'mine' | 'full' | 'freebusy'; server decides what role can request what mode; freebusy uses public.free_busy SQL function
    - schedule.cancel: status transition to cancelled_free / cancelled_late based on current time vs start
    - schedule.markNoShow: transitions to no_show + writes Phase 2 no_show row
    - recurring.create: accepts {frequency, daysOfWeek, until|count, base reservation fields}; expands server-side; inserts all rows in one transaction with shared series_id; rolls back on any constraint violation with the offending date in the error
    - recurring.editScope: 'occurrence' | 'following' | 'series' modes
    - blocks.create / list / delete: schedule_block CRUD (admin only); creates schedule_block_instance children
    - freebusy.forResource: wraps the SQL free_busy() function
    - admin/rooms.ts: CRUD (admin only)
    - instructorOrAdminProcedure added to procedures.ts: requires role IN ('instructor','admin')
    - All routers exported from _root.ts under appRouter.schedule
    - Display label for reservation status uses "Confirmed" not "Approved" in any user-facing string returned by the API
  </behavior>
  <action>
    Mirror the Phase 2 plan 02-03 structure (packages/api/src/routers/admin/people.ts is the cleanest reference). Use withTenantTx for every mutation. For the conflict-error mapping, catch postgres error code '23P01' and map to a TRPCError with code 'CONFLICT' and a message that names the colliding row (query the table by the same EXCLUDE predicate to find it). For free/busy, call the SQL function via raw query — don't try to materialize it in TypeScript. Note the banned-term rule: any string literal with "approved" in apps/web/** is banned, but data values are fine in packages/api. UI display text for status='approved' must say "Confirmed" in plan 03 (the UI plan).
  </action>
  <verify>
    <automated>. scripts/dev-env.sh && pnpm -r typecheck && pnpm --filter @part61/rls-tests test -- api-schedule</automated>
  </verify>
  <done>schedule.* router exposes all methods; conflict errors map to friendly messages; recurring expansion rollback works; integration test green</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: dispatch.* + fif.* + admin/squawks routers + integration tests</name>
  <files>packages/api/src/routers/dispatch.ts, packages/api/src/routers/fif.ts, packages/api/src/routers/admin/squawks.ts, packages/api/src/routers/_root.ts, tests/rls/api-schedule.test.ts, tests/rls/api-dispatch.test.ts, tests/rls/api-fif.test.ts</files>
  <behavior>
    - dispatch.list (instructorOrAdminProcedure): returns { currentlyFlying, aboutToFly, recentlyClosed }; aboutToFly = status='approved' AND start within 60min; overdue flag computed via now() > end + school.overdue_grace_minutes
    - dispatch.markStudentPresent: sets student_checked_in_at/by
    - dispatch.authorizeRelease: sets instructor_authorized_at/by
    - dispatch.dispatchReservation: requires student_checked_in_at + instructor_authorized_at + (for flight) hobbsOut + tachOut; verifies all active FIF notices acked by the dispatching pilot; writes flight_log_entry kind='flight_out'; transitions status to dispatched
    - dispatch.closeOut: input {hobbsIn, tachIn, fuelGal?, oilQt?, route?, squawks?: [{title, description, severity}], notes, signedOffByInstructor: bool}; writes flight_log_entry kind='flight_in' with paired_entry_id pointing at the flight_out row; creates aircraft_squawk rows; transitions to closed (if signedOffByInstructor) or pending_sign_off
    - dispatch.openSquawk (any role): inserts into aircraft_squawk; severity='grounding' future reservations are blocked via is_airworthy_at
    - dispatch.passengerManifest.upsert: replaces manifest rows for a reservation
    - fif.listActive (protectedProcedure): notices where effective_at <= now() AND (expires_at IS NULL OR expires_at > now())
    - fif.listUnacked (protectedProcedure): active minus user's acks
    - fif.acknowledge: upsert ack
    - fif.post (adminProcedure): create notice
    - fif.revoke (adminProcedure): set expires_at = now()
    - admin/squawks.ts: list/resolve (mechanic or admin)
    - Tests cover: full happy path (request → approve → dispatch → closeOut), grounded aircraft blocks approve, unacked FIF blocks dispatch, missing student check-in blocks dispatch, no_show writes Phase 2 row, paired flight_log entries land correctly, overdue flag set when past end+grace
  </behavior>
  <action>
    Same Phase 2 router pattern. The dispatch closeOut is the most logic-heavy — keep it in one transaction so the flight_log_entry insert + squawk inserts + status transition all rollback together if any step fails. For the FIF gate in dispatch, query: `SELECT id FROM fif_notice WHERE active AND id NOT IN (SELECT notice_id FROM fif_acknowledgement WHERE user_id=?)` and reject if non-empty. Tests use the Phase 2 RLS harness pattern; reuse seedTwoSchools and asUserOf.
  </action>
  <verify>
    <automated>. scripts/dev-env.sh && pnpm --filter @part61/rls-tests test -- api-dispatch api-fif api-schedule</automated>
  </verify>
  <done>dispatch + fif + squawks routers complete; all integration tests green; full suite (Phase 1+2+3) still passing</done>
</task>

</tasks>

<verification>
- `. scripts/dev-env.sh && pnpm -r typecheck` clean
- `. scripts/dev-env.sh && pnpm -r lint` clean (no banned terms in user-facing strings)
- `. scripts/dev-env.sh && pnpm --filter @part61/rls-tests test` full suite green
</verification>

<success_criteria>
All Phase 3 server-side business rules locked behind tRPC procedures and integration-tested against live Postgres. UI plans 03-04 are now thin clients.
</success_criteria>

<output>
After completion, create `.planning/phases/03-scheduling-dispatch-execution/03-02-SUMMARY.md`
</output>
