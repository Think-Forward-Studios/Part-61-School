---
phase: 03-scheduling-dispatch-execution
plan: 05
type: execute
wave: 5
depends_on: ["03-04"]
files_modified:
  - apps/web/app/(app)/admin/fif/page.tsx
  - apps/web/app/(app)/admin/fif/new/page.tsx
  - apps/web/app/(app)/admin/fif/[id]/page.tsx
  - apps/web/app/(app)/admin/dashboard/page.tsx
  - apps/web/app/(app)/page.tsx
  - apps/web/components/dispatch/FifInbox.tsx
autonomous: false
requirements:
  - FTR-07

must_haves:
  truths:
    - "Admin can post a FIF notice with title, body markdown, severity, effective/expiry dates"
    - "Admin can revoke a notice (sets expires_at = now)"
    - "Pilots see unread FIF notices in their inbox component on the dashboard"
    - "Phase 3 end-to-end smoke test passes against the live local Supabase stack via human verification"
  artifacts:
    - path: "apps/web/app/(app)/admin/fif/page.tsx"
      provides: "List + create + revoke notices"
    - path: "apps/web/components/dispatch/FifInbox.tsx"
      provides: "User-facing unread FIF inbox surface"
  key_links:
    - from: "/admin/fif/new"
      to: "trpc.fif.post"
      via: "react-hook-form mutation"
      pattern: "fif\\.post"
---

<objective>
Ship the FIF admin UI + the user-facing inbox component, fold today's flight line + pending approvals into the admin/student/instructor dashboards, then perform end-to-end live verification of the entire Phase 3 stack against the running Supabase stack.

Purpose: Last 5% (FIF admin) + the human-verify gate that closes the phase.
Output: /admin/fif routes, dashboard panel updates, end-of-phase verification log.
</objective>

<execution_context>
@/Users/christopher/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christopher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-scheduling-dispatch-execution/03-CONTEXT.md
@.planning/phases/03-scheduling-dispatch-execution/03-02-PLAN.md
@.planning/phases/03-scheduling-dispatch-execution/03-04-PLAN.md

@apps/web/app/(app)/admin/dashboard/page.tsx
@apps/web/app/(app)/page.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: /admin/fif CRUD + FifInbox component + dashboard panel updates</name>
  <files>apps/web/app/(app)/admin/fif/page.tsx, apps/web/app/(app)/admin/fif/new/page.tsx, apps/web/app/(app)/admin/fif/[id]/page.tsx, apps/web/app/(app)/admin/dashboard/page.tsx, apps/web/app/(app)/page.tsx, apps/web/components/dispatch/FifInbox.tsx</files>
  <behavior>
    - /admin/fif: list of notices with title, severity badge, posted_at, effective window, ack count, revoke button
    - /admin/fif/new: form (title, body markdown, severity, effective_at, expires_at, base_id?) calls trpc.fif.post
    - /admin/fif/[id]: detail with edit + revoke
    - FifInbox component: fetches trpc.fif.listUnacked; shows unread count badge + expandable list with "Acknowledge" button per notice
    - /admin/dashboard: add "Today's flight line" panel (link to /dispatch) + "Pending approvals" count
    - /(app)/page.tsx: role-aware dashboard — student gets next reservation + FifInbox + open squawks; instructor gets today's schedule + pending approvals + FifInbox; admin gets the panels above + FifInbox
    - All button labels avoid banned terms (use "Confirm" / "Acknowledge" / "Post" / "Revoke")
  </behavior>
  <action>
    Standard /admin/* pattern. Markdown rendering: use react-markdown (already a small dep, or add it if absent). Run lint + build.
  </action>
  <verify>
    <automated>. scripts/dev-env.sh && pnpm --filter ./apps/web lint && pnpm --filter ./apps/web build</automated>
  </verify>
  <done>/admin/fif routes ship; FifInbox renders on dashboards; lint + build clean</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Full automated verification suite</name>
  <files>(verification only — no source changes)</files>
  <behavior>
    - All workspaces typecheck clean
    - All workspaces lint clean (banned-term rule passes)
    - apps/web builds clean
    - Full RLS test suite passes (Phase 1 + Phase 2 + all new Phase 3 tests)
  </behavior>
  <action>
    Run the full verification chain:
    ```
    . scripts/dev-env.sh
    pnpm -r typecheck
    pnpm -r lint
    pnpm --filter ./apps/web build
    pnpm --filter @part61/rls-tests test
    ```
    Capture test counts and any warnings into the SUMMARY. Fix any regressions before checkpoint.
  </action>
  <verify>
    <automated>. scripts/dev-env.sh && pnpm -r typecheck && pnpm -r lint && pnpm --filter ./apps/web build && pnpm --filter @part61/rls-tests test</automated>
  </verify>
  <done>All four commands return zero; test count documented in SUMMARY</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human end-to-end verification of Phase 3 against live stack</name>
  <what-built>
    Full Phase 3: reservation table + four exclusion constraints + shadow-row trigger for unavailability + recurring/block materialization + is_airworthy_at stub + minimal squawks + tRPC routers (schedule, dispatch, fif, rooms, squawks) + calendar UI + dispatch screen + close-out form + passenger manifest + FIF notices/ack.
  </what-built>
  <how-to-verify>
    Start the dev server (`. scripts/dev-env.sh && pnpm --filter ./apps/web dev`) and walk through:

    1. **Calendar visibility** — visit /schedule as a student, see own reservations + free/busy
    2. **Reservation request** — click empty slot, fill the form, submit; reservation appears as "Pending"
    3. **Conflict prevention** — log in as instructor; visit /schedule/approvals; confirm a request; create a SECOND overlapping reservation on the same aircraft → expect conflict error message naming the colliding row
    4. **Airworthiness gate** — ground an aircraft (set aircraft.grounded_at via /admin/aircraft); attempt to confirm a reservation on it → expect error
    5. **Unavailability** — create a person_unavailability row for an instructor; attempt to schedule that instructor in the window → expect block
    6. **Recurring** — create a weekly recurring reservation for 4 weeks; verify 4 children with shared series_id; cancel one without affecting siblings
    7. **Block scheduling** — create a schedule_block at /admin/blocks; book a reservation into a block instance from /schedule
    8. **FIF gate** — post a FIF notice as admin; attempt dispatch as a pilot who hasn't acked → expect modal blocks; acknowledge then dispatch succeeds
    9. **Dispatch flow** — full happy path: student check-in → instructor authorize → Hobbs out → submit dispatch → row appears in Currently Flying
    10. **Overdue alarm** — set a reservation's time_range to past + grace; refresh /dispatch; row goes red and audio cue plays once
    11. **Close-out** — visit /dispatch/close/[id]; fill Hobbs in, route, one squawk (severity grounding); submit with sign-off; reservation moves to closed; aircraft now blocked from future bookings via is_airworthy_at
    12. **Passenger manifest** — open the print preview at /dispatch/manifest/[id]; verify print layout
    13. **Banned-term sweep** — scan the rendered UI for the words "Approved" / "Approve" — none should appear (data values are fine; display labels must read "Confirmed" / "Confirm")
    14. **Cross-tenant** — log in as a user from a different school; verify zero Phase 3 data leaks

    Expected: every step succeeds. Any failure = note it and resume signal "issues: <list>".
  </how-to-verify>
  <resume-signal>Type "approved" (the user-side approval for the checkpoint, not a banned UI string) or list issues to triage</resume-signal>
</task>

</tasks>

<verification>
- All Phase 3 requirements (23 total) demonstrated
- No banned-term lint regressions
- Full RLS suite green
- Live stack walkthrough complete
</verification>

<success_criteria>
Phase 3 end-to-end functional: reservations, exclusion constraints, dispatch, close-out, FIF, manifest, recurring, blocks, unavailability, airworthiness gate. Every Phase 3 requirement (SCH-01..18 subset, INS-04, FLT-04, FTR-01..08) is observable through the running app.
</success_criteria>

<output>
After completion, create `.planning/phases/03-scheduling-dispatch-execution/03-05-SUMMARY.md` and mark Phase 3 complete in ROADMAP.md
</output>
