---
phase: 03-scheduling-dispatch-execution
plan: 04
type: execute
wave: 4
depends_on: ["03-03"]
files_modified:
  - apps/web/app/(app)/dispatch/page.tsx
  - apps/web/app/(app)/dispatch/DispatchBoard.tsx
  - apps/web/app/(app)/dispatch/DispatchModal.tsx
  - apps/web/app/(app)/dispatch/PassengerManifestPanel.tsx
  - apps/web/app/(app)/dispatch/FifGate.tsx
  - apps/web/app/(app)/dispatch/OverdueAlarm.tsx
  - apps/web/app/(app)/dispatch/close/[id]/page.tsx
  - apps/web/app/(app)/dispatch/close/[id]/CloseOutForm.tsx
  - apps/web/app/(app)/dispatch/manifest/[id]/page.tsx
  - apps/web/components/dispatch/StudentCheckInButton.tsx
  - apps/web/public/sounds/overdue.mp3
autonomous: true
requirements:
  - SCH-08
  - SCH-09
  - INS-04
  - FTR-01
  - FTR-02
  - FTR-03
  - FTR-04
  - FTR-05
  - FTR-06
  - FTR-08

must_haves:
  truths:
    - "Visiting /dispatch shows three live panels (currently flying / about to fly / recently closed) refreshing every 15 seconds"
    - "Clicking Dispatch on an approved reservation opens a modal that requires student check-in, instructor authorization, FIF acks, Hobbs/tach out (flight only), and passenger manifest (if any)"
    - "An overdue reservation row turns red and plays an audio cue ONCE per new overdue (sessionStorage diff)"
    - "Visiting /dispatch/close/[id] shows the close-out form; instructor sign-off transitions reservation to closed; student-only save transitions to pending_sign_off"
    - "Visiting /dispatch/manifest/[id] renders a print-friendly passenger manifest"
    - "Student can click 'Check in' on their own reservation card 15 minutes before start"
  artifacts:
    - path: "apps/web/app/(app)/dispatch/DispatchBoard.tsx"
      provides: "Three-panel live board with TanStack Query refetchInterval=15000"
    - path: "apps/web/app/(app)/dispatch/DispatchModal.tsx"
      provides: "Multi-step gate: student present → instructor authorize → FIF acks → Hobbs/tach → manifest → submit"
    - path: "apps/web/app/(app)/dispatch/close/[id]/CloseOutForm.tsx"
      provides: "Hobbs/tach in, fuel/oil, route, squawks, notes, sign-off"
  key_links:
    - from: "DispatchBoard.tsx"
      to: "trpc.dispatch.list"
      via: "useQuery refetchInterval 15000"
      pattern: "refetchInterval"
    - from: "DispatchModal.tsx"
      to: "trpc.dispatch.dispatchReservation"
      via: "mutation after FIF gate + Hobbs capture"
      pattern: "dispatchReservation"
    - from: "OverdueAlarm.tsx"
      to: "sessionStorage seen-set + audio play"
      via: "diff against previous render"
      pattern: "sessionStorage.*overdue"
---

<objective>
The dispatch screen is the day-of-work hub. Three-panel live board polling every 15s, dispatch modal that gates on student check-in + instructor authorization + FIF acks + Hobbs capture, overdue alarm with audio cue, close-out form that consolidates Hobbs/tach in + fuel/oil + route + squawks + sign-off, passenger manifest panel + print preview.

Purpose: The school can run its day on this screen.
Output: /dispatch, /dispatch/close/[id], /dispatch/manifest/[id], student check-in button on the schedule page.
</objective>

<execution_context>
@/Users/christopher/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christopher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-scheduling-dispatch-execution/03-CONTEXT.md
@.planning/phases/03-scheduling-dispatch-execution/03-02-PLAN.md
@.planning/phases/03-scheduling-dispatch-execution/03-03-PLAN.md

@apps/web/app/(app)/admin/aircraft/[id]/FlightLogEntryForm.tsx
@apps/web/app/(app)/schedule/Calendar.tsx
@CLAUDE.md

<interfaces>
trpc.dispatch.list returns:
  { currentlyFlying: Reservation[], aboutToFly: Reservation[], recentlyClosed: Reservation[] }
  Each Reservation has computed `isOverdue: boolean` and `overdueByMinutes: number`

Audio autoplay caveat: browsers require a user gesture before audio.play() works. Solution per research: on first user click anywhere on /dispatch, prime an AudioContext; subsequent overdue alarms can play. Prompt the user with a "Enable alarm sounds" button if AudioContext is suspended.

Phase 1/2 banned-term reminder: "Approve" / "Approved" banned in JSX. Use "Confirm" / "Confirmed" / "Authorize" instead.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: /dispatch board + DispatchModal + OverdueAlarm + StudentCheckInButton</name>
  <files>apps/web/app/(app)/dispatch/page.tsx, apps/web/app/(app)/dispatch/DispatchBoard.tsx, apps/web/app/(app)/dispatch/DispatchModal.tsx, apps/web/app/(app)/dispatch/FifGate.tsx, apps/web/app/(app)/dispatch/OverdueAlarm.tsx, apps/web/app/(app)/dispatch/PassengerManifestPanel.tsx, apps/web/components/dispatch/StudentCheckInButton.tsx, apps/web/public/sounds/overdue.mp3</files>
  <behavior>
    - /dispatch page.tsx: server-side role check (instructor or admin); renders DispatchBoard
    - DispatchBoard ('use client'): three columns (Currently Flying / About to Fly / Recently Closed); TanStack Query useQuery({ queryFn: trpc.dispatch.list, refetchInterval: 15000 }); each row shows aircraft tail, student, instructor, dispatched-at, expected-end, overage badge
    - Overdue rows render red; OverdueAlarm component watches for newly-overdue rows and plays audio.mp3 once per id (sessionStorage seen-set)
    - DispatchModal: opens from "Dispatch" button on About-to-Fly rows; multi-step:
      1. Student check-in (shows student name + "Mark present" button if not yet checked in, or green checkmark)
      2. FifGate: lists unacked active FIF notices; user must click "I have read" on each before proceeding
      3. Instructor authorization (button "Authorize Release")
      4. Hobbs / tach out fields (flight only)
      5. Passenger manifest panel (PIC auto-filled, add additional pax)
      6. Submit → calls dispatch.dispatchReservation
    - StudentCheckInButton: rendered on the student's own reservation card on /schedule; appears 15min before start; calls dispatch.markStudentPresent
    - All button labels avoid banned terms — use "Confirm", "Authorize", "Dispatch", "Acknowledge"
  </behavior>
  <action>
    Reuse FlightLogEntryForm.tsx layout for the Hobbs/tach inputs. For the audio cue use a tiny generated beep (or commit a short MP3 to public/sounds/overdue.mp3 — a 1KB simple tone). For AudioContext gating, render a small "Enable sound alerts" toggle in the page header that primes the context. Run lint after writing.
  </action>
  <verify>
    <automated>. scripts/dev-env.sh && pnpm --filter ./apps/web lint && pnpm --filter ./apps/web build</automated>
  </verify>
  <done>/dispatch builds and lints; modal walks through all gates; overdue alarm logic implemented with sessionStorage diff</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: /dispatch/close/[id] CloseOutForm + /dispatch/manifest/[id] print preview</name>
  <files>apps/web/app/(app)/dispatch/close/[id]/page.tsx, apps/web/app/(app)/dispatch/close/[id]/CloseOutForm.tsx, apps/web/app/(app)/dispatch/manifest/[id]/page.tsx</files>
  <behavior>
    - /dispatch/close/[id] page: server fetches reservation; renders CloseOutForm
    - CloseOutForm ('use client'): sections — Times (Hobbs in, tach in), Fuel/Oil (gal, qt), Route (text, defaulted from XC plan), Squawks (dynamic add: title, description, severity), Notes, Sign-off button (instructor only) and Save-draft button
    - On submit calls trpc.dispatch.closeOut; on success redirects back to /dispatch
    - "Grade lesson" placeholder button rendered disabled with tooltip "Coming in Phase 5"
    - /dispatch/manifest/[id]: prints the passenger manifest in a print-friendly layout (Tailwind print: classes); single-page, large legible font, includes flight details + pax list with weights + emergency contacts
  </behavior>
  <action>
    Standard react-hook-form pattern. For dynamic squawk rows use useFieldArray. Print page uses a minimal layout with print: utilities and a "Print" button that calls window.print(). No PDF library needed in v1.
  </action>
  <verify>
    <automated>. scripts/dev-env.sh && pnpm --filter ./apps/web lint && pnpm --filter ./apps/web build</automated>
  </verify>
  <done>Close-out form complete; manifest print preview renders; build clean</done>
</task>

</tasks>

<verification>
- `. scripts/dev-env.sh && pnpm -r lint` clean
- `. scripts/dev-env.sh && pnpm --filter ./apps/web build` clean
- `. scripts/dev-env.sh && pnpm -r typecheck` clean
</verification>

<success_criteria>
Dispatch screen is alive: polling, gating, overdue alarms, close-out workflow, passenger manifest. Phase 3 functionality is now complete; only FIF admin UI + final verification remain.
</success_criteria>

<output>
After completion, create `.planning/phases/03-scheduling-dispatch-execution/03-04-SUMMARY.md`
</output>
