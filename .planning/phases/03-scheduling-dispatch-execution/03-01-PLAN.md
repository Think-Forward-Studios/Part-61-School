---
phase: 03-scheduling-dispatch-execution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/db/src/schema/reservations.ts
  - packages/db/src/schema/schedule_blocks.ts
  - packages/db/src/schema/rooms.ts
  - packages/db/src/schema/squawks.ts
  - packages/db/src/schema/fif.ts
  - packages/db/src/schema/passenger_manifest.ts
  - packages/db/src/schema/person_unavailability.ts
  - packages/db/src/schema/enums.ts
  - packages/db/src/schema/aircraft.ts
  - packages/db/src/schema/flight_log.ts
  - packages/db/src/schema/index.ts
  - packages/db/src/schema/views.ts
  - packages/db/migrations/0002_phase3_scheduling.sql
  - supabase/migrations/20260408000000_phase3_scheduling.sql
  - tests/rls/phase3-scheduling.test.ts
  - tests/rls/phase3-exclusion-concurrency.test.ts
  - tests/rls/cross-tenant.test.ts
autonomous: true
requirements:
  - SCH-01
  - SCH-02
  - SCH-04
  - SCH-13
  - SCH-15
  - SCH-16
  - SCH-18
  - FLT-04
  - FTR-03
  - FTR-05
  - FTR-06
  - FTR-07

must_haves:
  truths:
    - "Two overlapping confirmed reservations on same aircraft/instructor/student/room are rejected by Postgres exclusion constraint"
    - "Concurrent inserts of overlapping approved reservations: exactly one succeeds, the other raises 23P01"
    - "is_airworthy_at(aircraft_id, ts) returns false for grounded aircraft and aircraft with open grounding squawks"
    - "Inserting a person_unavailability row creates a shadow reservation that blocks bookings for that person"
    - "Reservation rows are RLS-isolated by school_id; cross-tenant reads return zero rows"
    - "aircraft_current_totals view returns correct totals for both legacy 'flight' rows and new flight_out/flight_in pairs"
  artifacts:
    - path: "packages/db/src/schema/reservations.ts"
      provides: "reservation table + status/activity_type enums + tstzrange column"
    - path: "packages/db/migrations/0002_phase3_scheduling.sql"
      provides: "DDL for all Phase 3 tables, btree_gist extension, four exclusion constraints, is_airworthy_at function, shadow-row trigger, view update"
    - path: "tests/rls/phase3-exclusion-concurrency.test.ts"
      provides: "Concurrent overlap test proving the constraint bites"
  key_links:
    - from: "reservation"
      to: "btree_gist exclusion constraint"
      via: "EXCLUDE USING gist (aircraft_id WITH =, time_range WITH &&) WHERE status IN ('approved','dispatched','flown')"
      pattern: "exclude using gist"
    - from: "person_unavailability BEFORE INSERT trigger"
      to: "reservation shadow row"
      via: "trigger inserts misc/approved reservation"
      pattern: "create trigger.*person_unavailability"
    - from: "is_airworthy_at()"
      to: "aircraft.grounded_at + aircraft_squawk.severity='grounding'"
      via: "SQL function body"
      pattern: "create.*function.*is_airworthy_at"
---

<objective>
Land the entire Phase 3 schema in one migration: reservation, schedule_block, schedule_block_instance, room, aircraft_squawk, fif_notice, fif_acknowledgement, passenger_manifest, person_unavailability. Add btree_gist + four partial EXCLUDE constraints. Add is_airworthy_at stub + minimal squawk path. Extend Phase 2 flight_log_entry kind enum with flight_out/flight_in and update aircraft_current_totals. Wire shadow-row trigger for unavailability. Ship cross-tenant + concurrency tests against the live local Supabase stack.

Purpose: Every later plan depends on this schema. Get the constraints right and the rest of the phase becomes typed glue.
Output: Migration applied, drizzle schema in sync, tests green.
</objective>

<execution_context>
@/Users/christopher/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christopher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-scheduling-dispatch-execution/03-CONTEXT.md
@.planning/phases/03-scheduling-dispatch-execution/03-RESEARCH.md
@.planning/phases/02-personnel-admin-fleet-primitives/02-04-SUMMARY.md

@packages/db/src/schema/aircraft.ts
@packages/db/src/schema/flight_log.ts
@packages/db/src/schema/views.ts
@packages/db/src/schema/enums.ts
@packages/db/src/schema/index.ts
@packages/db/src/schema/holds.ts
@packages/db/src/schema/no_show.ts
@tests/rls/cross-tenant.test.ts

<interfaces>
Phase 2 patterns to mirror exactly:
- pgPolicy `to:` is a STRING literal (NOT sql`authenticated`)
- Views use `WITH (security_invoker = true)`
- audit.attach('table_name') in the migration for every safety-relevant table
- fn_block_hard_delete trigger on training-record-relevant tables
- Hand-authored migration mirrored to supabase/migrations/
- Cross-tenant test pattern in tests/rls/cross-tenant.test.ts uses raw postgres-js with request.jwt.claims GUC
- Phase 2 flight_log_entry exists with `kind` enum (currently: baseline | correction | flight). Extend the enum, do NOT recreate.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Drizzle schema for all Phase 3 tables + enum extensions</name>
  <files>packages/db/src/schema/reservations.ts, packages/db/src/schema/schedule_blocks.ts, packages/db/src/schema/rooms.ts, packages/db/src/schema/squawks.ts, packages/db/src/schema/fif.ts, packages/db/src/schema/passenger_manifest.ts, packages/db/src/schema/person_unavailability.ts, packages/db/src/schema/enums.ts, packages/db/src/schema/aircraft.ts, packages/db/src/schema/flight_log.ts, packages/db/src/schema/index.ts</files>
  <behavior>
    - reservation table exports with all columns from CONTEXT.md decisions section: id, school_id, base_id, activity_type, time_range (tstzrange), status, aircraft_id, instructor_id, student_id, room_id, series_id, parent_block_id, notes, requested_at/by, approved_at/by, dispatched_at/by, closed_at/by, close_out_reason, deleted_at, student_checked_in_at/by, instructor_authorized_at/by, route_string, ete_minutes, stops, fuel_stops, alternate
    - status enum includes pending_sign_off
    - close_out_reason enum: cancelled_free | cancelled_late | no_show | scrubbed_weather | scrubbed_maintenance | scrubbed_other
    - schedule_block + schedule_block_instance tables (materialized children)
    - room table with features text[]
    - aircraft_squawk table with severity enum (info | watch | grounding)
    - fif_notice + fif_acknowledgement (unique on notice_id, user_id)
    - passenger_manifest with position enum (pic | sic | passenger)
    - person_unavailability with kind enum (vacation | sick | personal | training | other)
    - aircraft.ts extended with grounded_at timestamptz nullable
    - flight_log.ts kind enum extended with flight_out, flight_in (keep flight as deprecated alias); add paired_entry_id self-FK nullable
    - All tables have school_id, RLS-eligible, deleted_at where appropriate
    - All exported from index.ts
  </behavior>
  <action>
    Create the listed schema files following the Phase 2 pattern in packages/db/src/schema/personnel.ts and aircraft.ts. Use pgPolicy with `to:` as a STRING literal. Use tstzrange via `customType` if Drizzle lacks a native helper — match Phase 2's pattern for postgres-specific types. RLS policies: school_id from auth.jwt(), base_id from current_setting('app.base_id', true) where applicable. Export every table from index.ts. Run `. scripts/dev-env.sh && pnpm --filter @part61/db typecheck` to validate.
  </action>
  <verify>
    <automated>. scripts/dev-env.sh && pnpm --filter @part61/db typecheck</automated>
  </verify>
  <done>All schema files compile, exported from index.ts, no TypeScript errors</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Hand-authored SQL migration with constraints, triggers, function, view update</name>
  <files>packages/db/migrations/0002_phase3_scheduling.sql, supabase/migrations/20260408000000_phase3_scheduling.sql</files>
  <behavior>
    - `create extension if not exists btree_gist` at top
    - DDL for every Phase 3 table matching the Drizzle schema
    - ALTER TABLE aircraft ADD COLUMN grounded_at timestamptz
    - ALTER TYPE flight_log_entry_kind ADD VALUE 'flight_out'; ADD VALUE 'flight_in'
    - ALTER TABLE flight_log_entry ADD COLUMN paired_entry_id uuid REFERENCES flight_log_entry(id)
    - DROP + recreate aircraft_current_totals view to handle baseline + correction + flight + flight_out/flight_in pair (sum hobbs_in - hobbs_out for paired rows; preserve legacy single 'flight' kind handling)
    - View has WITH (security_invoker = true)
    - Four EXCLUDE USING gist constraints on reservation (aircraft_id, instructor_id, student_id, room_id), each gated by `WHERE (<col> is not null AND status IN ('approved','dispatched','flown'))`
    - is_airworthy_at(aircraft_id uuid, at timestamptz) RETURNS boolean: false if aircraft.deleted_at, aircraft.grounded_at <= at, OR EXISTS open grounding squawk at that time
    - person_unavailability BEFORE INSERT trigger that inserts a matching reservation row (activity_type='misc', status='approved', instructor_id or student_id from NEW, time_range from NEW, notes='unavailable: '||NEW.reason). Symmetric DELETE trigger removes the shadow.
    - schedule_block_instance → reservation parent_block_id inflate trigger (BEFORE INSERT on reservation: if parent_block_id set and instructor/aircraft/room null, copy from block instance)
    - RLS policies for every new table (school_id + base_id where applicable). pgPolicy `to:` STRING.
    - audit.attach('reservation'), audit.attach('aircraft_squawk'), audit.attach('fif_notice'), audit.attach('passenger_manifest'), audit.attach('person_unavailability'), audit.attach('schedule_block')
    - fn_block_hard_delete attached to reservation, aircraft_squawk, fif_notice, passenger_manifest, schedule_block, person_unavailability
    - Mirror file copied to supabase/migrations/ with the YYYYMMDDHHMMSS prefix
  </behavior>
  <action>
    Hand-author the SQL migration. Reference packages/db/migrations/0001_*.sql (Phase 2) for the audit.attach + fn_block_hard_delete + RLS policy pattern. After writing, run `. scripts/dev-env.sh && supabase db reset` to apply. Then run `. scripts/dev-env.sh && pnpm --filter @part61/db drizzle-kit generate --name phase3_diff_check` and diff the generated DDL against your hand-authored DDL portion — they should match for table/column definitions (triggers/constraints/policies are hand-only). Delete the diff file after verification.

    NOTE: btree_gist + tstzrange exclusion is the load-bearing piece. Test it manually in psql before moving on:
    ```
    INSERT INTO reservation (..., status='approved', time_range='[2026-05-01 10:00, 2026-05-01 12:00)');
    -- Second insert with overlapping range on same aircraft must raise 23P01
    ```
  </action>
  <verify>
    <automated>. scripts/dev-env.sh && supabase db reset && psql "$DIRECT_DATABASE_URL" -c "select 1 from pg_constraint where conname='reservation_aircraft_no_overlap'" | grep -q '1 row'</automated>
  </verify>
  <done>Migration applies cleanly via supabase db reset; all four exclusion constraints present; is_airworthy_at function defined; shadow-row trigger present</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Cross-tenant RLS tests + concurrent-insert exclusion test</name>
  <files>tests/rls/phase3-scheduling.test.ts, tests/rls/phase3-exclusion-concurrency.test.ts, tests/rls/cross-tenant.test.ts</files>
  <behavior>
    - phase3-scheduling.test.ts: for each new table (reservation, schedule_block, room, aircraft_squawk, fif_notice, fif_acknowledgement, passenger_manifest, person_unavailability) seed two schools, insert as school A, attempt read as school B → expect zero rows
    - phase3-scheduling.test.ts: is_airworthy_at(grounded_aircraft, now()) returns false; is_airworthy_at(active_aircraft, now()) returns true; is_airworthy_at(aircraft_with_open_grounding_squawk, now()) returns false
    - phase3-scheduling.test.ts: inserting a person_unavailability row creates a corresponding shadow reservation with status='approved' and matching time_range
    - phase3-scheduling.test.ts: aircraft_current_totals view returns correct delta when a flight_out/flight_in pair exists (e.g. hobbs_out=100.0, hobbs_in=101.5 → delta 1.5 added to baseline)
    - phase3-exclusion-concurrency.test.ts: open two concurrent transactions, both insert overlapping approved reservations on the same aircraft, commit both → exactly one succeeds, the other raises Postgres error code 23P01 (exclusion violation)
    - cross-tenant.test.ts: extended with phase 3 table assertions (one assertion per new table)
  </behavior>
  <action>
    Use the Phase 2 RLS harness pattern from tests/rls/harness.ts (seedTwoSchools, asUserOf, raw postgres-js with request.jwt.claims GUC). For the concurrency test, use two separate postgres-js clients in two parallel `await` blocks, each inside a transaction; expect exactly one to throw with code 23P01. Run with `. scripts/dev-env.sh && pnpm --filter @part61/rls-tests test`.
  </action>
  <verify>
    <automated>. scripts/dev-env.sh && pnpm --filter @part61/rls-tests test -- phase3</automated>
  </verify>
  <done>All Phase 3 RLS tests green; concurrent-insert test proves exclusion constraint bites; cross-tenant test extended for new tables; full suite still passes</done>
</task>

</tasks>

<verification>
- `. scripts/dev-env.sh && pnpm -r typecheck` clean
- `. scripts/dev-env.sh && supabase db reset` clean
- `. scripts/dev-env.sh && pnpm --filter @part61/rls-tests test` — all tests including new phase3 ones pass
- btree_gist extension present, four exclusion constraints present, is_airworthy_at function present
</verification>

<success_criteria>
Schema landed, migration deterministic, exclusion constraints proven by concurrent test, shadow-row trigger proven by unit test, RLS proven by cross-tenant test, view handles new flight_out/flight_in pairs.
</success_criteria>

<output>
After completion, create `.planning/phases/03-scheduling-dispatch-execution/03-01-SUMMARY.md`
</output>
