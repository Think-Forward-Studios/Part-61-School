---
phase: 08-experience-reporting-messaging-beta
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/db/src/schema/notification.ts
  - packages/db/src/schema/messaging.ts
  - packages/db/src/schema/session_activity.ts
  - packages/db/src/schema/school_rate.ts
  - packages/db/src/schema/index.ts
  - packages/db/src/schema/grading.ts
  - packages/db/migrations/0032_phase8_notifications.sql
  - packages/db/migrations/0033_phase8_messaging.sql
  - packages/db/migrations/0034_phase8_cost_and_rates.sql
  - packages/db/migrations/0035_phase8_pg_cron_reminders.sql
  - packages/db/migrations/0036_phase8_stage_check_faa_flag.sql
  - packages/db/migrations/0037_phase8_dispatch_cue_cron.sql
  - packages/api/src/helpers/notifications.ts
  - packages/api/src/routers/notifications.ts
  - packages/api/src/routers/messaging.ts
  - packages/api/src/routers/broadcasts.ts
  - packages/api/src/routers/admin/activeSessions.ts
  - packages/api/src/routers/_root.ts
  - apps/web/package.json
  - apps/web/emails/_components/EmailShell.tsx
  - apps/web/emails/ReservationRequested.tsx
  - apps/web/emails/ReservationApproved.tsx
  - apps/web/emails/ReservationChanged.tsx
  - apps/web/emails/ReservationCancelled.tsx
  - apps/web/emails/ReservationReminder24h.tsx
  - apps/web/emails/GradingComplete.tsx
  - apps/web/emails/SquawkOpened.tsx
  - apps/web/emails/SquawkGrounding.tsx
  - apps/web/emails/SquawkReturnedToService.tsx
  - apps/web/emails/DocumentExpiring.tsx
  - apps/web/emails/CurrencyExpiring.tsx
  - apps/web/emails/AdminBroadcast.tsx
  - apps/web/emails/DutyHourWarning.tsx
  - apps/web/emails/_loader.ts
  - apps/web/app/api/emails/send/route.ts
  - apps/web/lib/supabase/middleware.ts
  - apps/web/.env.example
  - packages/api/src/routers/schedule/reservations.ts
  - packages/api/src/routers/gradeSheet.ts
  - packages/api/src/routers/admin/squawks.ts
  - packages/api/src/routers/admin/stageChecks.ts
  - tests/rls/phase8-notifications.test.ts
  - tests/rls/phase8-messaging.test.ts
  - packages/api/src/routers/admin/_root.ts
  - packages/api/src/routers/__tests__/notifications.router.test.ts
  - packages/api/src/routers/__tests__/messaging.router.test.ts
  - packages/api/src/routers/__tests__/broadcasts.router.test.ts
autonomous: true
requirements:
  - SCH-10
  - NOT-01
  - NOT-02
user_setup:
  - service: resend
    why: 'Transactional email provider for notifications (SCH-10, NOT-02)'
    env_vars:
      - name: RESEND_API_KEY
        source: 'Resend Dashboard -> API Keys (https://resend.com/api-keys). Free tier: 3,000/mo, 100/day.'
      - name: RESEND_FROM_EMAIL
        source: 'Must match a verified sender domain in Resend (e.g. `noreply@yourschool.test`). For local dev, `onboarding@resend.dev` works without verification.'
      - name: INTERNAL_WORKER_SECRET
        source: 'Any random long string. Claude generates one automatically in `.env.example` — user must copy to their `.env.local` and to the deploy environment.'
    dashboard_config:
      - task: 'Verify sender domain in Resend'
        location: 'Resend Dashboard -> Domains -> Add Domain -> copy DKIM + SPF DNS records to your registrar'
      - task: 'Keep `RESEND_API_KEY` out of commits'
        location: '.env.local (git-ignored)'

must_haves:
  truths:
    - "A tRPC mutation that calls createNotification() atomically inserts a notifications row and (if email enabled) an email_outbox row inside the caller's transaction"
    - 'Inserting a notifications row fires a Supabase Realtime postgres_changes event that the browser receives'
    - 'The email dispatcher route handler claims pending rows with FOR UPDATE SKIP LOCKED, sends via Resend, and marks them sent (or re-queues on transient failure up to 5 attempts)'
    - "A pg_cron job every 5 minutes inserts reservation_reminder_24h notifications for approved/dispatched reservations starting in ~24h that don't yet have a reminder"
    - 'User notification preferences per (kind, channel) override the role-based defaults; safety-critical kinds always deliver in-app regardless of prefs'
    - 'Broadcast creation fans out one notifications row per target-role recipient in the same transaction as the broadcast row insert'
    - 'user_session_activity is upserted by the Next.js auth middleware on each request at most once per 60 seconds per user'
    - 'stage_check has is_faa_checkride + attempt_number columns so IPF-03 pass rate can be computed'
    - "A pg_cron job (migration 0037) every 1 minute inserts channel='dispatch' kind='overdue_aircraft' notifications for flight reservations past their expected ramp-in that don't yet have a dispatch cue in the last 5 minutes"
  artifacts:
    - path: 'packages/db/migrations/0032_phase8_notifications.sql'
      provides: 'notifications, user_notification_pref, notification_default_by_role, email_outbox tables + RLS + realtime publication registration'
      contains: 'alter publication supabase_realtime add table public.notifications'
    - path: 'packages/db/migrations/0033_phase8_messaging.sql'
      provides: 'conversation, message, message_read, broadcast, broadcast_read tables + RLS + realtime publication registration for message + broadcast'
      contains: 'create table public.conversation'
    - path: 'packages/db/migrations/0034_phase8_cost_and_rates.sql'
      provides: 'school_rate table + user_session_activity table + RLS'
      contains: 'create table public.school_rate'
    - path: 'packages/db/migrations/0035_phase8_pg_cron_reminders.sql'
      provides: 'pg_cron jobs — reservation 24h reminder (every 5 min) + email outbox drainer (every 1 min) + outbox retention cleanup (nightly)'
      contains: 'cron.schedule'
    - path: 'packages/db/migrations/0036_phase8_stage_check_faa_flag.sql'
      provides: 'is_faa_checkride boolean, attempt_number int columns on stage_check'
      contains: 'alter table public.stage_check add column is_faa_checkride'
    - path: 'packages/db/migrations/0037_phase8_dispatch_cue_cron.sql'
      provides: 'pg_cron job (every 1 min) that inserts dispatch-channel notifications for overdue_aircraft on reservations past their expected ramp-in without an existing recent dispatch cue'
      contains: 'cron.schedule.*phase8_dispatch_cue'
    - path: 'packages/api/src/helpers/notifications.ts'
      provides: 'createNotification(tx, opts) helper used by all mutations that need to emit events'
      exports: ['createNotification', 'NotificationEventKind']
    - path: 'apps/web/app/api/emails/send/route.ts'
      provides: 'Next.js Route Handler (nodejs runtime) that drains email_outbox and sends via Resend'
      exports: ['POST', 'runtime']
    - path: 'apps/web/emails/_loader.ts'
      provides: 'loadEmailTemplate(templateKey, props) — template-key to React Email component registry'
      exports: ['loadEmailTemplate']
  key_links:
    - from: 'packages/api/src/routers/schedule/reservations.ts (request/approve/change/cancel)'
      to: 'packages/api/src/helpers/notifications.ts'
      via: "await createNotification(tx, { kind: 'reservation_*', ... })"
      pattern: "createNotification\\(tx"
    - from: 'packages/api/src/routers/gradeSheet.ts (sign/finalize)'
      to: 'packages/api/src/helpers/notifications.ts'
      via: "await createNotification(tx, { kind: 'grading_complete', ... })"
      pattern: "createNotification\\(tx.*grading_complete"
    - from: 'packages/api/src/routers/admin/squawks.ts'
      to: 'packages/api/src/helpers/notifications.ts'
      via: "await createNotification(tx, { kind: 'squawk_*', ... })"
      pattern: "createNotification\\(tx.*squawk"
    - from: 'apps/web/app/api/emails/send/route.ts'
      to: 'resend.emails.send'
      via: 'Resend SDK'
      pattern: "resend\\.emails\\.send"
    - from: 'packages/db/migrations/0035_phase8_pg_cron_reminders.sql'
      to: 'public.notifications (reservation_reminder_24h inserts)'
      via: 'pg_cron scheduled INSERT ... SELECT FROM reservation'
      pattern: 'reservation_reminder_24h'
---

<objective>
Build the Phase 8 cross-cutting infrastructure: notifications + email pipeline via Resend, messaging/broadcast data model, active-session tracking, school rates + stage-check FAA-checkride columns for downstream plans. This is the single spine every other Phase 8 surface hangs off.

Purpose: Plans 08-02 (dashboards + messaging UI + dispatch cues), 08-03 (audit UI + cost + instructor metrics), and 08-04 (reports) all depend on the tables, helpers, and env vars this plan lands. Without this, every downstream surface has to invent its own ad-hoc notification path.

Output:

- 5 DB migrations (0032–0036) with full RLS, audit where safety-relevant, realtime publication registration
- Drizzle schema files wired into `packages/db/src/schema/index.ts`
- tRPC routers (notifications, messaging, broadcasts, admin.activeSessions) mounted in `_root.ts`
- `createNotification()` helper + `loadEmailTemplate()` registry
- 13 React Email templates covering all NOT-01 event kinds
- `/api/emails/send` route handler drained by pg_cron
- Middleware extension to upsert `user_session_activity` (throttled 60s)
- 3 existing routers (reservations, gradeSheet, admin.squawks, admin.stageChecks) patched to call `createNotification()` for SCH-10 + NOT-01 event emission
- RLS Vitest tests proving per-user isolation on notifications + messaging
  </objective>

<execution_context>
@/Users/christopher/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christopher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-experience-reporting-messaging-beta/08-CONTEXT.md
@.planning/phases/08-experience-reporting-messaging-beta/08-RESEARCH.md

# Foundation + conventions

@packages/db/src/schema/audit.ts
@packages/db/src/schema/tenancy.ts
@packages/db/src/schema/users.ts
@packages/db/src/schema/user_base.ts
@packages/db/src/schema/grading.ts
@packages/db/src/schema/reservations.ts
@packages/db/src/schema/index.ts
@packages/db/migrations/0029_phase6_pg_cron.sql
@packages/db/migrations/0031_phase7_geofence.sql

# Patterns to follow

@packages/api/src/routers/\_root.ts
@packages/api/src/routers/schedule/reservations.ts
@packages/api/src/routers/gradeSheet.ts
@packages/api/src/routers/admin/squawks.ts
@packages/api/src/routers/admin/stageChecks.ts
@apps/web/lib/supabase/middleware.ts
@apps/web/middleware.ts

# Existing RLS test harness

@tests/rls/harness.ts

<interfaces>
<!-- The executor MUST use these existing types as-is. No codebase exploration required. -->

From packages/db/src/schema/audit.ts (already shipped in Phase 1):

```typescript
// audit_log is append-only via RLS + trigger-populated via audit.fn_log_change()
// New safety-relevant tables should attach:
//   create trigger audit_<table> after insert or update or delete on public.<table>
//     for each row execute function audit.fn_log_change();
// The function reads auth.uid() / app.school_id / app.active_role from session.
```

From packages/api/src/routers/\_root.ts (existing shape):

```typescript
export const appRouter = router({
  me: meRouter,
  auth: authRouter,
  schedule: scheduleRouter,
  dispatch: dispatchRouter,
  gradeSheet: gradeSheetRouter,
  admin: adminRouter,
  // ... add: notifications, messaging, broadcasts here
});
```

From packages/api/src/trpc.ts (pattern — extend if it exists):

```typescript
// Every protected procedure wraps mutation work in withTenantTx(db, userContext, async (tx) => { ... })
// which sets `SET LOCAL app.school_id`, `SET LOCAL app.active_role`, etc.
// createNotification() takes the tx from that closure so the notification insert
// participates in the user-facing mutation's transaction.
```

From packages/db/src/schema/grading.ts — stageCheck table (to be extended):

```typescript
// Add:
//   is_faa_checkride boolean not null default false
//   attempt_number int          (computed by trigger as ROW_NUMBER() per student_enrollment_id, stage_id, ordered by scheduled_at)
// IPF-03 pass rate = count(*) filter(where is_faa_checkride and status='passed' and attempt_number=1)
//                  / count(*) filter(where is_faa_checkride and attempt_number=1)
```

From apps/web/lib/supabase/middleware.ts — updateSession():

```typescript
// Existing: refreshes Supabase cookies, reads session. Extend to also throttled-upsert
// user_session_activity when a valid user + school_id is resolved.
```

Role-based notification defaults (from CONTEXT.md — SEED into notification_default_by_role):

- Student: reservation_requested, reservation_approved, reservation_changed, reservation_cancelled,
  reservation_reminder_24h, grading_complete, squawk_opened (on next aircraft), document_expiring,
  currency_expiring, admin_broadcast -> in_app + email ON
- Instructor: reservation_requested (for assigned lessons), reservation_approved, reservation_changed,
  reservation_reminder_24h, grading_complete (their grades), squawk_opened (assigned aircraft),
  duty_hour_warning, admin_broadcast -> in_app + email ON
- Mechanic: squawk_opened, squawk_grounding, squawk_returned_to_service, admin_broadcast -> in_app + email ON
- Admin: everything above, including overdue_aircraft and grounded_aircraft_attempted_use -> in_app + email ON

Safety-critical kinds (always deliver in-app regardless of prefs; email respects pref per RESEARCH Q6):

- overdue_aircraft
- grounded_aircraft_attempted_use
- squawk_grounding
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Drizzle schema + migrations 0032–0036 + publication + Vitest RLS tests</name>
  <files>
    packages/db/src/schema/notification.ts,
    packages/db/src/schema/messaging.ts,
    packages/db/src/schema/session_activity.ts,
    packages/db/src/schema/school_rate.ts,
    packages/db/src/schema/grading.ts,
    packages/db/src/schema/index.ts,
    packages/db/migrations/0032_phase8_notifications.sql,
    packages/db/migrations/0033_phase8_messaging.sql,
    packages/db/migrations/0034_phase8_cost_and_rates.sql,
    packages/db/migrations/0035_phase8_pg_cron_reminders.sql,
    packages/db/migrations/0036_phase8_stage_check_faa_flag.sql,
    tests/rls/phase8-notifications.test.ts,
    tests/rls/phase8-messaging.test.ts
  </files>
  <action>
    Create the Phase 8 data model. Each migration is hand-authored SQL (not Drizzle Kit generated) to match the existing migration-authoring convention from Phases 2–7 (see 0031_phase7_geofence.sql). Drizzle schema files mirror the SQL for type inference only — the SQL is the source of truth.

    **Migration 0032_phase8_notifications.sql:**
    - Enums: `notification_event_kind` (all 15 kinds listed in RESEARCH Pattern 1 — reservation_requested, reservation_approved, reservation_changed, reservation_cancelled, reservation_reminder_24h, grading_complete, squawk_opened, squawk_grounding, squawk_returned_to_service, document_expiring, currency_expiring, overdue_aircraft, grounded_aircraft_attempted_use, admin_broadcast, duty_hour_warning), `notification_channel` (in_app, email, dispatch).
    - Tables: `public.notifications` (schema per RESEARCH Pattern 1 — one row per (user, channel)), `public.user_notification_pref` (PK on user_id+kind+channel), `public.notification_default_by_role` (PK on role+kind+channel — seeded per CONTEXT), `public.email_outbox` (idempotency_key unique, FOR UPDATE SKIP LOCKED safe).
    - RLS policies per RESEARCH Pattern 1 (select_own, insert_own_school, update_own). notifications is school-scoped AND user-scoped. email_outbox is NOT exposed to authenticated (only the service-role worker reads it) — set `alter table public.email_outbox enable row level security; revoke all on public.email_outbox from authenticated, anon;`.
    - Indexes: `notifications_user_unread_idx` (partial on `read_at is null`), `notifications_school_created_idx`, `email_outbox_status_created_idx`, `user_notification_pref_user_idx`.
    - Publication registration: `alter publication supabase_realtime add table public.notifications; alter table public.notifications replica identity default;` (per RESEARCH Pitfall 3).
    - Audit attach: notifications + user_notification_pref get audit-only trigger (`audit.fn_log_change`). email_outbox does NOT — transient infra table.
    - Seed `notification_default_by_role` with the role-curated defaults from CONTEXT.md (Student/Instructor/Mechanic/Admin) + mark safety-critical kinds with `is_safety_critical = true` on the row.
    - NO `deleted_at` on notifications (transient) or email_outbox. Follows "transient infrastructure" decision.

    **Migration 0033_phase8_messaging.sql:**
    - Tables per RESEARCH Pattern 4: `public.conversation` (deterministic pair key with `userALow < userBHigh` CHECK + unique `(school_id, user_a_low, user_b_high)`), `public.message` (RLS: select/insert only if participant), `public.message_read` (PK on conversation_id+user_id), `public.broadcast` (admin-only create via tRPC), `public.broadcast_read` (dismissal watermark).
    - RLS per RESEARCH Pattern 4.
    - Publication: add message + broadcast to `supabase_realtime`.
    - `deleted_at` on message (safety-relevant — users can rely on evidence). Hard-delete blocker trigger per CLAUDE.md contract.
    - Audit attach on message + broadcast.

    **Migration 0034_phase8_cost_and_rates.sql:**
    - Table `public.school_rate` per RESEARCH Pattern 6 — all fields + `deleted_at` (safety-relevant: cost records affect billing numbers users trust).
    - Table `public.user_session_activity` per RESEARCH Pattern 8 — PK on user_id, upsert-friendly.
    - RLS: school_rate visible to all in school (select), mutate by admin only (enforced at tRPC layer — `withCheck` matches school_id). user_session_activity: user can select own school, upsert self only.
    - school_rate gets audit trigger. user_session_activity does NOT (transient session data, high-frequency writes).
    - Seed: NO default rates — school admin configures in 08-03 UI.

    **Migration 0035_phase8_pg_cron_reminders.sql:**
    - Wrap cron.schedule in DO/EXCEPTION like 0029 (extension may be absent in local dev):
      1. `phase8_reservation_reminder_24h` every 5 min — the SQL from RESEARCH Pitfall 6 (JOIN reservation, filter status in approved/dispatched, 23.5h–24.5h window, `NOT EXISTS` guard on existing reservation_reminder_24h row).
      2. `phase8_email_outbox_drain` every 1 min — calls `net.http_post` to `/api/emails/send` with `x-internal-secret` header from a setting. Use `current_setting('app.internal_worker_secret', true)` with `coalesce(..., '')` — empty secret = skip job (safe default for local dev).
      3. `phase8_email_outbox_retention` at 03:15 daily — delete sent rows older than 30 days, failed rows older than 90 days.
    - The `net.http_post` calls require `pg_net` extension. Add `create extension if not exists pg_net;` with DO/EXCEPTION wrap.
    - The INSERT for reminder must also insert an `email_outbox` row using the same helper logic as `createNotification()` — but in SQL this becomes a straight INSERT that reads `effectivePref` via LEFT JOIN on `user_notification_pref` + `notification_default_by_role`.

    **Migration 0036_phase8_stage_check_faa_flag.sql:**
    - `alter table public.stage_check add column is_faa_checkride boolean not null default false;`
    - `alter table public.stage_check add column attempt_number int;`
    - Backfill `attempt_number` via one-shot UPDATE using `row_number() over (partition by student_enrollment_id, stage_id order by scheduled_at, id)`.
    - Add `NOT NULL` after backfill.
    - Trigger `stage_check_attempt_number_tg` BEFORE INSERT/UPDATE on stage_check (of student_enrollment_id, stage_id, scheduled_at): recompute `NEW.attempt_number` as the count of existing rows for the same enrollment+stage with `scheduled_at < NEW.scheduled_at` + 1.
    - Index: `stage_check_pass_rate_idx on stage_check (school_id, instructor_id, is_faa_checkride, attempt_number, status)` for IPF-03 pass-rate queries.
    - No RLS change (existing stage_check RLS covers both columns).

    **Migration 0037_phase8_dispatch_cue_cron.sql:**
    - `cron.schedule('phase8_dispatch_cue', '*/1 * * * *', $$ ... $$)` — runs every minute.
    - Body: INSERT INTO public.notifications (school_id, user_id, channel, kind, title, body, source_table, source_record_id, severity, created_at) SELECT r.school_id, u.id as user_id, 'dispatch'::notification_channel, 'overdue_aircraft'::notification_event_kind, 'Overdue aircraft — ' || a.tail_number, 'Expected ramp-in at ' || to_char(upper(r.time_range), 'HH24:MI') || '; not yet closed', 'reservation', r.id, 'critical', now() FROM public.reservation r JOIN public.aircraft a ON a.id = r.aircraft_id JOIN public.users u ON u.school_id = r.school_id AND u.active_role IN ('admin','dispatcher','instructor') WHERE r.activity_type = 'flight' AND r.status IN ('approved','dispatched','flown') AND r.deleted_at IS NULL AND upper(r.time_range) < now() - interval '5 minutes' AND r.closed_at IS NULL AND NOT EXISTS ( SELECT 1 FROM public.notifications n WHERE n.source_table = 'reservation' AND n.source_record_id = r.id AND n.kind = 'overdue_aircraft' AND n.channel = 'dispatch' AND n.created_at > now() - interval '5 minutes' );
    - Wrap in DO/EXCEPTION like migrations 0029/0035 so the migration applies cleanly in local dev without pg_cron.
    - Rationale: Phase 7 ADS-B pipeline handles grounded-aircraft-attempted-use emission (08-02 Task 3 wires it into packages/api/src/routers/adsb.ts). This cron fills the complementary overdue_aircraft emitter — keeping ALL Phase 8 migrations owned by this plan (Wave 1).
    - No new indexes — overdue filter uses the existing reservation (school_id, upper(time_range)) index from Phase 3.

    **Drizzle schema files** (packages/db/src/schema/*.ts):
    - notification.ts: export `notifications`, `userNotificationPref`, `notificationDefaultByRole`, `emailOutbox`, `notificationEventKindEnum`, `notificationChannelEnum` — types only, no RLS (RLS lives in SQL migration).
    - messaging.ts: export `conversation`, `message`, `messageRead`, `broadcast`, `broadcastRead`.
    - session_activity.ts: export `userSessionActivity`.
    - school_rate.ts: export `schoolRate`, `rateKindEnum`.
    - grading.ts: ADD `isFaaCheckride` and `attemptNumber` columns to existing `stageCheck` table object.
    - index.ts: re-export all new tables.
    - Use same import style as existing schemas (`import { pgTable, uuid, ... } from 'drizzle-orm/pg-core'`, `import { sql } from 'drizzle-orm'`).

    **RLS Vitest tests** (tests/rls/):
    - `phase8-notifications.test.ts`: create two users in two different schools; insert notifications for each; verify user A cannot select user B's rows across schools AND cannot select user C's rows within the same school. Test update-to-mark-read only works on own rows. Test email_outbox is not readable by `authenticated`.
    - `phase8-messaging.test.ts`: create a conversation between users A and B; verify user A can select + insert messages; user C (not a participant) cannot see the conversation or the messages. Verify broadcast is admin-insertable only (via tRPC, not SQL — test at router layer in task 2). SQL-layer test: participant-only select works.

    **Concurrency note:** migration 0032 must run before 0033 (messaging references users only) — numbering enforces this. 0036 can run any time after stage_check exists (Phase 5) — numbering keeps it last so Phase 6 migrations (0023–0030) are upstream.

    Run `pnpm --filter @part61/db drizzle-kit generate --name phase8_schema_check` in a scratch shell (do NOT commit the diff file) to verify the Drizzle schema and hand-authored SQL agree. If they diverge, record the divergence in the plan summary per 01-02 decision precedent (hand-authored SQL wins, Drizzle schema provides type inference).

  </action>
  <verify>
    <automated>cd packages/db && pnpm typecheck && pnpm exec vitest run tests/rls/phase8-notifications.test.ts tests/rls/phase8-messaging.test.ts</automated>

    Additional manual checks executor must do:
    - `psql "$DATABASE_URL" -c "\d public.notifications"` shows all expected columns + RLS policies
    - `psql "$DATABASE_URL" -c "select * from notification_default_by_role;"` returns seeded rows per role
    - `psql "$DATABASE_URL" -c "select extname from pg_extension where extname='pg_net';"` returns 1 row (or the DO/EXCEPTION wrap logs "skipped")
    - `psql "$DATABASE_URL" -c "select jobname from cron.job where jobname like 'phase8_%';"` lists 4 jobs (phase8_reservation_reminder_24h, phase8_email_outbox_drain, phase8_email_outbox_retention, phase8_dispatch_cue)

  </verify>
  <done>
    6 migrations (0032–0037) apply cleanly from a fresh DB via `pnpm --filter @part61/db migrate`. Drizzle schema compiles under strict TS. RLS tests prove per-user isolation on notifications + messaging. notification_default_by_role seeded with correct role-based defaults. stage_check has is_faa_checkride + attempt_number (backfilled and trigger-maintained). Realtime publication includes notifications, message, broadcast. pg_cron registers 4 Phase 8 jobs (reservation_reminder_24h, email_outbox_drain, email_outbox_retention, dispatch_cue).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: createNotification helper + email worker route + React Email templates + middleware session upsert</name>
  <files>
    apps/web/package.json,
    packages/api/src/helpers/notifications.ts,
    apps/web/emails/_components/EmailShell.tsx,
    apps/web/emails/ReservationRequested.tsx,
    apps/web/emails/ReservationApproved.tsx,
    apps/web/emails/ReservationChanged.tsx,
    apps/web/emails/ReservationCancelled.tsx,
    apps/web/emails/ReservationReminder24h.tsx,
    apps/web/emails/GradingComplete.tsx,
    apps/web/emails/SquawkOpened.tsx,
    apps/web/emails/SquawkGrounding.tsx,
    apps/web/emails/SquawkReturnedToService.tsx,
    apps/web/emails/DocumentExpiring.tsx,
    apps/web/emails/CurrencyExpiring.tsx,
    apps/web/emails/AdminBroadcast.tsx,
    apps/web/emails/DutyHourWarning.tsx,
    apps/web/emails/_loader.ts,
    apps/web/app/api/emails/send/route.ts,
    apps/web/lib/supabase/middleware.ts,
    apps/web/.env.example,
    packages/api/src/helpers/__tests__/notifications.test.ts
  </files>
  <behavior>
    - Test 1 (createNotification honors prefs): given user with `(kind=reservation_approved, channel=email, enabled=false)` pref, calling `createNotification({ kind: 'reservation_approved', ... })` inserts an in_app notifications row but NO email_outbox row.
    - Test 2 (createNotification safety-critical bypass): given user with all prefs disabled, calling `createNotification({ isSafetyCritical: true, kind: 'overdue_aircraft', ... })` still inserts the in_app row. Per RESEARCH Q6, email still respects pref — so no email_outbox row in this case.
    - Test 3 (createNotification falls back to defaults): given a user with NO row in user_notification_pref for the given kind, effective pref is read from notification_default_by_role using the user's active role — if default is enabled, the row is inserted.
    - Test 4 (createNotification transactional): if the outer transaction rolls back, no notifications or email_outbox rows persist.
    - Test 5 (loadEmailTemplate): `loadEmailTemplate('reservation_approved', { studentName, instructorName, aircraftTail, startTimeLocal, reservationUrl })` returns a React element with the expected prop values rendered (use @react-email/render snapshot).
    - Test 6 (email worker idempotency): two POST calls to /api/emails/send with the same pending row produce exactly one `resend.emails.send` call (the second call sees status='sending' or 'sent' and skips). Use a mock Resend client.
    - Test 7 (middleware throttle): calling updateSession twice within 60s for the same user upserts user_session_activity only once.
  </behavior>
  <action>
    **Install deps** (run in the repo root):
    ```bash
    pnpm --filter web add resend @react-email/components @react-email/render
    ```
    Verify `apps/web/package.json` gains `resend`, `@react-email/components`, `@react-email/render`.

    **packages/api/src/helpers/notifications.ts:**
    Export `NotificationEventKind`, `NotificationChannel` type aliases (match the enum values from schema). Export `createNotification(tx, opts)` per RESEARCH Pattern 1 helper pattern. Key specifics:
    - Signature: `createNotification(tx: PgTransaction, opts: { schoolId: string; userId: string; kind: NotificationEventKind; title: string; body: string; linkUrl?: string; sourceTable?: string; sourceRecordId?: string; severity?: 'info'|'warn'|'critical'; isSafetyCritical?: boolean; emailTemplateKey?: string; emailTemplateProps?: Record<string, unknown>; baseId?: string; activeRole?: string; }): Promise<{ notificationId: string | null; emailQueued: boolean }>`.
    - Compute effective pref via SQL that LEFT JOINs `user_notification_pref` onto `notification_default_by_role` using the user's `active_role` from opts (if opts.activeRole omitted, read from `current_setting('app.active_role', true)`).
    - Safety-critical always writes in-app. Safety-critical on email channel still respects pref per RESEARCH Q6.
    - Email outbox idempotency_key = `${notificationId}:${kind}` (per RESEARCH Pattern 1). `ON CONFLICT (idempotency_key) DO NOTHING` to tolerate retries.
    - Return `{ notificationId, emailQueued }` so callers can pass notificationId back to their test fixtures if needed.
    - Do NOT swallow errors — let the transaction roll back.

    **apps/web/emails/*.tsx:**
    One React Email component per template key listed in files_modified.

    **Mid-task self-checkpoint (Warning 1 — planner-recommended):** After writing the first 4 templates (EmailShell + ReservationRequested + ReservationApproved + ReservationReminder24h) and the `_loader.ts` skeleton, manually render one template with test props via `pnpm --filter web exec tsx -e 'import { render } from "@react-email/render"; import { ReservationApproved } from "./apps/web/emails/ReservationApproved"; console.log(await render(<ReservationApproved studentName="A" instructorName="B" aircraftTail="N12345" startTimeLocal="Jun 1 2PM CT" reservationUrl="/schedule/abc" />))'`. Verify the HTML contains "confirmed" and no banned terms. If this fails, fix the pattern BEFORE writing the remaining 9 templates — do not churn through a broken approach. Each follows the same signature pattern as RESEARCH Code Examples §ReservationApproved — `Html/Head/Body/Container` from `@react-email/components`. Use "confirmed" not "approved" in user-facing copy per banned-terms rule (internal enum values stay 'approved'; put `// allow-banned-term: internal enum value` above any `'approved'` literal).
    - EmailShell.tsx: shared header + footer ("Part 61 School · You received this because {event-category} notifications are on." — planner's-discretion utility design per CONTEXT).
    - Each template takes a strongly-typed Props interface. Example Props for ReservationApproved: `{ studentName: string; instructorName: string; aircraftTail: string; startTimeLocal: string; reservationUrl: string; }`.
    - ReservationReminder24h includes an explicit "in ~24 hours" subject-line hint + timezone-local rendering (pass `startTimeLocal` pre-formatted by caller using date-fns-tz).
    - SquawkGrounding marked safety-critical in the copy tone ("Do not fly N12345 — grounded pending repair.").
    - AdminBroadcast Props `{ title: string; body: string; urgency: 'normal' | 'urgent'; senderName: string; }`.

    **apps/web/emails/_loader.ts:**
    ```typescript
    export function loadEmailTemplate(key: string, props: unknown): ReactElement {
      switch (key) {
        case 'reservation_requested':   return <ReservationRequested {...props as any} />;
        case 'reservation_approved':    return <ReservationApproved {...props as any} />;
        // ... all 13 template keys
        default: throw new Error(`Unknown email template: ${key}`);
      }
    }
    ```
    Zod-validate props at runtime? No — templates define their own Props type, caller provides matching shape. Runtime validation overhead not worth it for beta.

    **apps/web/app/api/emails/send/route.ts** per RESEARCH Pattern 1 email dispatcher:
    - `export const runtime = 'nodejs';` (required for @react-email/render)
    - Auth: `x-internal-secret` header must match `INTERNAL_WORKER_SECRET`.
    - Query: `FOR UPDATE SKIP LOCKED` claim up to 50 pending rows.
    - Render each via `loadEmailTemplate` + `render()` from `@react-email/render`, send via `resend.emails.send({ from, to, subject, react: template, headers: { 'X-Entity-Ref-ID': idempotency_key }})`.
    - On success: `status='sent', sent_at=now()`. On failure: increment attempts, set `status='pending'` for retry (or `'failed'` if attempts >= 5) + record error_message.
    - `pnpm --filter web build` must pass — route must not pull Node-only imports into the Edge runtime. Use `postgres` (the pg driver from `drizzle-orm/postgres-js` chain) via `DIRECT_DATABASE_URL`.

    **apps/web/lib/supabase/middleware.ts** — extend `updateSession()`:
    After resolving the user + school_id, upsert `user_session_activity` with throttle:
    ```typescript
    // Cheap throttle via a signed cookie storing last_upsert_ts; only upsert if > 60s.
    const lastUpsert = request.cookies.get('part61.session_activity_ts')?.value;
    const now = Date.now();
    if (!lastUpsert || now - Number(lastUpsert) > 60_000) {
      await supabase.from('user_session_activity').upsert({
        user_id: user.id,
        school_id: claims.school_id,
        last_seen_at: new Date().toISOString(),
        last_seen_ip: request.headers.get('x-forwarded-for')?.split(',')[0] ?? null,
        last_seen_user_agent: request.headers.get('user-agent') ?? null,
        active_role: claims.active_role ?? null,
        active_base_id: request.cookies.get('part61.active_base_id')?.value ?? null,
      });
      response.cookies.set('part61.session_activity_ts', String(now), { path: '/', httpOnly: false, sameSite: 'lax', maxAge: 300 });
    }
    ```
    Wrap in try/catch — session-activity upsert failures must NOT break auth. Log and continue.

    **apps/web/.env.example** — add:
    ```
    # Phase 8 — Resend transactional email (https://resend.com)
    RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxx
    RESEND_FROM_EMAIL=noreply@yourdomain.test   # must match a verified Resend domain; onboarding@resend.dev works in dev
    INTERNAL_WORKER_SECRET=generate-a-64-char-random-string-and-use-the-same-value-in-pg-settings-and-here
    ```
    Also document: `select set_config('app.internal_worker_secret', '<secret>', false);` must be set on the DB so pg_cron can authenticate into the route. Include this in the summary so the user can run it once post-migration.

    **TDD cycle** (packages/api/src/helpers/__tests__/notifications.test.ts):
    Red → green → refactor per RESEARCH standard TDD. Mock the tx object with vitest `vi.fn()` returning canned SQL results. For the email-worker test, mock `Resend` SDK. For the middleware test, mock `NextRequest` + `NextResponse`.

  </behavior>
  <verify>
    <automated>cd apps/web && pnpm typecheck && pnpm lint && pnpm build && cd ../.. && pnpm --filter @part61/api exec vitest run packages/api/src/helpers/__tests__/notifications.test.ts</automated>

    Additional:
    - Visual snapshot of at least one email: `pnpm --filter web exec tsx -e "import { render } from '@react-email/render'; import { ReservationApproved } from './apps/web/emails/ReservationApproved'; console.log(await render(<ReservationApproved studentName='A' instructorName='B' aircraftTail='N12345' startTimeLocal='Jun 1 2PM CT' reservationUrl='/schedule/abc' />))"` — HTML contains "confirmed" and does NOT contain "approved" or "Part 141".
    - `part61/no-banned-terms` ESLint rule passes for `apps/web/emails/**/*.tsx` (it should — templates use "confirmed").

  </verify>
  <done>
    `createNotification(tx, opts)` helper exists + 7 unit tests pass. All 13 React Email templates render without error and use compliant terminology. `/api/emails/send` route builds under Node runtime and drains outbox with a mocked Resend. Middleware upserts user_session_activity at most once per 60s with cookie-based throttle. `.env.example` documents all 3 new env vars.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: tRPC routers (notifications, messaging, broadcasts, admin.activeSessions) + wire into existing reservation/grading/squawk routers</name>
  <files>
    packages/api/src/routers/notifications.ts,
    packages/api/src/routers/messaging.ts,
    packages/api/src/routers/broadcasts.ts,
    packages/api/src/routers/admin/activeSessions.ts,
    packages/api/src/routers/_root.ts,
    packages/api/src/routers/admin/_root.ts,
    packages/api/src/routers/schedule/reservations.ts,
    packages/api/src/routers/gradeSheet.ts,
    packages/api/src/routers/admin/squawks.ts,
    packages/api/src/routers/admin/stageChecks.ts,
    packages/api/src/routers/__tests__/notifications.router.test.ts,
    packages/api/src/routers/__tests__/messaging.router.test.ts,
    packages/api/src/routers/__tests__/broadcasts.router.test.ts
  </files>
  <behavior>
    - notifications.list returns only current user's rows (RLS enforced) ordered by created_at desc.
    - notifications.markRead updates read_at; markAllRead updates all user's unread rows.
    - notifications.unreadCount returns integer count of user's unread rows.
    - notifications.listPrefs returns effective prefs (user override OR default-by-role). updatePref upserts user_notification_pref for (kind, channel) pair.
    - messaging.conversations.list returns conversations where user is participant, ordered by last_message_at desc.
    - messaging.openConversation({ otherUserId }) upserts the canonical conversation row (LEAST/GREATEST) and returns it.
    - messaging.thread.list({ conversationId, cursor }) paginates messages desc, RLS-enforced.
    - messaging.send({ conversationId, body }) inserts a message, updates conversation.last_message_at, fires createNotification for the recipient (kind='admin_broadcast'? no — use a new kind 'direct_message' added to the enum, OR reuse existing without notification; DECIDE: reuse createNotification only for broadcast; IM delivery relies on realtime subscription on `message` table. No notification row per message in v1 — avoids doubling the write path.). Document the decision in the router file header.
    - messaging.markRead({ conversationId }) upserts message_read watermark to now().
    - broadcasts.create (admin-only): inserts broadcast row AND one notifications row per target-role recipient in the same transaction per RESEARCH Q3 transactional fan-out.
    - broadcasts.listActive: returns broadcasts where expires_at is null or > now() AND current user has no broadcast_read row.
    - broadcasts.acknowledge({ broadcastId }): inserts broadcast_read for current user.
    - admin.activeSessions.list: admin-only, returns users with last_seen_at > now() - interval '5 minutes' joined with users for display (email, role, active_base_id).
    - Reservation mutations (request, approve, change, cancel) call createNotification with the matching kind + email template key for the student AND instructor as appropriate.
    - GradeSheet sign/finalize calls createNotification(kind='grading_complete') for the student.
    - Squawks open/ground/RTS call createNotification for affected parties (mechanic queue on open, all instructors scheduled on that aircraft on ground, original opener on RTS).
    - StageCheck.complete: when is_faa_checkride=true and attempt_number=1, the pass rate the instructor's profile reports updates (verified indirectly by querying the pass-rate view in the test).
  </behavior>
  <action>
    **packages/api/src/routers/notifications.ts:**
    Five procedures: `list`, `unreadCount`, `markRead({id})`, `markAllRead`, `listPrefs`, `updatePref({kind, channel, enabled})`. All use `protectedProcedure` (authenticated, same school). Wrap mutation procedures in `withTenantTx`. Follow the shape of existing routers like `packages/api/src/routers/me.ts` for style.

    **packages/api/src/routers/messaging.ts:**
    `conversations.list`, `conversations.open({otherUserId})`, `thread.list({conversationId, cursor?})`, `thread.send({conversationId, body})`, `thread.markRead({conversationId})`. Enforce participant-only at router (RLS catches it too, but early error is better UX). `open` uses `on conflict (school_id, user_a_low, user_b_high) do update set last_message_at=last_message_at returning *` to upsert.

    **packages/api/src/routers/broadcasts.ts:**
    `create({targetRoles, title, body, urgency})` — admin-only via `adminProcedure`. Inside withTenantTx: insert broadcast → SELECT users WHERE role_claim IN targetRoles AND school_id = current → for each, call `createNotification(tx, { kind: 'admin_broadcast', channel: 'in_app', userId, title, body, emailTemplateKey: 'admin_broadcast', emailTemplateProps: {...}, severity: urgency === 'urgent' ? 'critical' : 'info' })`. Also insert a dispatch-channel notification if urgency='urgent' so MSG-04 fires on the dispatch screen.
    - `listActive`: returns broadcasts not yet acknowledged by current user and not expired.
    - `acknowledge({broadcastId})`: insert broadcast_read.

    **packages/api/src/routers/admin/activeSessions.ts:**
    `list()`: admin-only. Returns `{ userId, email, activeRole, activeBaseId, lastSeenAt }[]` where last_seen_at > now() - 5 min. Join `user_session_activity` ↔ `users`.

    **Mount in `_root.ts`:**
    ```typescript
    import { notificationsRouter } from './notifications';
    import { messagingRouter } from './messaging';
    import { broadcastsRouter } from './broadcasts';
    // Under appRouter:
    //   notifications: notificationsRouter,
    //   messaging: messagingRouter,
    //   broadcasts: broadcastsRouter,
    ```
    Add `activeSessions: activeSessionsRouter` under admin in `packages/api/src/routers/admin/_root.ts`.

    **Wire existing routers:**
    - `packages/api/src/routers/schedule/reservations.ts` — patch `request`, `approve`, `changeTime`, `cancel` mutations. After the existing business-data write, inside the same tx, call `createNotification()` for each affected user. Template key maps 1:1 to kind. `linkUrl` = `/schedule/reservation/${id}`.
    - `packages/api/src/routers/gradeSheet.ts` — patch `sign` (or whatever finalizes the sheet — read the existing file to confirm) to fire `grading_complete` notification to the student. `linkUrl` = `/record`.
    - `packages/api/src/routers/admin/squawks.ts` — patch `open`, `ground`, `returnToService` (or actual mutation names — read file) to fire the three squawk kinds.
    - `packages/api/src/routers/admin/stageChecks.ts` — when `complete` or equivalent mutation fires, if `is_faa_checkride` is true, no special notification needed (already covered by grading_complete path). The is_faa_checkride flag is set at stage_check creation time — expose a parameter on the existing create/schedule mutation: `isFaaCheckride: boolean` (default false).

    **Tests** — use the pattern from existing router tests (check for `__tests__` folders in existing routers; if none exist, follow the pattern from tests/rls/harness.ts and spin up a real Postgres via testcontainers-style pattern already used in phase 2). If no such infrastructure exists yet in `packages/api`, use vitest + a mocked tRPC caller (createCallerFactory + mocked context). Each behavior from the behavior block maps to at least one test.

    **Check**: `part61/no-banned-terms` will fire on any user-facing `title`/`body` strings in the notification-creation calls. Use "confirmed" not "approved", "scheduled" not "certified". Audit all notification title/body strings before committing.

  </action>
  <verify>
    <automated>pnpm --filter @part61/api typecheck && pnpm --filter @part61/api exec vitest run packages/api/src/routers/__tests__/notifications.router.test.ts packages/api/src/routers/__tests__/messaging.router.test.ts packages/api/src/routers/__tests__/broadcasts.router.test.ts && pnpm --filter web exec eslint packages/api/src/routers/schedule/reservations.ts packages/api/src/routers/gradeSheet.ts packages/api/src/routers/admin/squawks.ts packages/api/src/routers/admin/stageChecks.ts packages/api/src/routers/notifications.ts packages/api/src/routers/messaging.ts packages/api/src/routers/broadcasts.ts</automated>
  </verify>
  <done>
    tRPC routers mounted + tested. Existing reservation/grading/squawk mutations emit the correct notification kinds atomically with the business mutation. Broadcast fan-out writes N+1 rows (1 broadcast + N notifications) in one transaction. admin.activeSessions returns throttled-last-5-min users. All user-facing strings pass banned-terms lint.
  </done>
</task>

</tasks>

<verification>
Full infra build:
1. `pnpm install` runs clean (new deps install).
2. `pnpm --filter @part61/db migrate` applies 0032–0037 on a fresh DB without error.
3. `pnpm typecheck` (root) passes across monorepo.
4. `pnpm lint` (root) passes — banned terms rule does not fire on email templates.
5. `pnpm test` — all new vitest suites green (RLS + helper + router).
6. `pnpm --filter web build` succeeds — email worker route compiles under Node runtime.

Manual (post-automated):

- `select * from cron.job where jobname like 'phase8_%'` returns 4 rows (reservation_reminder_24h, email_outbox_drain, email_outbox_retention, dispatch_cue).
- Insert a test reservation via tRPC CLI / curl; confirm a notifications row lands AND an email_outbox row lands in the same transaction.
- Post to `/api/emails/send` with the internal secret; confirm the email_outbox row moves `pending` → `sent` (or into Resend's test-domain inbox if keys are set).
- Confirm `pnpm --filter web dev` hot-reloads and the layout still works (middleware extension didn't break auth).
  </verification>

<success_criteria>

- All 3 tasks' `<verify>` commands green
- No banned-term lint violations in email templates or notification titles/bodies
- New RLS tests pass proving per-user + per-school isolation on notifications, messaging
- `createNotification()` helper works atomically within a parent transaction (transactional outbox pattern verified)
- User setup section of frontmatter fully actionable — user can provision Resend + set env vars without coming back with questions
- Downstream plans (08-02, 08-03, 08-04, 08-05) can `import { createNotification } from '@part61/api/helpers/notifications'` and `import { loadEmailTemplate } from '@/emails/_loader'` without further setup
  </success_criteria>

<output>
After completion, create `.planning/phases/08-experience-reporting-messaging-beta/08-01-SUMMARY.md` following the standard SUMMARY template. Must document:
- Any hand-authored-SQL vs Drizzle-generated divergences (per 01-02 precedent)
- Resolution of RESEARCH Open Question #1 (IPF-03 pass rate source) — IS_FAA_CHECKRIDE + ATTEMPT_NUMBER columns added, formula documented for 08-03.
- Resolution of RESEARCH Open Question #2 (notification channel modeling) — one row per channel, no array column.
- Resolution of RESEARCH Open Question #3 (broadcast fan-out strategy) — transactional fan-out for v1, `broadcast_recipient` fanout table deferred.
- Resolution of RESEARCH Open Question #6 (safety-critical email) — safety-critical always in-app; email respects user pref per literal CONTEXT reading; `createNotification()` helper enforces this.
- User-setup TODOs NOT yet complete (Resend domain verification, production secret rotation) — call out in the summary so onboarding runbook in 08-05 can link to them.
- Key interfaces (`createNotification`, `loadEmailTemplate`, tRPC router shapes) for 08-02/03/04/05 executors to reuse.
</output>
