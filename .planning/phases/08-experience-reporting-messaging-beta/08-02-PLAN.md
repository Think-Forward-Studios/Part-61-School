---
phase: 08-experience-reporting-messaging-beta
plan: 2
type: execute
wave: 2
depends_on: ['08-01']
files_modified:
  - apps/web/app/layout.tsx
  - apps/web/app/(app)/layout.tsx
  - apps/web/app/(app)/page.tsx
  - apps/web/app/(app)/dashboard/page.tsx
  - apps/web/app/(app)/dashboard/_components/StudentDashboard.tsx
  - apps/web/app/(app)/dashboard/_components/InstructorDashboard.tsx
  - apps/web/app/(app)/dashboard/_components/MechanicDashboard.tsx
  - apps/web/app/(app)/dashboard/_components/GradingActionDrawer.tsx
  - apps/web/app/(app)/dashboard/_components/ReservationApproveInline.tsx
  - apps/web/app/(app)/profile/notifications/page.tsx
  - apps/web/app/(app)/profile/documents/page.tsx
  - apps/web/components/DashboardTile.tsx
  - apps/web/components/ExpiryBadge.tsx
  - apps/web/components/NotificationBell.tsx
  - apps/web/components/MessagingDrawer.tsx
  - apps/web/components/MessagingDrawerProvider.tsx
  - apps/web/components/BroadcastBanner.tsx
  - apps/web/components/RealtimeUserChannelProvider.tsx
  - apps/web/components/dispatch/CueSubscriber.tsx
  - apps/web/app/(app)/dispatch/page.tsx
  - apps/web/app/(app)/dispatch/DispatchBoard.tsx
  - apps/web/components/__tests__/ExpiryBadge.test.tsx
  - packages/api/src/routers/me.ts
  - packages/api/src/routers/admin/squawks.ts
  - packages/api/src/routers/admin/workOrders.ts
  - packages/api/src/routers/admin/people.ts
  - packages/api/src/routers/adsb.ts
  - apps/web/app/(app)/admin/active-sessions/page.tsx
autonomous: true
requirements:
  - STU-04
  - INS-02
  - INS-03
  - MSG-01
  - MSG-02
  - MSG-03
  - MSG-04

must_haves:
  truths:
    - 'Visiting `/` while authenticated redirects admins to /admin/dashboard and all other roles to /dashboard'
    - 'Student on /dashboard sees a card-grid with 6 tiles: next reservation, syllabus progress, currency, outstanding squawks on next aircraft, expiring documents, document upload'
    - "Instructor on /dashboard sees a card-grid with 6 tiles: today's schedule, assigned students, pending grades, pending stage checks, reservation approval requests, workload ticker"
    - 'An instructor clicking Grade on a pending-grade row opens an in-place drawer with the grade sheet; saving invalidates the pending-grades query'
    - 'An instructor clicking Approve on a reservation approval row optimistically confirms the reservation without a full-page reload'
    - 'A document upload tile on the student dashboard drops a file into Supabase Storage via the existing documents tRPC upload flow and shows expiry'
    - 'ExpiryBadge renders green (>30d), yellow (8–30d), red (≤7d or EXPIRED) and sorts by urgency across all tiles that use it'
    - 'A single Supabase Realtime channel per user (mounted in RealtimeUserChannelProvider at the root layout) feeds the notification bell badge, messaging drawer unread counts, and dispatch cue subscriber'
    - 'The messaging drawer is mounted once at the root layout — navigating between pages preserves its open/closed state and draft text'
    - 'Admin broadcasts show as a dismissible pinned banner at the top of the dashboard; clicking Dismiss inserts a broadcast_read and hides the banner'
    - 'Dispatch screen flashes the affected row (red glow, 60s or until click-to-dismiss) and shows a toast when overdue_aircraft, grounded_aircraft_attempted_use, or urgent admin_broadcast notifications arrive on the dispatch channel'
    - 'User profile notifications page shows a kind × channel matrix with toggles; changes persist via trpc.notifications.updatePref'
  artifacts:
    - path: 'apps/web/app/(app)/page.tsx'
      provides: 'Role-based redirect from / to /dashboard or /admin/dashboard'
      min_lines: 15
    - path: 'apps/web/app/(app)/dashboard/page.tsx'
      provides: 'Role-resolved dashboard entry point'
      min_lines: 20
    - path: 'apps/web/app/(app)/dashboard/_components/StudentDashboard.tsx'
      provides: '6-tile student dashboard'
      min_lines: 80
    - path: 'apps/web/app/(app)/dashboard/_components/InstructorDashboard.tsx'
      provides: '6-tile instructor dashboard with inline drawers'
      min_lines: 100
    - path: 'apps/web/components/MessagingDrawer.tsx'
      provides: 'Right-edge IM drawer with conversation list + active thread'
      min_lines: 100
    - path: 'apps/web/components/NotificationBell.tsx'
      provides: 'Header notification icon + unread badge + dropdown'
      min_lines: 50
    - path: 'apps/web/components/BroadcastBanner.tsx'
      provides: 'Pinned top banner for active broadcasts'
      min_lines: 40
    - path: 'apps/web/components/RealtimeUserChannelProvider.tsx'
      provides: 'Single Supabase Realtime channel per user, fans events to consumers via React context'
      exports: ['RealtimeUserChannelProvider', 'useRealtimeEvents']
    - path: 'apps/web/components/dispatch/CueSubscriber.tsx'
      provides: 'Dispatch-channel flash/toast renderer'
      min_lines: 50
    - path: 'apps/web/app/(app)/profile/notifications/page.tsx'
      provides: 'Notification preferences matrix UI'
      min_lines: 50
  key_links:
    - from: 'apps/web/app/layout.tsx'
      to: 'apps/web/components/RealtimeUserChannelProvider.tsx'
      via: 'provider mounted at root so channel persists across navigations (RESEARCH Pitfall 10)'
      pattern: 'RealtimeUserChannelProvider'
    - from: 'apps/web/app/layout.tsx'
      to: 'apps/web/components/MessagingDrawer.tsx'
      via: 'drawer mounted at root so it persists across navigations'
      pattern: 'MessagingDrawer'
    - from: 'apps/web/app/(app)/page.tsx'
      to: 'role-based redirect'
      via: "redirect('/admin/dashboard' | '/dashboard') based on active role cookie"
      pattern: "redirect\\(.*/dashboard"
    - from: 'apps/web/app/(app)/dashboard/_components/InstructorDashboard.tsx'
      to: 'trpc.gradeSheet + trpc.schedule.approve'
      via: 'GradingActionDrawer + ReservationApproveInline inline mutations'
      pattern: "gradeSheet\\.|schedule\\.approve"
    - from: 'apps/web/components/NotificationBell.tsx'
      to: 'apps/web/components/RealtimeUserChannelProvider.tsx'
      via: 'useRealtimeEvents hook to invalidate unread query on notifications insert'
      pattern: 'useRealtimeEvents'
    - from: 'apps/web/components/dispatch/CueSubscriber.tsx'
      to: 'apps/web/components/RealtimeUserChannelProvider.tsx'
      via: "filter realtime events by channel='dispatch'"
      pattern: 'channel.*dispatch'
---

<objective>
Ship the user-facing surfaces for Phase 8 product cohesion: role-resolved dashboards for students/instructors/mechanics, a globally-mounted messaging drawer + notification bell, admin broadcast banner, dispatch cue subscriber, and the document upload + notification preferences pages.

Purpose: This is where the user feels Phase 8 — the role dashboards, the bell, the drawer, the flashing dispatch row, the preferences matrix. The infra in 08-01 provides the data; this plan renders it.

Output:

- 3 role dashboards (+ admin gets a role-redirect from `/`)
- Document upload tile and notification-prefs page
- Messaging drawer mounted at root layout with RLS-enforced Realtime subscription
- Notification bell in app-shell header with unread badge
- Broadcast banner pinned at top of authenticated pages
- Dispatch screen wired to flash rows + show toasts on dispatch-channel events
- A single per-user Realtime channel mounted once at root, fanning events to consumers via React context (prevents Pitfall 1 connection storm)
  </objective>

<execution_context>
@/Users/christopher/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christopher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-experience-reporting-messaging-beta/08-CONTEXT.md
@.planning/phases/08-experience-reporting-messaging-beta/08-RESEARCH.md
@.planning/phases/08-experience-reporting-messaging-beta/08-01-SUMMARY.md

# Root + app-shell layouts (must extend, not replace)

@apps/web/app/layout.tsx
@apps/web/app/(app)/layout.tsx
@apps/web/app/(app)/page.tsx
@apps/web/lib/supabase/client.ts
@apps/web/lib/trpc/client.ts

# Patterns from prior phases

@apps/web/components/BaseSwitcher.tsx
@apps/web/components/RoleSwitcher.tsx
@apps/web/components/LogoutButton.tsx
@apps/web/app/(app)/admin/dashboard/page.tsx
@apps/web/app/(app)/record/page.tsx
@apps/web/app/(app)/flight-log/page.tsx
@apps/web/app/(app)/schedule/page.tsx
@apps/web/app/(app)/dispatch/page.tsx
@apps/web/app/(app)/dispatch/DispatchBoard.tsx
@apps/web/app/(app)/dispatch/OverdueAlarm.tsx
@apps/web/app/(app)/profile/documents/page.tsx

# Data contracts from 08-01 (INTERFACE REFERENCE — do not rediscover)

@packages/api/src/helpers/notifications.ts
@packages/api/src/routers/notifications.ts
@packages/api/src/routers/messaging.ts
@packages/api/src/routers/broadcasts.ts

<interfaces>
<!-- Extracted from 08-01 — executor should consume these directly. -->

tRPC procedures available after 08-01:

- trpc.notifications.list({ limit?, cursor? }) -> { items: Notification[], nextCursor }
- trpc.notifications.unreadCount() -> number
- trpc.notifications.markRead({ id })
- trpc.notifications.markAllRead()
- trpc.notifications.listPrefs() -> { kind, channel, enabled, is_safety_critical }[]
- trpc.notifications.updatePref({ kind, channel, enabled })
- trpc.messaging.conversations.list() -> { id, otherUser: { id, email, name }, lastMessageAt, unreadCount }[]
- trpc.messaging.conversations.open({ otherUserId }) -> { conversationId }
- trpc.messaging.thread.list({ conversationId, cursor? }) -> { items, nextCursor }
- trpc.messaging.thread.send({ conversationId, body })
- trpc.messaging.thread.markRead({ conversationId })
- trpc.broadcasts.listActive() -> { id, title, body, urgency, senderName, sentAt }[]
- trpc.broadcasts.acknowledge({ broadcastId })
- trpc.admin.activeSessions.list() -> { userId, email, activeRole, activeBaseId, lastSeenAt }[]

Existing tRPC procedures already shipped (reuse):

- trpc.schedule.reservations.listUpcoming, approve, cancel (Phase 3)
- trpc.gradeSheet.listPending, getForReservation, updateLineItem, sign (Phase 5)
- trpc.admin.stageChecks.list (Phase 5)
- trpc.documents.listForMe, getUploadUrl, confirmUpload (Phase 1)
- trpc.record.getSyllabusProgress, getCurrencies (Phase 5)
- trpc.admin.squawks.listOpenForAircraft (Phase 4)
- trpc.me.get (Phase 1)

Supabase client (already configured, reuse):

- createSupabaseBrowserClient() from '@/lib/supabase/client' — use inside 'use client' components
- Channel naming convention: `notifications:user:${userId}` and `messages:user:${userId}` and `broadcasts:school:${schoolId}` — consolidate into ONE per-user channel that filters INSERT events on notifications (user_id=eq.UID), message (conversation_id IN (select id from conversation where user_a_low=UID or user_b_high=UID — filter expression; use simpler filter: sender != UID + server-side dedupe via tRPC refetch)), broadcast (school-scoped; client filters by targetRoles).

Notification kinds that route to dispatch channel (from 08-01):

- overdue_aircraft (channel='dispatch')
- grounded_aircraft_attempted_use (channel='dispatch')
- admin_broadcast with urgency=urgent (channel='dispatch' AND in_app)
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Realtime provider + NotificationBell + MessagingDrawer + BroadcastBanner + root layout wiring + role redirect + notification prefs page</name>
  <files>
    apps/web/components/RealtimeUserChannelProvider.tsx,
    apps/web/components/NotificationBell.tsx,
    apps/web/components/MessagingDrawer.tsx,
    apps/web/components/MessagingDrawerProvider.tsx,
    apps/web/components/BroadcastBanner.tsx,
    apps/web/app/layout.tsx,
    apps/web/app/(app)/layout.tsx,
    apps/web/app/(app)/page.tsx,
    apps/web/app/(app)/profile/notifications/page.tsx,
    apps/web/components/DashboardTile.tsx,
    apps/web/components/ExpiryBadge.tsx,
    apps/web/components/__tests__/ExpiryBadge.test.tsx
  </files>
  <action>
    **RealtimeUserChannelProvider.tsx** — Per RESEARCH Pitfall 1 (connection storm), mount ONE Supabase Realtime channel per user at the root level. It subscribes to three event streams:
    - `postgres_changes` on `notifications` filter `user_id=eq.{userId}` (INSERT, UPDATE)
    - `postgres_changes` on `message` filter needed via participant join — simpler approach: subscribe to ALL inserts on `message` and let RLS drop non-participant rows; client re-fetches conversations list on any event.
    - `postgres_changes` on `broadcast` filter `school_id=eq.{schoolId}` (INSERT)

    Expose `useRealtimeEvents(kind: 'notification' | 'message' | 'broadcast', handler)` via React context — consumers subscribe to the relevant event kind and handler is invoked with the payload. Internally, the provider dedupes event tokens and tracks connection status.

    On mount: create one `supabase.channel('user:' + userId)` with three `.on('postgres_changes', ...)` handlers merged. On unmount: removeChannel. Console.warn if multiple providers mount (dev-only).

    Also expose `useMessagingDrawer()` hook from `MessagingDrawerProvider.tsx` — boolean open state + `openConversation(otherUserId)` method that calls `trpc.messaging.conversations.open` then sets the active conversation and opens the drawer.

    **NotificationBell.tsx** — Client component. Uses `trpc.notifications.unreadCount.useQuery` + `trpc.notifications.list.useInfiniteQuery`. Subscribes via `useRealtimeEvents('notification', () => utils.notifications.invalidate())`. Renders bell icon with red dot when unread > 0. Click opens a dropdown panel showing the last 20 notifications with title/body/link + a "Mark all read" button. Clicking a notification calls markRead + navigates to linkUrl.

    **MessagingDrawer.tsx** — Client component. Right-edge slide-out (use inline styles + CSS transform or tailwind `translate-x-0`/`translate-x-full`). Mounted once at root layout. Conversation list on left pane, active thread on right pane. Uses `trpc.messaging.conversations.list` + `trpc.messaging.thread.list.useInfiniteQuery({ conversationId })`. On send, calls `trpc.messaging.thread.send.useMutation` + optimistically prepends. Subscribes to `useRealtimeEvents('message', () => utils.messaging.invalidate())`. Header icon toggles open/close; state lives in `MessagingDrawerProvider` context so the drawer survives navigations.

    **BroadcastBanner.tsx** — Client component. Uses `trpc.broadcasts.listActive.useQuery`. Subscribes to `useRealtimeEvents('broadcast', () => refetch)`. Renders a top-pinned banner (position: sticky, top: 0, z-index: 50) with title + body + urgency-colored border (yellow for normal, red for urgent). Dismiss button calls `trpc.broadcasts.acknowledge` + removes from local state. Mount inside `(app)/layout.tsx` below the header so it's only visible to authenticated users.

    **DashboardTile.tsx** — Reusable card shell. Props: `{ title: string; href?: string; action?: ReactNode; children: ReactNode; accent?: 'info' | 'warn' | 'critical' }`. Tailwind classes for shadow + border + padding. If href, wraps the tile in a Link. If action, renders it in the top-right of the tile.

    **ExpiryBadge.tsx** — Exactly per RESEARCH Code Examples §ExpiryBadge. Props `{ expiresAt: Date | string | null }`. Returns span with green/yellow/red per CONTEXT-locked thresholds (green >30d, yellow 8–30d, red ≤7d or EXPIRED).
    Test file: assert all four bands render the correct label + background color tuple.

    **apps/web/app/layout.tsx** (ROOT) — wrap children in:
    ```tsx
    <TRPCProvider>
      <RealtimeUserChannelProvider>
        <MessagingDrawerProvider>
          {children}
          <MessagingDrawer />
        </MessagingDrawerProvider>
      </RealtimeUserChannelProvider>
    </TRPCProvider>
    ```
    RealtimeUserChannelProvider reads `userId` from `/api/me` via a tRPC `me.get` ssr-hydrated client call; if unauthenticated, it renders children as a passthrough (no channel). MessagingDrawer renders null if unauthenticated.

    **apps/web/app/(app)/layout.tsx** (authenticated shell) — add NotificationBell next to RoleSwitcher + a message-icon button in the header that calls `useMessagingDrawer().toggle()`. Add BroadcastBanner below the header.

    **apps/web/app/(app)/page.tsx** (was the student/instructor/admin dashboard stub) — replace with the role redirect per RESEARCH Code Examples §Role-Based Redirect. `cookies()` reads `part61.active_role`; admin → `/admin/dashboard`, everyone else → `/dashboard`. Preserve existing handling of unauthenticated users (redirect to `/login`).

    **apps/web/app/(app)/profile/notifications/page.tsx** — Prefs matrix. Server component fetches `trpc.notifications.listPrefs` via `ssrHelper`; client component renders a 2-column matrix: rows are notification kinds (grouped by category: Reservations, Grading, Squawks, Documents & Currency, Messaging), columns are `in_app` and `email`. Checkbox per cell. `onChange` calls `trpc.notifications.updatePref.useMutation`. Disabled checkbox with tooltip "always delivered for safety" for (safety_critical=true, channel=in_app) pairs.

    **Caveats:**
    - No imports of `@react-email/*` in client bundles.
    - Channel filter on message uses `conversation_id=in.(…)` which Supabase Realtime supports via `filter: 'conversation_id=in.(uuid1,uuid2,...)'`. But list can be long — simplest: omit the filter, rely on RLS to drop non-participant rows client-side, and re-fetch via tRPC on any event. Document tradeoff in MessagingDrawer header comment.

  </action>
  <verify>
    <automated>cd apps/web && pnpm typecheck && pnpm lint && pnpm build && pnpm exec vitest run components/__tests__/ExpiryBadge.test.tsx</automated>

    - Manual: `pnpm --filter web dev`, log in as a seeded user, confirm bell + drawer icon appear in header and drawer opens/closes. Verify `/` redirects to `/dashboard`.

  </verify>
  <done>
    Root layout mounts exactly one Realtime channel per user. Bell + drawer icon in app-shell header. Broadcast banner visible on authenticated pages. `/` redirects by role. Notification prefs page renders matrix and persists toggles. ExpiryBadge + DashboardTile tests green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Student + Instructor + Mechanic dashboards with inline action drawers + document upload tile</name>
  <files>
    apps/web/app/(app)/dashboard/page.tsx,
    apps/web/app/(app)/dashboard/_components/StudentDashboard.tsx,
    apps/web/app/(app)/dashboard/_components/InstructorDashboard.tsx,
    apps/web/app/(app)/dashboard/_components/MechanicDashboard.tsx,
    apps/web/app/(app)/dashboard/_components/GradingActionDrawer.tsx,
    apps/web/app/(app)/dashboard/_components/ReservationApproveInline.tsx,
    apps/web/app/(app)/profile/documents/page.tsx
  </files>
  <action>
    **apps/web/app/(app)/dashboard/page.tsx** — Server component. Read active role from cookies. If role === 'admin' redirect to /admin/dashboard (double-belt: should never hit because /(app)/page.tsx already redirected, but belt-and-suspenders). Otherwise render one of <StudentDashboard />, <InstructorDashboard />, <MechanicDashboard /> based on active role. Pass server-hydrated user + school context to the client components via props (so they don't refetch me.get on mount).

    **StudentDashboard.tsx** — Client component, card grid (responsive: 3 cols on lg, 2 on md, 1 on mobile). Six tiles in this order per CONTEXT:
    1. **Next reservation** — `trpc.schedule.reservations.listUpcoming.useQuery({ limit: 1 })`. Show aircraft tail, CFI name, start time in school-local TZ (use `date-fns-tz` + school timezone from `trpc.me.get`). If reservation is past its end + not closed-out, action button "Close out flight" → existing close-out flow.
    2. **Syllabus progress** — `trpc.record.getSyllabusProgress.useQuery`. Show current stage + overall % + link to /record.
    3. **Currency status** — `trpc.record.getCurrencies.useQuery`. List BFR, medical, FAA ID, solo endorsement (if applicable), day/night/PIC currency. Use `<ExpiryBadge />` for each. Sort by urgency (red → yellow → green).
    4. **Outstanding squawks on next aircraft** — `trpc.admin.squawks.listOpenForAircraft.useQuery({ aircraftId: nextReservation.aircraftId })`. Only show if next-reservation tile has an aircraft. Max 3 rows; "View all" link to aircraft squawks page.
    5. **Expiring documents** — `trpc.documents.listForMe.useQuery`. Filter docs with expiration_date. Sort by urgency with `<ExpiryBadge />`.
    6. **Document upload** — drag-and-drop area that calls `trpc.documents.getUploadUrl.useMutation` → PUT file to signed URL → `trpc.documents.confirmUpload.useMutation` with captured {kind, expirationDate}. Kind dropdown offers: medical, license, id, insurance. Show current uploads list below the drop zone.

    **InstructorDashboard.tsx** — Client component, card grid. Six tiles per CONTEXT:
    1. **Today's schedule** — `trpc.schedule.reservations.listForInstructorToday.useQuery` (add this procedure if it doesn't exist — otherwise filter listUpcoming client-side to today). Show lesson + aircraft + student + time. Click row → reservation detail.
    2. **Assigned students** — `trpc.me.getAssignedStudents.useQuery` (add procedure; queries enrollments where instructor_id = me). Max 10 rows, each links to /admin/students/[id].
    3. **Pending grades** — `trpc.gradeSheet.listPending.useQuery` (existing from Phase 5). Each row has an inline "Grade" button that opens <GradingActionDrawer gradeSheetId={row.id} />.
    4. **Pending stage checks** — `trpc.admin.stageChecks.list.useQuery({ status: 'scheduled', assigneeId: me })`. Each row links to /admin/stage-checks/[id].
    5. **Reservation approval requests** — `trpc.schedule.reservations.listRequestedForMe.useQuery` (add procedure; status='requested' AND instructor_id = me). Each row has inline <ReservationApproveInline reservationId={row.id} /> with Approve + Deny buttons.
    6. **Workload ticker** — `trpc.instructorMetrics.weekly.useQuery` (will be added in 08-03; for now, if router not yet shipped, use a placeholder div "Workload data coming with 08-03" and gate render behind a feature flag or try/catch the tRPC call. Document the cross-plan dependency in the component header.). Show `${hoursScheduled}/${weeklyLimit}h this week` + red border if `hoursScheduled > 0.9 * weeklyLimit`.

    **MechanicDashboard.tsx** — Simpler, four tiles:
    1. Open squawks assigned to me (`trpc.admin.squawks.listAssignedToMe.useQuery` — add procedure).
    2. Open work orders assigned to me (`trpc.admin.workOrders.listAssignedToMe.useQuery` — add procedure).
    3. Aircraft nearing next-100hr (`trpc.admin.dashboard.downtimeForecast.useQuery` — reuse Phase 4 admin dashboard procedure).
    4. Active broadcasts (render nothing — the global banner covers this; skip tile).
    Mechanics get 3 tiles not 4. Update header accordingly.

    **GradingActionDrawer.tsx** — Per RESEARCH Pattern 3 Inline Action Drawer. Right-edge drawer separate from messaging drawer (z-index above). `trpc.gradeSheet.get.useQuery({ id })` loads the sheet; minimal line-item grader UI (use existing grading UI patterns from `apps/web/app/(app)/admin/_components/GradeSheetEditor.tsx` if present — otherwise extract the core line-item row). On save: `trpc.gradeSheet.updateLineItem.useMutation` → `utils.gradeSheet.listPending.invalidate()` → optimistic close. Show "Open full grading form" link for the deep workflow (drill into `/admin/grade-sheets/[id]`).

    **ReservationApproveInline.tsx** — Two buttons: Approve (calls `trpc.schedule.reservations.approve.useMutation`) + Deny (calls `trpc.schedule.reservations.cancel.useMutation` with reason 'instructor_denied'). Optimistic UI — disable buttons during pending, toast on success/error. After approve, `utils.schedule.reservations.listRequestedForMe.invalidate()` removes the row.

    **apps/web/app/(app)/profile/documents/page.tsx** — extend existing page (read it first; the file already exists). Add the same drag-and-drop upload tile used on the student dashboard (extract into `apps/web/components/DocumentUploadTile.tsx` and reuse). Show existing docs list with ExpiryBadge. No net new flow — this page is the canonical place for document management; the dashboard tile is a shortcut.

    **New tRPC procedures to add** (these go in the respective existing routers, NOT new routers):
    - `trpc.me.getAssignedStudents` — in `packages/api/src/routers/me.ts`. Returns `{ id, name, email, courseName, stageName }[]` where the caller is the assigned instructor on any active enrollment.
    - `trpc.schedule.reservations.listRequestedForMe` — in `packages/api/src/routers/schedule/reservations.ts`. Returns requested reservations where instructor_id = caller.
    - `trpc.admin.squawks.listAssignedToMe` — in `packages/api/src/routers/admin/squawks.ts`. Returns open squawks where assigned_mechanic_id = caller.
    - `trpc.admin.workOrders.listAssignedToMe` — in `packages/api/src/routers/admin/workOrders.ts`. Returns open work orders where assigned_mechanic_id = caller.

    These are simple additive queries; use existing patterns from the same routers.

    **INS-02** (instructor views assigned students' records): Reuse the existing `/admin/students/[id]` route but gate access: if caller.role === 'admin' → full access; if caller.role === 'instructor' AND the student appears in caller's getAssignedStudents → read-only access (no destructive buttons). Gate at the tRPC layer in `packages/api/src/routers/admin/people.ts` `getStudent` procedure — replace `adminProcedure` with a custom `adminOrAssignedInstructorProcedure` that checks the relation. Re-apply the gate on other student-read procedures the `/admin/students/[id]` page calls.

  </action>
  <verify>
    <automated>cd apps/web && pnpm typecheck && pnpm lint && pnpm build</automated>

    - Manual: log in as a seeded student → hit `/`, confirm redirect to `/dashboard`, confirm 6 tiles render without console errors.
    - Manual: log in as a seeded instructor, confirm 6 tiles render; click Grade on a pending-grade row, confirm drawer opens with the sheet; approve a reservation inline, confirm the row disappears from the queue.
    - Manual: log in as a seeded mechanic, confirm 3 tiles render.

  </verify>
  <done>
    Three role dashboards render correctly. Inline grading drawer and reservation approve/deny work without full-page reloads. Document upload tile uploads + confirms + lists existing docs with expiry badges. INS-02 gate lets instructors view their assigned students' records read-only. All user-facing strings pass banned-terms lint.
  </done>
</task>

<task type="auto">
  <name>Task 3: Dispatch cue subscriber (MSG-04) + active-session view + messaging drawer DM button</name>
  <files>
    apps/web/components/dispatch/CueSubscriber.tsx,
    apps/web/app/(app)/dispatch/page.tsx,
    apps/web/app/(app)/dispatch/DispatchBoard.tsx,
    apps/web/app/(app)/admin/active-sessions/page.tsx,
    apps/web/components/MessagingDrawer.tsx
  </files>
  <action>
    **CueSubscriber.tsx** — Client component. Uses `useRealtimeEvents('notification', handler)`. Handler:
    - If event.channel === 'dispatch':
      - Track in local state by source_record_id (reservation_id for overdue_aircraft; aircraft_id for grounded_aircraft_attempted_use).
      - Apply a 60s auto-clear timer.
      - Emit a toast notification using a simple inline toast queue (no new library — create a minimal `useToast` in this file or use an existing one if present).
    - On click-to-dismiss: call `trpc.notifications.markRead` + remove from local state.
    Expose `useDispatchFlash(reservationId | aircraftId)` hook that returns `{ flashing: boolean; dismiss: () => void }` so DispatchBoard rows can apply the red-glow class.

    **DispatchBoard.tsx** — Read the existing component first. Extend:
    - Wrap the existing row rendering with `const { flashing, dismiss } = useDispatchFlash(reservation.id);` and apply `className={flashing ? 'animate-pulse ring-2 ring-red-500 ring-offset-2' : ''}` — Tailwind animate-pulse gives a subtle flash.
    - Click the row while flashing → dismiss() + still open the reservation detail.

    **apps/web/app/(app)/dispatch/page.tsx** — Read existing. Add `<CueSubscriber />` inside the page (it's a client component mounted alongside DispatchBoard). CueSubscriber has no visible UI apart from the toasts — it's a side-effect component.

    **Publisher side** (grounded_aircraft_attempted_use): The Phase 7 ADS-B polling currently updates aircraft positions. We need a new side-effect: when a position update lands AND the aircraft has `is_grounded = true`, fire a dispatch-channel notification for admin + duty instructor. This is an 08-01-adjacent task: add a helper in `packages/api/src/routers/adsb.ts` or a pg_cron job that scans positions every minute. Simpler: do it at the adsb-poll tRPC procedure level — after updating positions, SELECT aircraft WHERE is_grounded AND last_adsb_position_at > now()-5min, emit notification per affected aircraft. Add this to the adsb position-update path in `packages/api/src/routers/adsb.ts`. Dedupe: only emit once per aircraft per 15 minutes via a helper row in `email_outbox`-style dedupe (or a new lightweight `grounded_use_alert_state` table — simplest: check the last notification row in the same transaction with `WHERE source_record_id = aircraftId AND kind = 'grounded_aircraft_attempted_use' AND created_at > now() - interval '15 minutes' LIMIT 1`).

    **Publisher side** (overdue_aircraft): 08-01 has already landed migration `0037_phase8_dispatch_cue_cron.sql`, which runs a pg_cron job every minute that inserts `channel='dispatch' kind='overdue_aircraft'` notifications for flight reservations past their expected ramp-in that do not yet have a dispatch cue in the last 5 minutes. Do NOT re-add this migration here — it is owned by 08-01. Audit `apps/web/app/(app)/dispatch/OverdueAlarm.tsx` — confirm that the new notifications arriving on the dispatch channel surface in the UI via the CueSubscriber wired in this task. No backend changes needed here beyond wiring.

    **apps/web/app/(app)/admin/active-sessions/page.tsx (MSG-03)** — Admin-only server component. Fetches `trpc.admin.activeSessions.list` (from 08-01). Renders a table: email, active role, active base, last seen (relative time). Each row has a "DM" button that calls `useMessagingDrawer().openConversation(userId)` — this requires `openConversation` to be exposed from the drawer provider (add in Task 1 if not already). "DM" opens the drawer with that user's conversation primed; the drawer is already mounted at root so the action just slides it open.

    **MessagingDrawer.tsx** addition — expose openConversation via the provider context. Already referenced in Task 1 but finalize the contract here: on `openConversation(otherUserId)` the drawer opens, calls `trpc.messaging.conversations.open({ otherUserId })`, sets the returned conversationId as the active thread, and focuses the input.

    Also: render a small "who else is online" affordance at the top of the conversation list in the drawer — shows up to 5 currently-active (< 5min) users from `trpc.admin.activeSessions.list` IF caller is admin (otherwise omit). This gives admins a quick start-a-conversation affordance.

  </action>
  <verify>
    <automated>cd apps/web && pnpm typecheck && pnpm lint && pnpm build</automated>

    - Manual: open `/dispatch` in one browser, insert a notification row via psql with `channel='dispatch'` + `kind='overdue_aircraft'` + `source_record_id=<active reservation uuid>`. Confirm the row flashes red for 60s and a toast appears.
    - Manual: open `/admin/active-sessions` as admin, confirm at least your own session is listed, click DM on another user, confirm drawer opens with that conversation.

  </verify>
  <done>
    Dispatch cue subscriber flashes rows + shows toasts for all three dispatch event kinds. Grounded-use publisher emits dispatch notifications via adsb-poll inline; overdue_aircraft publisher is handled by 08-01's migration 0037 pg_cron job. Active-sessions page lists throttled-last-5-min users and lets admin initiate IM from there.
  </done>
</task>

</tasks>

<verification>
- `pnpm --filter web typecheck && pnpm --filter web lint && pnpm --filter web build` green
- All new vitest suites green
- Manual smoke test script (execute-plan agent must run this):
  1. Log in as seeded admin; verify / redirects to /admin/dashboard; navigate to /admin/active-sessions; see self; verify bell + drawer icons in header.
  2. Log in as seeded student; / redirects to /dashboard; 6 tiles visible; upload a dummy document; confirm it appears in the "Expiring documents" tile.
  3. Log in as seeded instructor (different browser); click Grade on a pending row; drawer opens; save a line item; drawer closes; pending queue updates.
  4. From admin session, insert a `kind='admin_broadcast'` broadcast row; confirm BroadcastBanner appears at top for the student session; dismiss; banner disappears.
  5. From a second admin session, insert a `channel='dispatch' kind='overdue_aircraft' source_record_id=<reservation>` notification; open /dispatch; confirm that reservation's row flashes red + toast fires.
- No realtime connection-count warnings in Supabase dashboard after 5 users open the app (should be exactly 5 channels, one per user).
</verification>

<success_criteria>

- Every requirement in frontmatter has at least one tile, page, or component implementing it (STU-01/04, INS-01/02/03, MSG-01/02/03/04)
- No banned-term lint violations in any UI strings
- Single Realtime channel per user (verified in Supabase dashboard)
- Messaging drawer persists state across navigations
- ExpiryBadge unit test passes all four bands
- Downstream plan 08-05 can write Playwright tests against these surfaces using stable selectors (role="button" name="Grade", role="button" name="Approve", role="dialog" name="Grading", etc.)
  </success_criteria>

<output>
After completion, create `.planning/phases/08-experience-reporting-messaging-beta/08-02-SUMMARY.md` documenting:
- Any new tRPC procedures added to existing routers (me.getAssignedStudents, schedule.reservations.listRequestedForMe, admin.squawks.listAssignedToMe, admin.workOrders.listAssignedToMe)
- INS-02 gate implementation — how adminOrAssignedInstructorProcedure was built
- Confirmation that migration 0037_phase8_dispatch_cue_cron.sql (from 08-01) fires dispatch notifications as expected end-to-end (wave-1 migration → wave-2 UI subscriber)
- The exact Realtime channel naming + filter scheme for any 08-05 executor writing E2E tests
- Resolution of RESEARCH Open Question #7 (realtime connection count) — single-channel-per-user strategy implemented; BroadcastChannel-across-tabs deferred to post-beta if seat count grows
</output>
