---
phase: 02-personnel-admin-fleet-primitives
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - packages/db/src/tx.ts
  - packages/api/src/middleware/tenant.ts
  - packages/api/src/context.ts
  - apps/web/app/api/trpc/[trpc]/route.ts
  - apps/web/app/(app)/layout.tsx
  - packages/db/src/functions/access_token_hook.sql
  - supabase/migrations/20260407000001_phase2_access_token_hook_status.sql
  - tests/rls/phase2-tenant-context.test.ts
autonomous: true
requirements:
  - MUL-02
must_haves:
  truths:
    - "withSchoolContext sets app.base_id GUC when a baseId is provided in the context"
    - "tRPC context builder reads the part61.active_base_id cookie and passes baseId through to withSchoolContext"
    - "Base-scoped RLS policies receive the active base via current_setting('app.base_id', true)"
    - "Access token hook rejects login for users where public.users.status != 'active' (raises or emits empty role claim)"
    - "Admin active_role short-circuits the base filter on base-scoped tables (admin sees all bases in school)"
  artifacts:
    - path: "packages/db/src/tx.ts"
      provides: "Extended SchoolContext interface with optional baseId; withSchoolContext sets app.base_id GUC"
    - path: "packages/api/src/context.ts"
      provides: "tRPC context type extended with baseId field"
    - path: "apps/web/app/api/trpc/[trpc]/route.ts"
      provides: "createContext reads cookies().get('part61.active_base_id') and passes to withSchoolContext"
    - path: "supabase/migrations/20260407000001_phase2_access_token_hook_status.sql"
      provides: "Updated access token hook that rejects status != 'active'"
    - path: "tests/rls/phase2-tenant-context.test.ts"
      provides: "Test that base-scoped table SELECT honors the app.base_id GUC"
  key_links:
    - from: "apps/web/app/api/trpc/[trpc]/route.ts"
      to: "withSchoolContext"
      via: "createContext reads cookie, passes baseId"
      pattern: "active_base_id|baseId"
    - from: "packages/db/src/tx.ts"
      to: "set_config('app.base_id', ...)"
      via: "withSchoolContext when ctx.baseId is set"
      pattern: "set_config.*app\\.base_id"
---

<objective>
Extend the Phase 1 tenant-context plumbing so every tRPC procedure runs inside a transaction that has BOTH `app.school_id` (already set) AND `app.base_id` (new) configured from the user's session cookies. Update the custom access token hook to reject pending/inactive/rejected users.

Purpose: Plans 03 and 04 cannot enforce base scoping or self-registration approval without this foundation. This is the smallest possible plumbing change that unlocks the rest of the phase.
Output: Extended withSchoolContext, updated tRPC context, updated layout, updated access token hook, and a context test.
</objective>

<execution_context>
@/Users/christopher/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christopher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-personnel-admin-fleet-primitives/02-CONTEXT.md
@.planning/phases/02-personnel-admin-fleet-primitives/02-RESEARCH.md
@.planning/phases/02-personnel-admin-fleet-primitives/02-01-SUMMARY.md
@CLAUDE.md
@packages/db/src/tx.ts
@packages/api/src/middleware/tenant.ts
@packages/api/src/context.ts
@apps/web/app/api/trpc/[trpc]/route.ts
@apps/web/app/(app)/layout.tsx
@packages/db/src/functions/access_token_hook.sql

<interfaces>
<!-- Phase 1 plumbing surfaces this plan extends. -->

From packages/db/src/tx.ts:
  - SchoolContext { schoolId, userId, activeRole } — EXTEND with baseId?: string | null
  - withSchoolContext(tx, ctx, fn) — sets app.school_id, app.user_id, app.active_role; EXTEND to also set app.base_id when provided

From packages/api/src/context.ts:
  - createContext returns { session, schoolId, userId, activeRole, ... } — EXTEND with baseId

From apps/web/app/(app)/layout.tsx:
  - Reads part61.active_role cookie via cookies() — ADD parallel read for part61.active_base_id

From packages/db/src/functions/access_token_hook.sql (Phase 1):
  - The hook currently joins public.users to read roles + school_id and emits them as JWT claims
  - EXTEND: filter `where status = 'active'`; if status is pending/inactive/rejected, raise an exception OR emit empty role claims (Research Pattern 8 + Pitfall 5)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend withSchoolContext + tRPC context for base_id</name>
  <files>
    packages/db/src/tx.ts,
    packages/api/src/middleware/tenant.ts,
    packages/api/src/context.ts,
    apps/web/app/api/trpc/[trpc]/route.ts,
    apps/web/app/(app)/layout.tsx
  </files>
  <behavior>
    - SchoolContext interface has `baseId?: string | null` (optional)
    - withSchoolContext, when called with a non-null baseId, executes `select set_config('app.base_id', <id>, true)` inside the transaction
    - When baseId is null/undefined, app.base_id is NOT set (remains unset) — relies on Pitfall 4 fix (`current_setting('app.base_id', true)` returns NULL)
    - tRPC createContext reads `cookies().get('part61.active_base_id')?.value` and passes it through
    - Layout reads the same cookie, validates it via Drizzle SELECT against user_base for the current user, and falls back to the user's primary base if cookie is missing or invalid
    - pnpm -r typecheck passes
  </behavior>
  <action>
    1. **packages/db/src/tx.ts** — Add `baseId?: string | null` to `SchoolContext`. In `withSchoolContext`, after setting app.active_role, add:
       ```ts
       if (ctx.baseId) {
         await tx.execute(sql`select set_config('app.base_id', ${ctx.baseId}, true)`);
       }
       ```

    2. **packages/api/src/context.ts** — Extend the context return type with `baseId: string | null`. Read it from createContext input.

    3. **packages/api/src/middleware/tenant.ts** — In `withTenantTx` (or whatever the wrapper is called), pass `ctx.baseId` to `withSchoolContext`.

    4. **apps/web/app/api/trpc/[trpc]/route.ts** — In `createContext`, read `cookies().get('part61.active_base_id')?.value` and include it in the returned context object. Validate format (uuid) before passing — invalid cookie value → null.

    5. **apps/web/app/(app)/layout.tsx** — Read the cookie. If present, validate that the current user has a `user_base` row matching it via Drizzle SELECT (use the existing tenant transaction or a fresh one). If missing/invalid, default to the user's first user_base row (auto-set the cookie via `cookies().set(...)` in a Server Component is not allowed — instead pass the resolved baseId to children via props and write the cookie on next user action). For Phase 2, simpler: if cookie is missing, just resolve to the user's first base server-side and pass it down — no cookie write yet (BaseSwitcher in Plan 04 will write).

    Verify with `pnpm -r typecheck` continuously.
  </action>
  <verify>
    <automated>cd "/Users/christopher/Desktop/Part 61 School" && . scripts/dev-env.sh && pnpm -r typecheck && pnpm -r lint</automated>
  </verify>
  <done>SchoolContext has optional baseId; withSchoolContext sets app.base_id when provided; tRPC createContext reads the cookie; layout resolves baseId for the current user; typecheck and lint pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Access token hook rejects non-active users + RLS context test</name>
  <files>
    packages/db/src/functions/access_token_hook.sql,
    supabase/migrations/20260407000001_phase2_access_token_hook_status.sql,
    tests/rls/phase2-tenant-context.test.ts
  </files>
  <behavior>
    - Updated access token hook adds `where u.status = 'active'` to the user lookup
    - If status is pending/inactive/rejected, the hook either raises or returns claims with empty roles (Pitfall 5 — pick "raise with friendly message" per Research Pattern 8 §2)
    - Migration applies cleanly via supabase db reset
    - Phase 1 cross-tenant test still passes (no regression)
    - New phase2-tenant-context.test.ts asserts: as a user of school A with app.base_id set to base X, SELECT FROM aircraft (Plan 01's table) returns only aircraft where base_id = X (or all when active_role = admin)
  </behavior>
  <action>
    1. **Update access_token_hook.sql** — Read the current Phase 1 hook (in `packages/db/src/functions/access_token_hook.sql`) and add `where u.status = 'active'` to the JOIN that loads user/role/school. Per Pitfall 5, if zero rows result, RAISE EXCEPTION 'account_not_active' so the login UX can surface a clear message. The JSON return format must remain identical for active users (no breaking change to active sessions).

    2. **Create supabase/migrations/20260407000001_phase2_access_token_hook_status.sql** containing the `create or replace function` block with the updated hook body. Mirror it (or just the function update) into a Drizzle migration if Phase 1 keeps these in sync.

    3. **Create tests/rls/phase2-tenant-context.test.ts**:
       - Seed two schools, each with two bases. School A has 2 aircraft (one per base).
       - As a non-admin instructor of school A with app.base_id = baseX, SELECT * FROM aircraft → expect 1 row (the baseX aircraft).
       - As an admin of school A with app.base_id = baseX, SELECT * FROM aircraft → expect 2 rows (admin branch in Pattern 2 RLS).
       - As a non-admin user of school A with app.base_id UNSET (NULL via current_setting), SELECT * FROM aircraft → expect 0 rows (the policy clause `base_id::text = NULL` is false for non-admins).
       - As any user of school B, SELECT * FROM aircraft → expect 0 school A rows (cross-tenant invariant).

    4. Run `supabase db reset` then `pnpm --filter @part61/rls-tests test`.
  </action>
  <verify>
    <automated>cd "/Users/christopher/Desktop/Part 61 School" && . scripts/dev-env.sh && supabase db reset --no-seed && pnpm --filter @part61/rls-tests test</automated>
  </verify>
  <done>Access token hook rejects non-active users (verified by direct PL/pgSQL call in psql or by a test); phase2-tenant-context test passes all four assertions (instructor sees 1, admin sees 2, unset sees 0, cross-tenant sees 0).</done>
</task>

</tasks>

<verification>
- `pnpm -r typecheck && pnpm -r lint` passes
- `pnpm --filter @part61/rls-tests test` — both Phase 1 cross-tenant tests AND the new phase2-tenant-context test pass
- `grep "set_config.*app\.base_id" packages/db/src/tx.ts` returns the new line
- `grep "active_base_id" apps/web/app/api/trpc/\[trpc\]/route.ts` returns the cookie read
- Direct check: `psql "$DATABASE_URL" -c "select public.access_token_hook('{...pending user payload...}'::jsonb);"` — should raise (or return empty roles)
</verification>

<success_criteria>
- Tenant context plumbing carries baseId end-to-end: cookie → tRPC ctx → withSchoolContext → app.base_id GUC → RLS policy
- Access token hook enforces status='active'
- New cross-context test confirms admin/instructor/unset/cross-tenant matrix
- Zero regression on Phase 1 RLS tests
</success_criteria>

<output>
After completion, create `.planning/phases/02-personnel-admin-fleet-primitives/02-02-SUMMARY.md` documenting:
- The exact change to withSchoolContext signature
- How the cookie flows from createContext to RLS
- Access token hook semantic change and how downstream login UX should handle the new exception
- Test coverage matrix
</output>
