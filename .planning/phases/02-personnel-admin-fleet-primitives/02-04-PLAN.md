---
phase: 02-personnel-admin-fleet-primitives
plan: 04
type: execute
wave: 4
depends_on:
  - 02-01
  - 02-02
  - 02-03
files_modified:
  - apps/web/app/(app)/admin/layout.tsx
  - apps/web/app/(app)/admin/dashboard/page.tsx
  - apps/web/app/(app)/admin/school/page.tsx
  - apps/web/app/(app)/admin/school/SchoolSettingsForm.tsx
  - apps/web/app/(app)/admin/people/page.tsx
  - apps/web/app/(app)/admin/people/PeopleTable.tsx
  - apps/web/app/(app)/admin/people/new/page.tsx
  - apps/web/app/(app)/admin/people/new/CreatePersonForm.tsx
  - apps/web/app/(app)/admin/people/pending/page.tsx
  - apps/web/app/(app)/admin/people/pending/PendingApprovalList.tsx
  - apps/web/app/(app)/admin/people/[id]/page.tsx
  - apps/web/app/(app)/admin/people/[id]/EditProfileForm.tsx
  - apps/web/app/(app)/admin/people/[id]/HoldsPanel.tsx
  - apps/web/app/(app)/admin/people/[id]/CurrenciesPanel.tsx
  - apps/web/app/(app)/admin/people/[id]/QualificationsPanel.tsx
  - apps/web/app/(app)/admin/people/[id]/EmergencyContactsPanel.tsx
  - apps/web/app/(app)/admin/people/[id]/InfoReleasePanel.tsx
  - apps/web/app/(app)/admin/people/[id]/ExperiencePanel.tsx
  - apps/web/app/(app)/admin/people/[id]/RolesPanel.tsx
  - apps/web/app/(app)/admin/aircraft/page.tsx
  - apps/web/app/(app)/admin/aircraft/AircraftTable.tsx
  - apps/web/app/(app)/admin/aircraft/new/page.tsx
  - apps/web/app/(app)/admin/aircraft/new/CreateAircraftForm.tsx
  - apps/web/app/(app)/admin/aircraft/[id]/page.tsx
  - apps/web/app/(app)/admin/aircraft/[id]/EditAircraftForm.tsx
  - apps/web/app/(app)/admin/aircraft/[id]/EnginesPanel.tsx
  - apps/web/app/(app)/admin/aircraft/[id]/EquipmentPanel.tsx
  - apps/web/app/(app)/admin/aircraft/[id]/PhotoPanel.tsx
  - apps/web/app/(app)/admin/aircraft/[id]/RecentFlightsPanel.tsx
  - apps/web/app/(app)/admin/aircraft/[id]/FlightLogEntryForm.tsx
  - apps/web/app/register/page.tsx
  - apps/web/app/register/RegisterForm.tsx
  - apps/web/components/BaseSwitcher.tsx
  - apps/web/app/(app)/layout.tsx
  - apps/web/package.json
autonomous: false
requirements:
  - ADM-05
  - ADM-06
  - ADM-07
  - FLT-06
must_haves:
  truths:
    - "Admin can navigate to /admin/dashboard and see fleet status sourced from aircraft_current_totals"
    - "Admin can list, create, edit, and soft-delete aircraft via /admin/aircraft pages"
    - "Aircraft profile page (/admin/aircraft/[id]) shows totals to 1 decimal, equipment tags, photo, recent flights"
    - "Admin can configure school settings (name, timezone, default base) via /admin/school"
    - "Admin can list, create, edit, soft-delete people via /admin/people pages, filter by role chip, and approve/reject pending self-registrations from /admin/people/pending"
    - "Public /register page collects bio + email and submits via the register tRPC procedure (no auth)"
    - "BaseSwitcher renders only when the user has user_base rows for >1 base; switching writes the cookie and refreshes the page"
    - "Held/grounded users show a red banner on their profile page with reason and creator"
    - "End-to-end live verification against Supabase confirms the entire phase works as a real admin"
  artifacts:
    - path: "apps/web/app/(app)/admin/dashboard/page.tsx"
      provides: "Server Component fleet status panel reading from admin.dashboard.fleetStatus"
    - path: "apps/web/app/(app)/admin/aircraft/[id]/page.tsx"
      provides: "Server Component aircraft profile with totals, equipment, photo, recent flights"
    - path: "apps/web/app/(app)/admin/people/pending/page.tsx"
      provides: "Server Component listing pending registrations with approve/reject actions"
    - path: "apps/web/app/register/page.tsx"
      provides: "Public registration form (no auth) calling register.submit"
    - path: "apps/web/components/BaseSwitcher.tsx"
      provides: "Client Component that writes part61.active_base_id cookie and refreshes"
  key_links:
    - from: "apps/web/app/(app)/admin/dashboard/page.tsx"
      to: "trpc.admin.dashboard.fleetStatus"
      via: "server-side tRPC caller in Server Component"
      pattern: "fleetStatus"
    - from: "apps/web/app/(app)/admin/people/pending/PendingApprovalList.tsx"
      to: "trpc.admin.people.approveRegistration"
      via: "client-side tRPC mutation"
      pattern: "approveRegistration"
    - from: "apps/web/components/BaseSwitcher.tsx"
      to: "part61.active_base_id cookie"
      via: "Server Action that calls cookies().set then redirect"
      pattern: "active_base_id"
---

<objective>
Build every Phase 2 admin UI page (people, aircraft, dashboard, school settings), the public /register page, the BaseSwitcher component, and conduct end-to-end live verification of the entire phase against a running Supabase stack.

Purpose: Closes Phase 2 by giving the admin a daily-use interface for the schema and routers built in Plans 01-03. The end-of-phase human-verify checkpoint validates all 26 requirements against a live system.
Output: Complete admin UI tree, public registration page, base switcher, and a passed live verification.
</objective>

<execution_context>
@/Users/christopher/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christopher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-personnel-admin-fleet-primitives/02-CONTEXT.md
@.planning/phases/02-personnel-admin-fleet-primitives/02-RESEARCH.md
@.planning/phases/02-personnel-admin-fleet-primitives/02-01-SUMMARY.md
@.planning/phases/02-personnel-admin-fleet-primitives/02-02-SUMMARY.md
@.planning/phases/02-personnel-admin-fleet-primitives/02-03-SUMMARY.md
@CLAUDE.md
@apps/web/app/(app)/layout.tsx
@apps/web/app/(app)/profile/documents/page.tsx
@apps/web/components/RoleSwitcher.tsx
@apps/web/lib/trpc.ts

<interfaces>
<!-- Patterns to mirror exactly. -->

From apps/web/app/(app)/profile/documents/page.tsx (Phase 1 reference layout):
  - Server Component reads via Drizzle directly (NOT through tRPC)
  - Mutations from Client Components via tRPC react-query hooks
  - storage signed URL via documents router

From apps/web/components/RoleSwitcher.tsx:
  - Cookie-write Server Action pattern
  - After writing the cookie, redirect() or router.refresh()
  - Conditional render based on whether the user has multiple options

From apps/web/app/(app)/layout.tsx (post-Plan-02):
  - Reads schoolId, activeRole, baseId server-side
  - Provides them to Client Components via React context or props

From packages/api/src/routers/_root.ts (post-Plan-03):
  - appRouter.admin.{ people, aircraft, school, dashboard }
  - appRouter.people.{ holds, currencies, qualifications, emergencyContacts, infoReleases, experience }
  - appRouter.flightLog
  - appRouter.register (public)
  - appRouter.documents.uploadAircraftPhoto

Recommended UI deps (Research §Standard Stack — install if not present):
  - react-hook-form
  - @hookform/resolvers/zod

Display rule (CONTEXT §specifics):
  - All times shown to 1 decimal place via Number(x).toFixed(1)
  - Numeric columns from postgres-js come back as STRINGS — Pitfall 7
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install RHF + build all admin/people pages and panels + /register + BaseSwitcher</name>
  <files>
    apps/web/package.json,
    apps/web/app/(app)/admin/layout.tsx,
    apps/web/app/(app)/admin/people/page.tsx,
    apps/web/app/(app)/admin/people/PeopleTable.tsx,
    apps/web/app/(app)/admin/people/new/page.tsx,
    apps/web/app/(app)/admin/people/new/CreatePersonForm.tsx,
    apps/web/app/(app)/admin/people/pending/page.tsx,
    apps/web/app/(app)/admin/people/pending/PendingApprovalList.tsx,
    apps/web/app/(app)/admin/people/[id]/page.tsx,
    apps/web/app/(app)/admin/people/[id]/EditProfileForm.tsx,
    apps/web/app/(app)/admin/people/[id]/HoldsPanel.tsx,
    apps/web/app/(app)/admin/people/[id]/CurrenciesPanel.tsx,
    apps/web/app/(app)/admin/people/[id]/QualificationsPanel.tsx,
    apps/web/app/(app)/admin/people/[id]/EmergencyContactsPanel.tsx,
    apps/web/app/(app)/admin/people/[id]/InfoReleasePanel.tsx,
    apps/web/app/(app)/admin/people/[id]/ExperiencePanel.tsx,
    apps/web/app/(app)/admin/people/[id]/RolesPanel.tsx,
    apps/web/app/register/page.tsx,
    apps/web/app/register/RegisterForm.tsx,
    apps/web/components/BaseSwitcher.tsx,
    apps/web/app/(app)/layout.tsx
  </files>
  <behavior>
    - `apps/web/app/(app)/admin/layout.tsx` is an AdminGuard server component (Pitfall 13): if active_role !== 'admin', return notFound() or redirect to / with 403
    - /admin/people lists all users in current school+base via Drizzle (server-side), filtered by role chip via search params; pending tab is `/admin/people/pending` and shows status='pending' rows with approve/reject buttons
    - /admin/people/new uses RHF + Zod (createPersonInput from domain) and calls trpc.admin.people.create.useMutation()
    - /admin/people/[id] page renders 8 panels: Edit Profile, Roles, Holds (with red banner if active hold/grounding exists), Currencies, Qualifications, Emergency Contacts, Info Releases, Experience. Read-only panels for No-Shows and Enrollments are also rendered as small Server Components on the same page (data fetched via Drizzle)
    - /register is a PUBLIC route (NOT under (app)) — RegisterForm calls trpc.register.submit.useMutation() with no auth header
    - BaseSwitcher renders ONLY when the current user has more than one user_base row; uses a Server Action to write part61.active_base_id cookie and call redirect(window.location.pathname)
    - Layout (app) is updated to render BaseSwitcher next to RoleSwitcher
    - Banned-term lint passes (no "Part 141", "approved", "certified course" in any user-facing string)
  </behavior>
  <action>
    1. **Install dependencies:**
       ```bash
       . scripts/dev-env.sh && pnpm --filter @part61/web add react-hook-form @hookform/resolvers
       ```

    2. **AdminGuard layout** at `apps/web/app/(app)/admin/layout.tsx` — read session, check active_role, 404/redirect if not admin.

    3. **People list** (`/admin/people/page.tsx`) — Server Component. Read search params for role filter. Drizzle query: SELECT users + person_profile + aggregated user_roles + active person_hold (subquery). Render `<PeopleTable rows={rows} />`. PeopleTable is a Client Component with the chip filter UI and Link rows.

    4. **People new** — Server Component shell + Client `CreatePersonForm` using RHF. On success, router.push(`/admin/people/${result.userId}`).

    5. **People pending** — Server Component lists status='pending' rows. `PendingApprovalList` (Client) renders approve/reject buttons calling the respective mutations.

    6. **People [id] page** — Server Component fetches the user + profile via Drizzle. If an active person_hold exists (cleared_at IS NULL), render a prominent red banner above the form with reason + created_by + created_at + a "Clear" button (in HoldsPanel). Render all 8+ panels passing the user data as props.

    7. **HoldsPanel** — Client Component. Lists active and cleared holds. Form to create new hold (kind, reason). Form to clear an active hold (cleared_reason).

    8. **CurrenciesPanel** — Lists rows with current/due_soon/expired chip. Form to add a currency. Calls trpc.people.currencies.*. Status chip uses currency_status function result returned from the server (or computed client-side using the same warning_days from currency_kind_config — server-computed is simpler, add it to the list query result).

    9. **QualificationsPanel** — Same shape, kind enum dropdown + descriptor text.

    10. **EmergencyContactsPanel** — List + add + edit + delete. Mark one as primary.

    11. **InfoReleasePanel** — List + add + revoke.

    12. **ExperiencePanel** — List of snapshots + add new snapshot form (numeric inputs).

    13. **RolesPanel** — Lists current role rows with chip per role; admin can assign a new role (dropdown of roleEnum + mechanicAuthority dropdown when role=mechanic) and remove existing ones.

    14. **/register page** — Outside (app) route group. Public. RegisterForm uses RHF, on submit calls trpc.register.submit, on success shows "Your registration has been submitted. An admin will review it shortly."

    15. **BaseSwitcher** — Client Component. Reads available bases from a small server-prefetched prop (parent layout passes user_base rows joined to bases). If length <= 1, render nothing. Otherwise render a select; onChange triggers a Server Action that does `cookies().set('part61.active_base_id', value, { ... })` then redirect to current pathname.

    16. **layout.tsx update** — In the (app) layout, fetch the user's user_base rows server-side, pass to a small `<BaseSwitcher availableBases={...} activeBaseId={...} />`. Ensure no rendering when only one base.

    17. **Banned-term check** — grep all new files for forbidden strings (the lint rule will catch them but quicker to grep first):
       ```bash
       grep -rEn "Part 141|approved|certified course" apps/web/app/\(app\)/admin apps/web/app/register apps/web/components/BaseSwitcher.tsx
       ```
       Replace any hits ("Approved by" → "Authorized by", etc.) or add `// allow-banned-term: <reason>` comments where intentional.
  </action>
  <verify>
    <automated>cd "/Users/christopher/Desktop/Part 61 School" && . scripts/dev-env.sh && pnpm -r typecheck && pnpm -r lint && pnpm --filter @part61/web build</automated>
  </verify>
  <done>All people pages, /register, and BaseSwitcher exist; build, typecheck, and lint (including banned-terms) pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: admin/aircraft pages + admin/dashboard + admin/school</name>
  <files>
    apps/web/app/(app)/admin/dashboard/page.tsx,
    apps/web/app/(app)/admin/school/page.tsx,
    apps/web/app/(app)/admin/school/SchoolSettingsForm.tsx,
    apps/web/app/(app)/admin/aircraft/page.tsx,
    apps/web/app/(app)/admin/aircraft/AircraftTable.tsx,
    apps/web/app/(app)/admin/aircraft/new/page.tsx,
    apps/web/app/(app)/admin/aircraft/new/CreateAircraftForm.tsx,
    apps/web/app/(app)/admin/aircraft/[id]/page.tsx,
    apps/web/app/(app)/admin/aircraft/[id]/EditAircraftForm.tsx,
    apps/web/app/(app)/admin/aircraft/[id]/EnginesPanel.tsx,
    apps/web/app/(app)/admin/aircraft/[id]/EquipmentPanel.tsx,
    apps/web/app/(app)/admin/aircraft/[id]/PhotoPanel.tsx,
    apps/web/app/(app)/admin/aircraft/[id]/RecentFlightsPanel.tsx,
    apps/web/app/(app)/admin/aircraft/[id]/FlightLogEntryForm.tsx
  </files>
  <behavior>
    - /admin/dashboard fetches admin.dashboard.fleetStatus server-side and renders a card grid: each aircraft shows tail, current Hobbs (1 decimal), current Tach (1 decimal), current Airframe (1 decimal), last flown timestamp in school timezone, status chip
    - /admin/school edit form persists name, timezone, default_base_id; uses RHF
    - /admin/aircraft list paginates and renders a table; row click → /admin/aircraft/[id]
    - /admin/aircraft/new creates an aircraft with required fields (tail, base, m/m/y) and inserts an initial flight_log_entry of kind='baseline' with the entered initial Hobbs/tach/airframe — call flightLog.create with kind='baseline' OR add a dedicated `aircraft.createWithBaseline` mutation in Plan 03 (if missing, add it as a small follow-up here)
    - /admin/aircraft/[id] profile page renders: header with totals from aircraft_current_totals (1 decimal), EquipmentPanel (multi-select of tags + notes text), EnginesPanel (list + add + remove), PhotoPanel (most recent aircraft_photo document via signed URL + upload widget reusing documents flow), RecentFlightsPanel (last 25 entries with corrections visually linked to their corrects_id row — Pitfall 10), and a "Log Flight" button that opens FlightLogEntryForm
    - FlightLogEntryForm collects hobbs_out/in, tach_out/in, airframe_delta, per-engine deltas, notes; submits via trpc.flightLog.create. On success, refreshes the page
    - All numeric displays use Number(x).toFixed(1) (Pitfall 7)
    - Banned-term check passes
  </behavior>
  <action>
    Build the pages following Phase 1 `/profile/documents` as the reference layout. Specifics:

    1. **Dashboard** — Server Component. Call appRouter.createCaller(ctx).admin.dashboard.fleetStatus(). Render a grid of `<AircraftCard>` (server-rendered, no client interactivity needed). Format numeric strings via `Number(x ?? 0).toFixed(1)`.

    2. **School settings** — Read schools row via Drizzle in the page; pass to Client `SchoolSettingsForm` using RHF + zod. Submit via trpc.admin.school.update.

    3. **Aircraft list** — Server Component, Drizzle SELECT joining bases. Client `AircraftTable` for pagination/filter UI.

    4. **Aircraft new** — Form with tail/make/model/year/base/initial Hobbs/initial Tach/initial Airframe. On submit, calls a single mutation that creates the aircraft AND the baseline flight_log_entry in one tRPC call. If Plan 03 didn't expose a combined mutation, call `admin.aircraft.create` then `flightLog.create({kind:'baseline', ...})` sequentially (Plan 03's flightLog.create accepts kind so this should work).

    5. **Aircraft [id]** — Server Component fetches aircraft + engines + equipment + recent flights + photo URL all via Drizzle (Phase 1 pattern). Renders the panels.

    6. **PhotoPanel** — Reuses Phase 1 documents upload pattern. Calls trpc.documents.uploadAircraftPhoto. Renders the latest photo via a signed URL fetched server-side.

    7. **RecentFlightsPanel** — Render a table. For correction rows, indent + show "↳ corrects {originalId}" link (Pitfall 10).

    8. **FlightLogEntryForm** — Client Component. Numeric inputs for hobbs/tach/airframe. If aircraft has >1 engine, render per-engine delta inputs; otherwise hide them and submit single delta automatically. Validates `hobbs_in >= hobbs_out` etc client-side and server-side (add to domain Zod schema if not already).

    9. **Banned-term grep** for all new files. Fix or annotate.
  </action>
  <verify>
    <automated>cd "/Users/christopher/Desktop/Part 61 School" && . scripts/dev-env.sh && pnpm -r typecheck && pnpm -r lint && pnpm --filter @part61/web build && pnpm --filter @part61/rls-tests test</automated>
  </verify>
  <done>All aircraft + dashboard + school pages exist and build; numeric formatting is consistent at 1 decimal; banned-terms lint passes; full RLS suite still passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: End-of-phase live verification against Supabase</name>
  <what-built>
    Complete Phase 2: schema (12+ new tables + 2 views), tenant context with base_id, tRPC routers (admin/people, admin/aircraft, admin/school, admin/dashboard, people sub-routers, flightLog, register, documents.uploadAircraftPhoto), and admin UI (/admin/people, /admin/aircraft, /admin/dashboard, /admin/school, /register, BaseSwitcher).

    All 26 Phase 2 requirements (ADM-01..07, FLT-01/02/03/05/06, PER-01..10, IPF-01/02, MUL-01/02) are claimed implemented.
  </what-built>
  <how-to-verify>
    Bring up the live stack:
    ```bash
    cd "/Users/christopher/Desktop/Part 61 School"
    . scripts/dev-env.sh
    supabase start
    supabase db reset --no-seed
    pnpm --filter @part61/web dev
    ```

    Then walk through these scenarios in a real browser at http://localhost:3000:

    **Setup (one-time, via psql or seed script — document the steps):**
    1. Create a school + an admin user (use Phase 1's admin invite flow or a seed script). Log in as the admin.

    **People CRUD (ADM-01/02/03/04, PER-01/03/04/05/06/07/08/09/10, IPF-01/02):**
    2. Visit /admin/people. Expect empty table with role chip filters.
    3. Click "New Person" → fill in Student bio (name, DOB, address, phone, FAA cert, citizenship). Submit. Expect redirect to /admin/people/[id].
    4. On the profile, add an emergency contact, an info release authorization, and an experience snapshot. Verify each persists after refresh.
    5. Add a hold to the student with reason "incomplete documents". Expect a red banner at the top of the profile.
    6. Clear the hold with reason "documents received". Expect the banner to disappear and the cleared hold to remain in the panel history.
    7. Go back to /admin/people, switch the role chip to "Pending" — expect empty list.
    8. Create another user, this time an Instructor. On the instructor profile, add a CFI currency expiring in 10 days (expect "due_soon" chip), a CFII expiring in 60 days (expect "current"), and a Medical expired yesterday (expect "expired"). Add an aircraft_type qualification "C172".
    9. Create a Mechanic with A&P/IA designation. Verify the mechanicAuthority field is captured.
    10. Create a Rental Customer. Verify the role chip and that it is selectable.

    **Self-registration approval queue (PER-02):**
    11. Open an incognito window, visit /register. Submit a registration form (different email).
    12. Back in the admin window, refresh /admin/people/pending — expect the new entry. Click "Approve". Verify (a) the user disappears from pending, (b) appears in the main /admin/people list with status active, (c) check Supabase auth.users via `psql` or Studio to confirm an auth user with the SAME id exists, (d) an invite email was triggered (check Inbucket at http://localhost:54324).
    13. Submit another registration. This time click "Reject" with a reason. Verify users.status='rejected', no auth user created.

    **Aircraft CRUD + multi-clock (ADM-05/06/07, FLT-01/02/03/05/06):**
    14. Visit /admin/aircraft → empty. Click "New Aircraft". Create N12345 / Cessna / 172 / 2010 with initial Hobbs 5000.0, Tach 4500.0, Airframe 5000.0. Submit.
    15. On /admin/aircraft/[id], verify the totals header shows 5000.0 / 4500.0 / 5000.0 (1 decimal).
    16. Click "Log Flight". Enter hobbs_out=5000.0, hobbs_in=5001.5, tach_out=4500.0, tach_in=4501.4, airframe_delta=1.5. Submit.
    17. Verify the totals header updates to 5001.5 / 4501.4 / 5001.5.
    18. Verify Recent Flights panel shows the entry.
    19. Add equipment tags: ifr_equipped, glass_panel, gtn_650. Verify the tags persist.
    20. Upload an aircraft photo. Verify it renders via signed URL.
    21. Try to log a "correction" flight (kind='correction') with negative deltas. Verify it appears in Recent Flights linked to the original via the corrects_id indicator and that the totals net out correctly.
    22. Soft-delete the aircraft. Verify it disappears from /admin/aircraft (or shows with "deleted" indicator).

    **Dashboard (ADM-07):**
    23. Create another aircraft N67890 with baseline. Visit /admin/dashboard. Expect a card per active aircraft with tail, totals, last flown.

    **School settings (ADM-06):**
    24. Visit /admin/school. Update the timezone to "America/Los_Angeles". Save. Refresh. Verify the new timezone persists.

    **Multi-base (MUL-01/02):**
    25. Via psql, insert a second base + a user_base row for the admin: `insert into bases (id, school_id, name) values (gen_random_uuid(), '<school>', 'KOAK Base'); insert into user_base (...);`
    26. Refresh the app. Expect BaseSwitcher to now appear in the layout (it was hidden when there was only one base).
    27. Switch to the new base. Expect /admin/aircraft to show only aircraft scoped to that base (empty initially). Create an aircraft in the new base. Verify it appears here but NOT when switching back to the original base (unless you switch back AND active_role is admin, in which case the cross-base admin branch should show ALL aircraft — verify Pattern 2 RLS).

    **RLS sanity (FND-01 invariant):**
    28. From psql as a non-admin instructor of school A, attempt `select * from public.aircraft;` — verify only school A rows are visible. Attempt to update flight_log_entry — verify zero rows affected.

    **Banned-term lint (FND-05):**
    29. Run `. scripts/dev-env.sh && pnpm -r lint` — verify no banned-term violations.

    **Cleanup:**
    30. `supabase stop`

    Document any issues found in `.planning/phases/02-personnel-admin-fleet-primitives/02-04-VERIFICATION.md` with truth/reason/artifacts/missing format. If everything passes, write a clean SUMMARY.
  </how-to-verify>
  <resume-signal>Type "approved" if all 26 requirements verify end-to-end, or describe issues for gap closure planning.</resume-signal>
</task>

</tasks>

<verification>
- `pnpm -r typecheck && pnpm -r lint && pnpm --filter @part61/web build` passes
- `pnpm --filter @part61/rls-tests test` passes with all phase2 suites green
- Live verification (Task 3) human-approved
</verification>

<success_criteria>
- All admin pages render and work end-to-end against a real Supabase
- /register accepts public submissions and approval flow creates auth user
- BaseSwitcher renders only when applicable and switches base context cleanly
- Aircraft profile uses 1-decimal display everywhere
- Banned-term lint clean across all new files
- Human verifier approves Task 3 against the 26 requirements
</success_criteria>

<output>
After completion, create `.planning/phases/02-personnel-admin-fleet-primitives/02-04-SUMMARY.md` documenting:
- All admin pages built with route paths
- BaseSwitcher conditional render logic
- /register flow walkthrough
- Live verification results (link to VERIFICATION.md if any gaps)
- Phase 2 done state — every requirement ID with a one-line note on where it's implemented
</output>
