---
phase: 02-personnel-admin-fleet-primitives
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/db/src/schema/enums.ts
  - packages/db/src/schema/users.ts
  - packages/db/src/schema/documents.ts
  - packages/db/src/schema/personnel.ts
  - packages/db/src/schema/holds.ts
  - packages/db/src/schema/currencies.ts
  - packages/db/src/schema/qualifications.ts
  - packages/db/src/schema/no_show.ts
  - packages/db/src/schema/enrollment.ts
  - packages/db/src/schema/aircraft.ts
  - packages/db/src/schema/flight_log.ts
  - packages/db/src/schema/user_base.ts
  - packages/db/src/schema/views.ts
  - packages/db/src/schema/index.ts
  - packages/db/migrations/0002_phase2_personnel_aircraft.sql
  - supabase/migrations/20260407000000_phase2_personnel_aircraft.sql
  - tests/rls/phase2-personnel.test.ts
  - tests/rls/phase2-aircraft.test.ts
  - tests/rls/phase2-views.test.ts
autonomous: true
requirements:
  - FLT-01
  - FLT-02
  - FLT-03
  - FLT-05
  - PER-01
  - PER-03
  - PER-04
  - PER-05
  - PER-06
  - PER-07
  - PER-08
  - PER-09
  - PER-10
  - IPF-01
  - IPF-02
  - MUL-01
must_haves:
  truths:
    - "Every Phase 2 table is RLS-isolated per school (cross-tenant SELECT returns zero rows)"
    - "flight_log_entry is append-only (UPDATE denied by RLS, DELETE blocked by trigger)"
    - "aircraft_current_totals view returns correct Hobbs/tach/airframe per aircraft and respects RLS through security_invoker"
    - "currency_status() SQL function returns current/due_soon/expired/unknown based on expires_at and warning_days"
    - "All new safety-relevant tables have audit triggers attached"
    - "rental_customer is a valid value of the role enum"
    - "users.status enum (pending/active/inactive/rejected) exists with default 'active'"
  artifacts:
    - path: "packages/db/migrations/0002_phase2_personnel_aircraft.sql"
      provides: "Hand-authored migration creating all Phase 2 tables, enums, RLS policies, audit attachments, currency_status function, and security_invoker views"
    - path: "supabase/migrations/20260407000000_phase2_personnel_aircraft.sql"
      provides: "Mirror of the Drizzle migration for supabase start"
    - path: "packages/db/src/schema/aircraft.ts"
      provides: "aircraft, aircraft_engine, aircraft_equipment Drizzle tables with pgPolicy entries"
    - path: "packages/db/src/schema/flight_log.ts"
      provides: "flight_log_entry + flight_log_entry_engine Drizzle tables (append-only)"
    - path: "packages/db/src/schema/views.ts"
      provides: "Drizzle pgView().existing() bindings for aircraft_current_totals and aircraft_engine_current_totals"
    - path: "tests/rls/phase2-views.test.ts"
      provides: "Cross-tenant test that asserts SELECT through aircraft_current_totals returns zero rows for the other school"
  key_links:
    - from: "packages/db/src/schema/views.ts"
      to: "public.aircraft_current_totals"
      via: "pgView('aircraft_current_totals', {...}).existing()"
      pattern: "pgView.*aircraft_current_totals.*existing"
    - from: "0002_phase2_personnel_aircraft.sql"
      to: "audit.attach"
      via: "select audit.attach('<table>') for every safety-relevant table"
      pattern: "select audit\\.attach"
    - from: "0002_phase2_personnel_aircraft.sql"
      to: "security_invoker views"
      via: "create view ... with (security_invoker = true)"
      pattern: "security_invoker\\s*=\\s*true"
---

<objective>
Ship the complete Phase 2 schema, RLS, audit wiring, derived-totals views, and cross-tenant tests in a single migration. After this plan: the database has every table, enum, view, and SQL function downstream plans need; nothing else in Phase 2 can begin without it.

Purpose: Phase 2's schema layer is the foundation every router and UI page reads from. Front-loading it means subsequent plans can run in parallel against a stable shape.
Output: New Drizzle schema files, hand-authored migration mirrored into supabase/migrations, RLS test suite, and the aircraft_current_totals view exposed via Drizzle.
</objective>

<execution_context>
@/Users/christopher/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christopher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-personnel-admin-fleet-primitives/02-CONTEXT.md
@.planning/phases/02-personnel-admin-fleet-primitives/02-RESEARCH.md
@CLAUDE.md
@packages/db/src/schema/enums.ts
@packages/db/src/schema/users.ts
@packages/db/src/schema/documents.ts
@packages/db/src/schema/tenancy.ts
@packages/db/src/schema/audit.ts
@packages/db/src/schema/index.ts
@packages/db/migrations/0000_init.sql
@tests/rls/cross-tenant.test.ts
@tests/rls/harness.ts

<interfaces>
<!-- Phase 1 primitives every new schema file extends. -->

From packages/db/src/schema/enums.ts:
  - roleEnum (currently: 'student' | 'instructor' | 'mechanic' | 'admin') — EXTEND with 'rental_customer'
  - documentKindEnum — EXTEND with 'aircraft_photo'

From packages/db/src/schema/users.ts:
  - users (id mirrors auth.users.id, NOT defaultRandom — Phase 1 decision; schoolId NOT NULL) — EXTEND with status column
  - userRoles (uniqueIndex on (user_id, role) — Pitfall 11 confirms dual roles allowed)

From packages/db/src/schema/tenancy.ts:
  - schools, bases (bases already exist; reuse without change)

From packages/db/src/schema/documents.ts:
  - documents (school_id, user_id, kind, storage_path, ...) — EXTEND with nullable aircraftId column

From packages/db/src/schema/audit.ts + functions:
  - select audit.attach('<table_name>') — adds audit trigger AND hard-delete blocker
  - For tables that should allow hard delete (emergency_contact, info_release_authorization, aircraft_equipment, currency_kind_config), use a split helper or omit the block trigger (Research §Migration step 7 comment)

From packages/db/src/tx.ts:
  - withSchoolContext sets app.school_id, app.user_id, app.active_role GUCs

From tests/rls/harness.ts:
  - seedTwoSchools(), asUserOf() — replicate cross-tenant pattern for every new table

CRITICAL Phase 1 contracts (do not break):
  - pgPolicy `to:` MUST be the string 'authenticated' (NOT sql`authenticated`)
  - users.id is NOT defaultRandom — mirrors auth.users.id (relaxed in Plan 02 for self-registration)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Drizzle schema files for all Phase 2 tables</name>
  <files>
    packages/db/src/schema/enums.ts,
    packages/db/src/schema/users.ts,
    packages/db/src/schema/documents.ts,
    packages/db/src/schema/personnel.ts,
    packages/db/src/schema/holds.ts,
    packages/db/src/schema/currencies.ts,
    packages/db/src/schema/qualifications.ts,
    packages/db/src/schema/no_show.ts,
    packages/db/src/schema/enrollment.ts,
    packages/db/src/schema/aircraft.ts,
    packages/db/src/schema/flight_log.ts,
    packages/db/src/schema/user_base.ts,
    packages/db/src/schema/views.ts,
    packages/db/src/schema/index.ts
  </files>
  <behavior>
    - Drizzle TypeScript compiles cleanly across the workspace (`pnpm -r typecheck` passes)
    - Every new pgTable export has at least one pgPolicy with `to: 'authenticated'` as a STRING literal
    - Every base-scoped table (aircraft, flight_log_entry, flight_log_entry_engine, instructor_qualification, user_base) has a NOT NULL base_id column referencing bases.id
    - Every new table has a school_id NOT NULL referencing schools.id
    - aircraft_current_totals and aircraft_engine_current_totals are exported via `pgView(...).existing()` (no DDL generation)
    - documents.aircraftId is added as a NULLABLE uuid column (existing rows must keep working)
    - users.status uses a new userStatusEnum with default 'active'
    - roleEnum gains 'rental_customer'; documentKindEnum gains 'aircraft_photo'
    - schema/index.ts re-exports every new table and enum
  </behavior>
  <action>
    Create the new schema files following Research §Recommended Schema Layout and Pattern 1 (New Table Template). Specifically:

    1. **enums.ts** — Extend roleEnum with `rental_customer`. Extend documentKindEnum with `aircraft_photo`. Add new pgEnums: `userStatusEnum` (pending/active/inactive/rejected), `holdKindEnum` (hold/grounding), `currencyKindEnum` (cfi/cfii/mei/medical/bfr/ipc), `qualificationKindEnum` (aircraft_type/sim_authorization/course_authorization), `flightLogEntryKindEnum` (flight/baseline/correction), `enginePositionEnum` (single/left/right/center/n1/n2/n3/n4), `citizenshipStatusEnum` (us_citizen/us_national/foreign_national/unknown), `tsaAfspStatusEnum` (not_required/pending/approved/expired), `experienceSourceEnum` (self_reported/imported/derived), `aircraftEquipmentTagEnum` (15 values per Research Pattern locked decisions).

    2. **users.ts** — Add `status: userStatusEnum('status').notNull().default('active')` column. Do NOT make users.id defaultRandom (Phase 1 contract). Add an index on status.

    3. **documents.ts** — Add `aircraftId: uuid('aircraft_id')` (nullable, no FK yet — added in migration after aircraft exists).

    4. **personnel.ts** — Create `personProfile` (1:1 user_id PK, all bio fields per CONTEXT), `emergencyContact` (1:N), `infoReleaseAuthorization` (1:N), `instructorExperience` (1:N snapshots). Each table: school_id, user_id, school RLS policy.

    5. **holds.ts** — Create `personHold` per Research Pattern 1 (literal copy-paste shape, with holdKindEnum). Index `(user_id) WHERE cleared_at IS NULL`.

    6. **currencies.ts** — Create `instructorCurrency` (school_id, user_id, kind, effective_at, expires_at, notes, document_id FK to documents nullable) and `currencyKindConfig` (kind PK, warning_days int — seeded in migration).

    7. **qualifications.ts** — Create `instructorQualification` (school_id, user_id, base_id NOT NULL, kind, descriptor text, granted_at, granted_by, notes).

    8. **no_show.ts** — Create `noShow` table per CONTEXT (lesson_descriptor text — Phase 5 will FK).

    9. **enrollment.ts** — Create `studentCourseEnrollment` (course_descriptor text — Phase 5 will FK).

    10. **aircraft.ts** — Create `aircraft` (id, school_id, base_id NOT NULL, tail_number unique within school, make, model, year, equipment_notes text, deleted_at timestamptz nullable for soft delete, created_at), `aircraftEngine` (aircraft_id, position enginePositionEnum, serial_number, installed_at, removed_at), `aircraftEquipment` (aircraft_id, tag aircraftEquipmentTagEnum, PK on both).

    11. **flight_log.ts** — Create `flightLogEntry` per Research Pattern 4 (numeric(10,1) for hobbs/tach/airframe; nullable hobbs/tach for baseline rows; correctsId self-FK; base_id NOT NULL). Create `flightLogEntryEngine` (flight_log_entry_id, engine_id, delta_hours numeric(10,1)). RLS: combined school+base policy per Research Pattern 2 with the admin-cross-base branch. CRUCIAL: NO update policy on flight_log_entry — append-only contract.

    12. **user_base.ts** — Create `userBase` join (user_id, base_id, school_id, PK both). RLS: school-scoped only.

    13. **views.ts** — Declare `aircraftCurrentTotals` and `aircraftEngineCurrentTotals` via `pgView(name, columns).existing()` per Research Pattern 5/6. Columns must match the SQL view definition exactly (names use snake_case in pgView column args).

    14. **index.ts** — Re-export every new table, enum, and view binding.

    PITFALLS to avoid (Research §Common Pitfalls):
    - Pitfall 1: `to: 'authenticated'` as STRING in every pgPolicy
    - Pitfall 12: base_id NOT NULL is fine (all tables are new)
    - Pitfall 11: user_roles unique constraint is (user_id, role) — already correct, no change
    - Anti-pattern: do NOT add a current_hobbs column to aircraft

    Use `pnpm -r typecheck` continuously to catch errors early.
  </action>
  <verify>
    <automated>cd "/Users/christopher/Desktop/Part 61 School" && . scripts/dev-env.sh && pnpm -r typecheck</automated>
  </verify>
  <done>All 14 schema files compile; pnpm -r typecheck passes; every new pgPolicy has `to: 'authenticated'` as a string literal (grep verifies); index.ts re-exports everything.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Hand-authored migration + Supabase mirror + currency_status + views</name>
  <files>
    packages/db/migrations/0002_phase2_personnel_aircraft.sql,
    supabase/migrations/20260407000000_phase2_personnel_aircraft.sql,
    packages/db/src/functions/currency_status.sql
  </files>
  <behavior>
    - Migration applies cleanly against a fresh Supabase local (`supabase db reset` succeeds end-to-end)
    - Re-running the migration is idempotent (ALTER TYPE uses IF NOT EXISTS — Pitfall 3)
    - aircraft_current_totals and aircraft_engine_current_totals are created `with (security_invoker = true)` (Pitfall 2)
    - Both views have `grant select ... to authenticated` (Pitfall 8)
    - currency_status function exists and returns expected values for current/due_soon/expired/unknown inputs
    - audit.attach() is called for every safety-relevant table (flight_log_entry, person_hold, instructor_currency, instructor_qualification, no_show, student_course_enrollment, instructor_experience, aircraft, aircraft_engine, flight_log_entry_engine, person_profile, user_base)
    - documents.aircraft_id FK is added AFTER aircraft table is created
  </behavior>
  <action>
    Hand-author the migration following Research Example 1 as the skeleton. Steps in order (single transaction):

    1. **Extend existing enums** with `IF NOT EXISTS` (hand-edit — Drizzle-kit omits this — Pitfall 3):
       ```sql
       alter type public.role add value if not exists 'rental_customer';
       alter type public.document_kind add value if not exists 'aircraft_photo';
       ```

    2. **Create new enums** via `create type ... as enum (...)` for all 10 new enums listed in Task 1.

    3. **Extend users**: `alter table public.users add column status public.user_status not null default 'active';` + index.

    4. **Extend documents**: `alter table public.documents add column aircraft_id uuid;` (FK added in step 6).

    5. **Create all Phase 2 tables** in FK order: bases-dependent first (aircraft → aircraft_engine → flight_log_entry → flight_log_entry_engine), then user-dependent (person_profile, emergency_contact, info_release_authorization, person_hold, instructor_currency, currency_kind_config, instructor_qualification, no_show, student_course_enrollment, instructor_experience, user_base, aircraft_equipment). Generate the CREATE TABLE statements with `pnpm --filter @part61/db drizzle-kit generate --name phase2_diff_check` and DIFF the output against your hand-authored SQL — they should produce identical CREATE TABLE blocks. Adopt the drizzle-kit output for table DDL; hand-author the RLS/audit/view blocks.

    6. **Wire documents.aircraft_id FK**:
       ```sql
       alter table public.documents add constraint documents_aircraft_id_fkey
         foreign key (aircraft_id) references public.aircraft(id);
       ```

    7. **Enable RLS + attach audit triggers** for every new table (Research Example 1 step 7).

    8. **Seed currency_kind_config** with defaults: `medical=30, bfr=60, cfi=30, cfii=30, mei=30, ipc=30`.

    9. **Create currency_status function** per Research Pattern 7 (LANGUAGE sql STABLE). Place the canonical definition in `packages/db/src/functions/currency_status.sql` and `\i` it OR inline it in the migration. Inline is simpler — do that and keep the .sql file as a reference copy.

    10. **Create the two views** per Research Pattern 5 and Pattern 6, BOTH `with (security_invoker = true)`, BOTH followed by `grant select on public.<view> to authenticated;`. The aircraft_current_totals SQL must handle baseline rows correctly (sum flight deltas + max baseline hobbs_in/tach_in). The aircraft_engine_current_totals view groups by (aircraft_id, engine_id) and sums delta_hours from flight_log_entry_engine.

    11. **Mirror to supabase/migrations/** — copy the file verbatim to `supabase/migrations/20260407000000_phase2_personnel_aircraft.sql`.

    12. Run `. scripts/dev-env.sh && supabase db reset` to verify the migration applies cleanly. Then run it AGAIN (no reset) to verify idempotence — should succeed because of `IF NOT EXISTS` on enum extensions.
  </action>
  <verify>
    <automated>cd "/Users/christopher/Desktop/Part 61 School" && . scripts/dev-env.sh && supabase db reset --no-seed 2>&1 | tail -30 && psql "$DATABASE_URL" -c "select 1 from public.aircraft_current_totals limit 0;" -c "select public.currency_status(now() + interval '5 days', 30);" -c "select reltype from pg_class where relname='flight_log_entry' and relkind='r';"</automated>
  </verify>
  <done>supabase db reset succeeds; both views exist and accept SELECT; currency_status returns 'due_soon' for the test input; flight_log_entry table exists with audit trigger attached.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Cross-tenant RLS tests for personnel, aircraft, and views</name>
  <files>
    tests/rls/phase2-personnel.test.ts,
    tests/rls/phase2-aircraft.test.ts,
    tests/rls/phase2-views.test.ts
  </files>
  <behavior>
    - Test 1 (personnel): Seed two schools, each with a person_profile/person_hold/instructor_currency/instructor_qualification/no_show/student_course_enrollment/instructor_experience row. As school A user, SELECT each table returns only school A's row (count = 1, not 2). Same negative test for school B.
    - Test 2 (aircraft): Seed two schools with aircraft + aircraft_engine + aircraft_equipment + flight_log_entry rows. As school A user, SELECT each returns school A only. Asserting flight_log_entry append-only: as school A user, attempt UPDATE on a flight_log_entry → expect 0 rows updated (RLS denies). Attempt DELETE → expect error from hard-delete blocker trigger.
    - Test 3 (views — CRITICAL Pitfall 2 + 9): Seed BOTH schools with ≥1 baseline + ≥1 flight row in flight_log_entry. As school A user, SELECT FROM aircraft_current_totals → returns ONLY school A's aircraft (positive assertion: rows.length === 1). Negative assertion: no row in result has school B's aircraft_id. Repeat for aircraft_engine_current_totals.
    - All three test files run via `pnpm --filter @part61/rls-tests test` in <30s combined.
  </behavior>
  <action>
    Replicate the pattern in `tests/rls/cross-tenant.test.ts` and `tests/rls/harness.ts`. For each new test file:

    1. Use `seedTwoSchools()` from harness — extend it if needed to also seed Phase 2 rows (or seed inline within each test file's `beforeAll`).

    2. Use raw postgres-js (NOT drizzle, NOT supabase-js) per Phase 1 decision — the harness already provides `asUserOf(schoolId, role)` which does `set role authenticated` + `set request.jwt.claims = ...` + `set local app.school_id = ...`.

    3. For the views test (Pitfall 9), seed BOTH schools with realistic flight_log_entry data:
       - School A: 1 aircraft with baseline (hobbs_in=1000.0, tach_in=900.0) + 1 flight row (hobbs_out=1000.0, hobbs_in=1001.5, tach_out=900.0, tach_in=901.4, airframe_delta=1.5)
       - School B: same shape, different ids
       - Then assert school A user sees current_hobbs = 1001.5 for their aircraft AND zero rows for school B's aircraft_id.

    4. Append-only assertion (Test 2):
       ```ts
       const result = await sqlA`update public.flight_log_entry set notes = 'tampered' where school_id = ${schoolBId}`;
       expect(result.count).toBe(0); // RLS hides the row
       await expect(sqlA`delete from public.flight_log_entry where id = ${schoolAEntryId}`).rejects.toThrow(/hard delete/i);
       ```

    5. If the harness seeding helpers don't yet exist for Phase 2 tables, ADD a `seedPhase2Fixtures(schoolId)` helper inline at the top of phase2-aircraft.test.ts and re-use it.

    Run `pnpm --filter @part61/rls-tests test` after each file to keep the failure surface small.
  </action>
  <verify>
    <automated>cd "/Users/christopher/Desktop/Part 61 School" && . scripts/dev-env.sh && pnpm --filter @part61/rls-tests test -- --run phase2</automated>
  </verify>
  <done>All three phase2 RLS test files pass; the views test specifically covers BOTH the positive (school A sees own data) and negative (school A sees zero rows from school B) assertions; append-only contract on flight_log_entry is verified by both UPDATE-blocked-by-RLS and DELETE-blocked-by-trigger assertions.</done>
</task>

</tasks>

<verification>
- `pnpm -r typecheck && pnpm -r lint` passes from repo root
- `supabase db reset` applies the migration cleanly twice in a row (idempotent)
- `pnpm --filter @part61/rls-tests test` passes the existing Phase 1 cross-tenant suite AND the three new phase2 files
- `grep -r "to: sql\`authenticated\`" packages/db/src/schema/` returns nothing (Pitfall 1)
- `grep -c "security_invoker = true" packages/db/migrations/0002_phase2_personnel_aircraft.sql` returns 2
- `grep -c "grant select on public\..*to authenticated" packages/db/migrations/0002_phase2_personnel_aircraft.sql` returns ≥2
- `grep -c "audit\.attach" packages/db/migrations/0002_phase2_personnel_aircraft.sql` returns ≥10
</verification>

<success_criteria>
- Drizzle schema files for all 14 Phase 2 modules exist and compile
- Hand-authored migration applies cleanly via `supabase db reset` and is idempotent
- Both views are `security_invoker = true` AND have explicit grants
- currency_status SQL function exists and is STABLE
- Three phase2 RLS test files pass with positive AND negative assertions
- All requirement IDs in this plan's frontmatter are addressed at the schema layer
</success_criteria>

<output>
After completion, create `.planning/phases/02-personnel-admin-fleet-primitives/02-01-SUMMARY.md` documenting:
- New tables created (with school/base scoping notes)
- Migration filename(s) and idempotence verification
- View RLS strategy (security_invoker = true)
- Cross-tenant test coverage matrix (table → test file → assertion type)
- Any deviations from Research patterns and why
- Open questions still pending for downstream plans (especially the supabase-js custom-id question for Plan 02)
</output>
